> **ΣΗΜΕΙΩΣΗ — αυτό είναι ΚΛΩΝΟΣ/demo repo** (δημιουργήθηκε 06/09/2026 από το πραγματικό `optikitec-portal` repo, για πειραματισμό σε ξεχωριστό Cloudflare Worker + Google Sheet). Το υπόλοιπο περιεχόμενο αυτού του αρχείου περιγράφει την ιστορία/αποφάσεις του ΠΡΩΤΟΤΥΠΟΥ OptikiTec Portal — παραμένει ακριβές ως προς ΤΟΝ ΚΩΔΙΚΑ (ίδιο codebase τη στιγμή του clone), αλλά το live URL, τα Cloudflare/Google Sheet secrets, και τα «Χειροκίνητα βήματα» ΔΕΝ έχουν ακόμα γίνει σε αυτό το demo — χρειάζεται δικό του πλήρες setup (βλ. SETUP.md).

---

## Demo clone setup — 06/09/2026

Αυτό το repo δημιουργήθηκε ως 1:1 λειτουργικός κλώνος του `optikitec-portal`, με νέο Cloudflare Worker (`portal-demo`, https://portal-demo.chsstathis.workers.dev) + 7 νέα KV namespaces (δικά τους IDs, βλ. wrangler.toml) — καμία κοινή υποδομή με το production OptikiTec. Εξαιρέθηκαν από τον κλώνο τα OptikiTec business docs (οδηγοί PDF, κοστολόγιο, παρουσίαση), τα παλιά design mockups, το ασύνδετο `synergia-app_2/`, και το ορφανό διπλότυπο "Αξιολόγηση Υπαλλήλων.html" (ταυτόσημο με evaluation.html).

**Νέο: εναλλακτικό, απλούστερο Google Sheets auth** (`src/sheets.js`) — ρητή απόφαση χρήστη, το demo project δεν έχει τον org policy περιορισμό που ανάγκασε το production να χρησιμοποιήσει WIF. Πρόσθεσα `getAccessTokenViaServiceAccountKey()` — απλό, κατεβασμένο service account JSON key (`GOOGLE_SERVICE_ACCOUNT_KEY` secret), τυπικό OAuth2 JWT-bearer flow, ΕΝΑ HTTP round-trip αντί για το two-step STS+impersonation του WIF. Το `getAccessToken()` διαλέγει αυτόματα: αν υπάρχει το secret, χρησιμοποιεί το απλό flow· αλλιώς κάνει fallback στο ΑΜΕΤΑΒΛΗΤΟ WIF flow (ίδιο με το production — μηδενικό ρίσκο εκεί, αφού αυτό το αρχείο υπάρχει μόνο σε αυτό το demo repo). Βλ. SETUP.md §3.0 για τα βήματα.

**Bootstrap του Google Sheet (18 tabs + headers) — έτοιμο, `scripts/`**: αντί να
δημιουργήσει κανείς χειροκίνητα τα ~18 φύλλα/tabs με τις σωστές επικεφαλίδες
(βλ. §Χειροκίνητα βήματα παρακάτω, στο κυρίως documentation του production),
έγινε `scripts/build_demo_sheet.py` (openpyxl) — παράγει ένα .xlsx με ΟΛΑ τα
sheets (Άδειες, Υπάλληλοι, TeamLeaders, Backoffice, Directors, Στόλος,
ΑτυχήματαΟχημάτων, Εξοπλισμός, EPass, SIMs, Χρεώσεις, Συνεργεία, Ενημερώσεις,
ΕρωτηματαΔιαθεσιμότητας, ΑπαντησειςΔιαθεσιμότητας, ΕγγραφαΠαράδοσης, Έγγραφα,
AuditLog) και τη σωστή σειρά επικεφαλίδων σε κάθε ένα — οι επικεφαλίδες
αντλήθηκαν απευθείας από τα `*_HEADER_ORDER` constants του production `src/*.js`
(source of truth, όχι από ενδεχομένως ξεπερασμένο prose), ΟΧΙ από το SETUP.md
του production repo. Το `.xlsx` που παρήγαγε το script (`scripts/portal-demo-sheet-bootstrap.xlsx`)
ανέβηκε ήδη στο Google Drive του χρήστη (`s.xronis@optikitec.gr`) ως
"OptikiTec Portal DEMO — Sheet (bootstrap, ανοίξτε με Google Sheets).xlsx".
**Εναπομένον χειροκίνητο βήμα (ο χρήστης):** άνοιγμα του αρχείου στο Google
Drive → δεξί κλικ (ή Αρχείο) → «Άνοιγμα με» → Google Sheets (η αυτόματη
μετατροπή μέσω του Google Drive MCP connector `create_file` απέτυχε με
"Invalid conversion requested" — άγνωστη αιτία, πιθανό server-side όριο του
connector· το raw .xlsx upload λειτούργησε κανονικά, άρα το manual
"Άνοιγμα με Google Sheets" είναι αξιόπιστη εναλλακτική, ένα μόνο κλικ) — αυτό
μετατρέπει το αρχείο σε πραγματικό Google Sheet με όλα τα 18 tabs/headers ήδη
έτοιμα. Μετά: αντιγραφή του Sheet ID από το URL για το `GOOGLE_SHEET_ID`
secret, και μοίρασμα (Share) του Sheet με το email του service account
(`client_email` μέσα στο κατεβασμένο JSON key) ως Editor — βλ. SETUP.md §3.0.

---

# OptikiTec Portal — κατάσταση έργου

Σύστημα αδειών + στόλου + χρεώσεων για την OptikiTec (κατασκευές FTTH, ~40 τεχνικοί).
Cloudflare Worker με Google Sheet ως data store. **Δεν υπάρχει Apps Script για το backend** (Sheets/auth/business logic) — αφαιρέθηκε πλήρως.
Εξαίρεση (ρητή απόφαση χρήστη): υπάρχει ΕΝΑ πολύ μικρό, απομονωμένο Apps Script Web App (`apps-script/EmailRelay.gs`) που κάνει ΜΟΝΟ `MailApp.sendEmail()` όταν το καλεί ο Worker — καμία πρόσβαση στο Sheet, καμία business logic. Επιλέχτηκε αντί για SendGrid/τρίτο πάροχο email. Βλ. `src/email.js` + SETUP.md §2.1.

Live: https://optikitec-portal.s-xronis.workers.dev
Deploy: git push στο main → auto-deploy από Cloudflare.

## Αρχιτεκτονική

| Αρχείο | Ρόλος |
|---|---|
| `src/index.js` | routing, auth, όλα τα API handlers |
| `src/sheets.js` | Google Sheets API v4 + **Workload Identity Federation** auth |
| `src/leaves.js` | άδειες: ισοζύγιο, υποβολή, έγκριση, εργάσιμες ημέρες |
| `src/fleet.js` | στόλος οχημάτων (CRUD) |
| `src/equipment.js` | εξοπλισμός/λοιπά αντικείμενα προς χρέωση (CRUD, ίδιο μοτίβο με fleet.js) |
| `src/epass.js` | κάρτες διοδίων e-pass (CRUD) — ανάθεση σε τεχνικό Ή/ΚΑΙ όχημα ταυτόχρονα |
| `src/sims.js` | κάρτες SIM (CRUD) — αριθμός κάρτας/PIN/PUK + σε ποιο tablet είναι τοποθετημένη, καθαρά πληροφοριακό, ΚΑΜΙΑ ανάθεση/χρέωση σε τεχνικό, νέο φύλλο `SIMs` |
| `src/assignments.js` | χρεώσεις τεχνικών (και οχημάτων, μέσω e-pass) |
| `src/handover.js` | Ψηφιακή υπογραφή εγγράφου παράδοσης/παραλαβής — δημιουργία από TL/Director (snapshot κατοχής) + απομακρυσμένη υπογραφή τεχνικού (canvas), νέο φύλλο `ΕγγραφαΠαράδοσης` |
| `src/documents.js` | tab «Έγγραφα» (TL/Director) — αυτοματοποιημένα επίσημα έγγραφα προσωπικού (π.χ. Αίτηση Άδειας Άνευ Αποδοχών), αυτόματη συμπλήρωση από προφίλ τεχνικού + χειροκίνητα πεδία, αποθήκευση/ιστορικό, ΧΩΡΙΣ ψηφιακή υπογραφή (φυσική στο χαρτί) — backend agnostic ως προς τον τύπο, extensible registry `DOCUMENT_TYPES` στο `hub.html`, νέο φύλλο `Έγγραφα` |
| `src/crews.js` | Σύνθεση Συνεργείων (drag & drop, Backoffice) — αποθήκευση/φόρτωση σύνθεσης ανά ημέρα, νέο φύλλο `Συνεργεία` |
| `src/announcements.js` | tab "Ενημερώσεις/Οδηγίες" — CRUD ανακοινώσεων + προαιρετικό συνημμένο PDF/Word (αποθηκεύεται σε Cloudflare KV `ANNOUNCEMENTS_FILES`, όχι στο Sheet), νέο φύλλο `Ενημερώσεις` |
| `src/availability.js` | tab "Ανακοινώσεις" (ΔΙΑΦΟΡΕΤΙΚΟ από το `announcements.js`/"Ενημερώσεις/Οδηγίες" παραπάνω) — ερωτήματα διαθεσιμότητας επαρχίας Ναι/Όχι, multi-team target, δύο νέα φύλλα `ΕρωτηματαΔιαθεσιμότητας`/`ΑπαντησειςΔιαθεσιμότητας`, βλ. Ρόλοι § Team Leader → Ανακοινώσεις |
| `src/email.js` | καλεί μικρό Apps Script mail relay μέσω HTTP (βλ. `apps-script/EmailRelay.gs`) — **no-op**, λείπουν τα secrets· εξάγει και `logEmailFailure()`, καλείται από ΟΛΑ τα σημεία αποστολής email στο portal για ορατότητα αποτυχιών (βλ. §Νέα ζητούμενα 13/08/2026 #4) |
| `src/tokens.js` | signPayload/verifyPayload (HMAC-SHA256, base64url) — κοινό signed-token σχήμα για session cookies, integration token, ΚΑΙ τα one-click decide-links αδειών (βλ. Εκκρεμότητες #4) |
| `src/reports.js` | εβδομαδιαίες/μηνιαίες email αναφορές ανά TL + μηνιαία αναφορά αδειών προς λογιστή (`sendAccountantMonthlyReport`) (καλείται από `scheduled()`, Cron Triggers) |
| `src/analytics.js` | tab «Αναλυτικά»/KPI (TL+Director) — χρήση αδειών/μήνα, ποσοστό χρήσης στόλου, μ.ο. τεχνικών/ημέρα σε συνεργεία, καθαρός συνδυασμός ήδη υπαρχόντων read paths (`leaves.js`/`fleet.js`/`crews.js`), καμία νέα αποθήκευση |
| `src/backup.js` | tab «Backup» (Director-only) — καθημερινό στιγμιότυπο JSON όλων των φύλλων του Sheet σε Cloudflare KV `SHEET_BACKUPS`, κρατάει τελευταία 14, βλ. §Backup Google Sheet παρακάτω |
| `src/dateutil.js` | `normalizeSheetDate()`/`formatSheetDateEl()`/`serialToIso()`/`resolveSheetDate()` — κοινό βοηθητικό κανονικοποίησης ημερομηνιών διαβασμένων από το Sheet, βλ. §Bugfix garbled ημερομηνιών παρακάτω |
| `index.html` | login (3 tabs), σκούρο fiber-optic theme |
| `hub.html` | όλο το UI μετά το login |
| `evaluation.html` | Αξιολόγηση τεχνικού (TL) — αυτόνομη στατική φόρμα, δεν αγγίζει το Sheet/backend (βλ. Ρόλοι) |
| `manual-ftth.html` | «Εγχειρίδιο FTTH» (TL/Backoffice/Director) — στατικό αρχείο αναφοράς (~13MB, self-contained, δεν αγγίζει το Sheet/backend), ανέβηκε από τον χρήστη 19/08/2026, βλ. Ρόλοι § Team Leader |

Φύλλα στο Sheet: `Άδειες`, `Υπάλληλοι`, `TeamLeaders`, `Backoffice`, `Στόλος`, `Εξοπλισμός`, `EPass`, `SIMs`, `Χρεώσεις`, `Συνεργεία`, `Ενημερώσεις`, `ΕρωτηματαΔιαθεσιμότητας`, `ΑπαντησειςΔιαθεσιμότητας`, `ΕγγραφαΠαράδοσης`, `Έγγραφα`, `AuditLog`, `Ρυθμίσεις`.

## Bugfix: garbled ημερομηνίες ("01/01/46267") σε πίνακες + emails — ΕΓΙΝΕ (24/08/2026)

Αναφορά χρήστη: ημερομηνίες τηλεργασίας εμφανίζονταν σαν "01/01/46267" τόσο
στον πίνακα «Αιτήσεις άδειας» (Άδειες Ομάδας) όσο και στο πραγματικό email
απόφασης ομαδικής τηλεργασίας. Ξεχωριστό, βαρύτερο πρόβλημα από το αρχικό
"η άδεια δεν εμφανίζεται στο ημερολόγιο" — και τα δύο ίδια ρίζα.

**Ρίζα:** το `readSheetAsObjects()` (`src/sheets.js`) δεν περνάει
`valueRenderOption` στο Sheets API v4 GET request — το default είναι
`FORMATTED_VALUE`. Για κελιά με πραγματική μορφοποίηση ημερομηνίας αυτό
επιστρέφει ελληνικό "24/8/2026" (ήδη διαχειρίσιμο). ΑΛΛΑ αν το κελί
προορισμού είχε ήδη μορφοποίηση "General" τη στιγμή της πρώτης εγγραφής
(`appendRow`/`updateCell`/`updateRow`, `valueInputOption=USER_ENTERED`), το
Sheets αποθηκεύει τον σειριακό αριθμό ημέρας (ημέρες από 30/12/1899) ΧΩΡΙΣ
μορφοποίηση ημερομηνίας — η ανάγνωση επιστρέφει τότε bare string π.χ.
"46267". Το `new Date("46267")` το παρερμηνεύει σαν έτος 46267/Ιανουάριος/1ο
→ σε ελληνική μορφή ακριβώς "01/01/46267".

**Fix:** νέο κοινό module `src/dateutil.js` — `normalizeSheetDate(v)`
(δέχεται ISO, ελληνική DD/MM/YYYY, bare Sheets serial, ή ό,τι άλλο κατανοητό
από `Date()`· πάντα επιστρέφει ISO "YYYY-MM-DD") και `formatSheetDateEl(v)`
(το ίδιο + ελληνική μορφή για emails/UI). Εφαρμόστηκε σε ΚΑΘΕ σημείο όπου
Sheet-sourced `StartDate`/`EndDate`/`Deadline`/`PublishAt`/`KteoDate`/
`ServiceDate`/`PickupDate`/`ReturnDate`/`ChargedAt`/`ReleasedAt` είτε (α)
συγκρίνεται ως raw string (`<=`/`>=`/`localeCompare`), είτε (β)
εμφανίζεται/embedding σε email:
- `src/leaves.js`: `getEmployeeBalance().history`, `getTeamLeaderData()`
  (`allLeaves`/`calendarLeaves` — αυτό τροφοδοτεί ΚΑΙ το ημερολόγιο αδειών
  ομάδας ΚΑΙ τα weekly/monthly TL reports), `decideLeaveRequest`/
  `decideLeaveRequestGroup` (email απόφασης — η ακριβής πηγή του bug στο
  screenshot), `searchTeamLeaves`, `getApprovedLeavesInRange` (τροφοδοτεί
  accountant report + `analytics.js`), `computeAvailabilityList`
  (Διαθεσιμότητα Τεχνικών/δεξαμενή Συνεργείων — ήταν functional bug, όχι
  μόνο εμφάνιση), `uploadLeaveAttachment` email.
- `src/index.js`: οι δύο σελίδες επιβεβαίωσης one-click decide-link
  (μεμονωμένη + ομαδική τηλεργασία) — GET confirmation page πριν το POST.
- `src/reports.js`: το τοπικό `fmtDateEl()` (weekly/monthly TL reports,
  accountant report, daily digest) πλέον περνάει από `normalizeSheetDate`
  πρώτα· `fleetAlertLines()` (ΚΤΕΟ/service alerts) διόρθωσε ΚΑΙ τη σύγκριση
  ημερών, όχι μόνο την εμφάνιση.
- `src/availability.js`: το τοπικό `fmtDateEl()`, `isPublished()`/
  `isExpired()`, `submitAvailabilityAnswer()`, `listAvailabilityQueries()`/
  `listMyAvailabilityQueries()` (Deadline/PublishAt fields + sort),
  `publishScheduledAvailabilityQueries()` (cron due-filter).
- `src/assignments.js`: `getVehicleCalendarEvents()` (Ημερολόγιο τεχνικού —
  PickupDate/ReturnDate/KteoDate/ServiceDate + το ChargedAt/ReleasedAt range
  check).
- `hub.html`: `normalizeDateStr()` επεκτάθηκε με ανίχνευση bare Sheets
  serial (ίδιο εύρος/formula με το backend) — δεύτερη γραμμή άμυνας
  client-side· `fmtDate()` περνάει πλέον πάντα πρώτα από αυτήν·
  `renderLeaveRequestsTable()`'s ομαδικό τηλεργασίας sort/join διορθώθηκε
  να ταξινομεί με βάση την κανονικοποιημένη τιμή (όχι raw `a.StartDate`).

`src/analytics.js`/`src/fleet.js`/`src/crews.js` ελέγχθηκαν — δεν
χρειάστηκαν αλλαγές (το analytics.js καταναλώνει ήδη διορθωμένα δεδομένα
από `getApprovedLeavesInRange`, το fleet.js δεν κάνει καμία σύγκριση/
εμφάνιση ημερομηνίας το ίδιο — μόνο περνάει τιμές, η ΚΤΕΟ/service λογική
ζει στο `reports.js`/`hub.html`· το crews.js κάνει μόνο equality lookup σε
`Date`, χαμηλό ρίσκο, δεν αγγίχτηκε).

**Δεύτερο, ξεχωριστό bug την ΙΔΙΑ μέρα (24/08/2026) μετά το πρώτο deploy:**
ο χρήστης ανέφερε νέα γραμμές τηλεργασίας να εμφανίζονται σαν "2026-16-09"
(μήνας/ημέρα αντεστραμμένα, ΜΕΣΑ σε ήδη dash-separated μορφή) αντί για
"16/09/2026" — διαφορετική ρίζα από το serial-number bug παραπάνω: το πρώτο
branch του `normalizeSheetDate()`/`normalizeDateStr()` έβλεπε οτιδήποτε στη
μορφή `\d{4}-\d{2}-\d{2}` και το εμπιστευόταν τυφλά ως ήδη-σωστό YYYY-MM-DD
— αλλά ορισμένα κελιά είχαν custom μορφοποίηση ημερομηνίας "yyyy-dd-mm"
(αντί για "yyyy-mm-dd") τη στιγμή που γράφτηκαν, οπότε η FORMATTED_VALUE
επέστρεφε πιστά αυτή τη λάθος-σειράς μορφή. Fix (`src/dateutil.js` +
`hub.html` `normalizeDateStr()`, ίδια λογική και στα δύο): το ISO-shaped
branch ελέγχει τώρα αν το μεσαίο group (θέση μήνα) είναι έγκυρος μήνας
1-12· αν όχι αλλά το ΤΕΛΕΥΤΑΙΟ group είναι, κάνει swap. Επιβεβαιωμένο με
Node ότι δεν σπάει ήδη-σωστές τιμές (π.χ. "2026-02-09" παραμένει Φεβρουάριος
9, δεν αλλάζει) — μόνο η αντεστραμμένη περίπτωση διορθώνεται.

**Τρίτο πέρασμα, ΙΔΙΑ μέρα (24/08/2026) — ριζική λύση, όχι πια ευρετική:** ο
χρήστης ανέφερε ότι ΜΕΡΙΚΕΣ από τις ίδιες γραμμές τηλεργασίας (Σεπτεμβρίου
2/3/9/10) εξακολουθούσαν να εμφανίζονται λάθος ΜΕΤΑ το δεύτερο fix —
"09/02/2026" (Φεβρουάριος 9) αντί για "02/09/2026" (Σεπτέμβριος 2), κλπ.
Ρίζα: όταν ΚΑΙ οι δύο θέσεις (ημέρα/μήνας) ενός "yyyy-dd-mm"-μορφοποιημένου
κελιού είναι ≤12 (π.χ. "02" vs "09"), ΚΑΜΙΑ ευρετική πάνω στο ήδη-
μορφοποιημένο FORMATTED_VALUE string δεν μπορεί να ξέρει με σιγουριά ποια
θέση είναι ημέρα και ποια μήνας — το δεύτερο fix παραπάνω λύνει ΜΟΝΟ την
περίπτωση όπου η μία θέση είναι σαφώς άκυρη ως μήνας (>12).

Οριστική λύση: νέα `getUnformattedColumns(env, sheetName, headerNames)`
(`src/sheets.js`) — διαβάζει τις ζητούμενες στήλες με
`valueRenderOption=UNFORMATTED_VALUE` αντί για το FORMATTED_VALUE default·
για date-typed κελιά αυτό επιστρέφει ΠΑΝΤΑ τον πραγματικό σειριακό αριθμό
ημέρας ως JS number — καμία εξάρτηση από μορφοποίηση/locale, καμία ασάφεια
(ΔΕΝ εφαρμόστηκε global σε όλο το `readSheetAsObjects()` — θα άλλαζε τον
τύπο ΚΑΙ άλλων πεδίων όπως boolean-like `Away`/`Status` στήλες που ήδη
συγκρίνονται ως strings `"TRUE"` σε πολλά σημεία του κώδικα, ρίσκο εκτός
scope). Νέα `serialToIso()`/`resolveSheetDate(rawValue, formattedValue)`
(`src/dateutil.js`) — προτιμά ΠΑΝΤΑ τον raw σειριακό αριθμό όταν υπάρχει,
fallback στο (ασαφές) `normalizeSheetDate(formattedValue)` μόνο αν λείπει.

Νέα `readLeavesRows(env)` (`src/leaves.js`, module-private) αντικατέστησε
ΟΛΕΣ τις 12 κλήσεις `readSheetAsObjects(env, SHEET_LEAVES)` σε αυτό το
αρχείο — κάνει ΜΙΑ επιπλέον κλήση Sheets API (StartDate+EndDate μαζί, είναι
γειτονικές στήλες) και επιστρέφει τις γραμμές με το StartDate/EndDate ΗΔΗ
οριστικά σωστά. Καμία αλλαγή χρειάστηκε στους callers (`getEmployeeBalance`,
`getTeamLeaderData`, `decideLeaveRequest`/`decideLeaveRequestGroup`,
`searchTeamLeaves`, `getApprovedLeavesInRange`, `computeAvailabilityList`,
κλπ.) — το fix διαπερνάει αυτόματα παντού απ' όπου ήδη περνούσε το
`allLeaves`.

**Γνωστό, τεκμηριωμένο residual όριο:** η ίδια ριζική λύση (UNFORMATTED_VALUE)
εφαρμόστηκε ΜΟΝΟ στο φύλλο `Άδειες` (StartDate/EndDate) — τα υπόλοιπα
Sheet-sourced date πεδία (`Deadline`/`PublishAt` στο `availability.js`,
`KteoDate`/`ServiceDate`/`PickupDate`/`ReturnDate`/`ChargedAt`/`ReleasedAt`
στο `assignments.js`/`reports.js`) έχουν ακόμα ΜΟΝΟ το δεύτερο-περάσματος
heuristic fix (swap μόνο όταν η μία θέση είναι σαφώς άκυρη ως μήνας) — θα
μπορούσαν θεωρητικά να έχουν το ΙΔΙΟ "και οι δύο θέσεις ≤12" ασαφές
σενάριο αν κάποιο αντίστοιχο κελί έχει επίσης custom "yyyy-dd-mm" μορφή.
Δεν αναφέρθηκε ακόμα πρόβλημα εκεί — αν εμφανιστεί, ίδιο fix (νέο
`readXxxRows()` wrapper με `getUnformattedColumns`) μπορεί να επεκταθεί στο
αντίστοιχο αρχείο/στήλες.

Σημείωση ονοματολογίας: το tab που στο UI λέγεται **"Στην Κατοχή μου"** (οχήματα/εξοπλισμός/e-pass ανατεθειμένα σε σένα) αντιστοιχεί στο ίδιο backend/Sheet `Χρεώσεις` — μετονομάστηκε μόνο στο UI (ρητή απόφαση χρήστη) γιατί το "Χρεώσεις" μπέρδευε με χρηματική χρέωση και το "Αναθέσεις" ήδη χρησιμοποιείται για έργα. Ο κώδικας (key: 'charges', sheet name, API paths) ΔΕΝ άλλαξε, μόνο το εμφανιζόμενο label.

## Ρόλοι

- **Τεχνικός** — login με employeeID + 4ψήφιο PIN (KV `TECHNICIAN_AUTH`, hashed). Tabs: Οι Άδειές μου, Στην Κατοχή μου, Ημερολόγιο, Ενημερώσεις/Οδηγίες, Ανακοινώσεις.
  **Ημερολόγιο — ΕΓΙΝΕ** (item #9, 15/08/2026, σχεδιάστηκε μέσω διευκρινιστικών ερωτήσεων πριν την υλοποίηση): νέο tab `techcalendar`, **ΜΟΝΟ στο δικό του tab** (ρητή απόφαση χρήστη — ΟΧΙ μέσα στο Προφίλ Τεχνικού του TL/Director, διαφορετικό scope από τις «Απαντήσεις Ανακοινώσεων» που ΕΙΝΑΙ εκεί). Μηνιαίο ημερολόγιο (ίδιο grid component `mountLeaveCalendar`/`mountTechCalendar` με το ημερολόγιο αδειών ομάδας του TL, γενικευμένο για πολλαπλούς τύπους γεγονότων) που δείχνει: (1) άδειες/τηλεργασία (εγκεκριμένες + εκκρεμείς, οι απορριφθείσες αποκλείονται — reuse `getEmployeeBalance()`, ίδια πηγή με «Οι Άδειές μου»), (2) παραλαβή/επιστροφή οχήματος, (3) ΚΤΕΟ/Service του οχήματος όσο ήταν ανατεθειμένο πάνω του — και τα τρία επιλέχτηκαν ρητά από τον χρήστη ανάμεσα σε προτεινόμενες επιλογές (ατυχήματα οχημάτων ΚΑΙ προθεσμίες Ανακοινώσεων ΔΕΝ επιλέχθηκαν, εκτός scope).
  `GET /api/calendar/my` (`src/index.js` → `handleTechnicianCalendar`, ΜΟΝΟ `identity.type === "technician"`) συνδυάζει `getEmployeeBalance()` (άδειες) + νέα `getVehicleCalendarEvents()` (`src/assignments.js`, οχήματα) — καμία νέα λογική υπολογισμού αδειών, καθαρός συνδυασμός.
  **Νέες στήλες `PickupDate`/`ReturnDate`** στο `Χρεώσεις` (χειροκίνητο βήμα, βλ. §Χειροκίνητα βήματα) — χρειάστηκαν επειδή το `PickupMileage`/`ReturnMileage` είναι μόνο αριθμοί χωρίς δική τους ημερομηνία (η στιγμή καταχώρησης δεν ταυτίζεται πάντα με το `ChargedAt`/`ReleasedAt` της χρέωσης — π.χ. ο τεχνικός μπορεί να επιστρέψει το όχημα πριν αλλάξει ο TL τον ανατεθειμένο οδηγό στο Στόλος). `setVehiclePickupMileage()`/`setVehicleReturnMileage()` γράφουν τώρα και την ημερομηνία αυτόματα (σημερινή, server-side).
  **ΚΤΕΟ/Service — γνωστός περιορισμός (ρητά τεκμηριωμένος, αποδεκτός):** το φύλλο `Στόλος` κρατά ΜΟΝΟ την τρέχουσα επόμενη ημερομηνία ΚΤΕΟ/Service ανά όχημα, όχι ιστορικό αλλαγών. Για ΚΛΕΙΣΤΕΣ (παλιές) αναθέσεις γίνεται best-effort έλεγχος αν η τρέχουσα ημερομηνία πέφτει μέσα στο διάστημα `[ChargedAt, ReleasedAt]` — μπορεί να μην αντιστοιχεί πια αν η ημερομηνία άλλαξε έκτοτε. Για ΑΝΟΙΧΤΗ ανάθεση (το όχημα είναι ΤΩΡΑ πάνω του) δεν υπάρχει πάνω όριο — κάθε επερχόμενη ημερομηνία εμφανίζεται, πιο χρήσιμο forward-looking παρά ιστορικό.
  Άμυνα: το `/api/auth/technician` απορρίπτει login (403) αν το EmployeeID ανήκει σε γραμμή του Υπάλληλοι με `Team == "Back Office"` ή `"Team Leader"` — ακόμα κι αν υπάρχει (παλιό) PIN seeded στο KV για αυτό το ID, ώστε staff/TL να ΜΗΝ μπορούν να μπουν σαν τεχνικοί από αυτή την καρτέλα. Ο ρόλος τους δίνεται αποκλειστικά μέσω Google SSO.
  **Παραλαβή οχήματος — ΕΓΙΝΕ** (ρητή απαίτηση χρήστη, item #4 στα «Νέα ζητούμενα 10/08/2026»): στο tab «Στην Κατοχή μου», κάθε ενεργή χρέωση οχήματος χωρίς ήδη καταγεγραμμένη παραλαβή δείχνει κουμπί «Καταγραφή παραλαβής» → μικρή φόρμα χιλιομέτρων + πολλαπλών φωτογραφιών (`hub.html` → `showPickupForm()`/`submitVehiclePickup()`). Μία φωτογραφία ανά HTTP κλήση (`POST /api/charges/pickup/photo/upload`, ίδιο μοτίβο με τα υπόλοιπα multi-file uploads), τα χιλιόμετρα καταχωρούνται ξεχωριστά (`POST /api/charges/pickup/mileage`).
  **Επιστροφή οχήματος — ΕΓΙΝΕ** (ρητή απαίτηση χρήστη, ΑΝΑΘΕΩΡΕΙ την παλιότερη απόφαση "μόνο παραλαβή, καμία καταγραφή διαφοράς χλμ"): μόλις έχουν καταχωρηθεί χιλιόμετρα παραλαβής, η ίδια στήλη «Επιστροφή» (ενεργές χρεώσεις) δείχνει κουμπί «Καταγραφή επιστροφής» → φόρμα με ΜΟΝΟ χιλιόμετρα (`showReturnForm()`/`submitVehicleReturn()` στο `hub.html`, `POST /api/charges/pickup/return-mileage`). Ο ΙΔΙΟΣ ο τεχνικός το καταχωρεί όποτε επιστρέφει φυσικά το όχημα — ανεξάρτητα από το πότε ο TL θα αλλάξει τον ανατεθειμένο οδηγό στο Στόλος (`setVehicleReturnMileage()` στο `src/assignments.js` δεν απαιτεί η χρέωση να είναι ακόμα ανοιχτή, μόνο να υπάρχει ήδη `PickupMileage`). Κάθε καταγραφή ενημερώνει αυτόματα και το `CurrentMileage` του οχήματος στο Στόλος (βλ. παρακάτω) — έτσι ο TL/ο επόμενος τεχνικός βλέπει πάντα την πιο πρόσφατη γνωστή ένδειξη χωρίς χειροκίνητο συγχρονισμό. Και η παραλαβή ΚΑΙ η επιστροφή, μαζί με την Απαλλαγή του οχήματος, φαίνονται πλέον και στο «Ιστορικό χρεώσεων» (ίδιο tab, χρεώσεις που έχουν κλείσει) — ρητή απαίτηση χρήστη. Λεπτομέρειες backend: βλ. Ρόλοι § Team Leader → Στόλος Οχημάτων.
- **Team Leader** — Google SSO· email στο φύλλο `TeamLeaders`. Tabs: Άδειες Ομάδας, Στόλος Οχημάτων, Εξοπλισμός, e-pass, Νέος Τεχνικός, Αξιολόγηση, Εγχειρίδιο FTTH, Ενημερώσεις/Οδηγίες, Ανακοινώσεις, Οι Άδειές μου, Στην Κατοχή μου.
  **Εγχειρίδιο FTTH — ΕΓΙΝΕ** (19/08/2026, ρητή απαίτηση χρήστη): νέο tab «Εγχειρίδιο FTTH» στην ενότητα «Ομάδα» του sidebar — ανοίγει σε ΝΕΑ καρτέλα το `manual-ftth.html` (`external: '/manual-ftth.html'`, ίδιο μοτίβο με το `evaluation.html`, `window.open`). Το αρχείο είναι το ΑΥΤΟΥΣΙΟ `index.html` που ανέβασε ο χρήστης (τίτλος «Ενοποιημένο Εγχειρίδιο FTTH — OSP Contractors / ΟΤΕ»), αποθηκεύτηκε με ΔΙΑΦΟΡΕΤΙΚΟ όνομα `manual-ftth.html` ώστε να ΜΗΝ συγκρουστεί/αντικαταστήσει το υπάρχον `index.html` (login σελίδα) — εξυπηρετείται ως static asset από το ήδη υπάρχον `[assets] directory = "./"` του `wrangler.toml`, καμία αλλαγή χρειάστηκε εκεί. 100% αυτόνομο (δικό του CSS/JS/αναζήτηση, δεν αγγίζει το Sheet/backend/session — ίδιο σκεπτικό με το `evaluation.html`), απλά ένα μεγάλο (~13MB) αρχείο αναφοράς. Ορατό ΜΟΝΟ σε TL/Backoffice/Director (ρητή επιλογή χρήστη — ίδιο σύνολο ρόλων που ήδη έχουν ενότητα «Ομάδα» στο sidebar· ΟΧΙ τεχνικός, δεν ζητήθηκε). Νέο εικονίδιο `manual: 'book-2'` στο `NAV_ICON_MAP`.
  **Ενημέρωση περιεχομένου 20/08/2026 — ΕΓΙΝΕ** (ρητή απαίτηση χρήστη, ο ίδιος ανέβασε το πρωτότυπο .docx «ΚΑΤΑΣΚΕΥΗ ΚΤΙΡΙΟΥ ΚΑΙ LAST DROP ΑΙΤΗΜΑΤΑ»): σύγκριση του .docx με το ήδη ενσωματωμένο περιεχόμενο έδειξε ότι το ίδιο έγγραφο ήταν ήδη μία από τις «Πηγές Σύνθεσης» του manual, αλλά με ΔΙΑΦΟΡΕΤΙΚΟΥΣ αριθμούς σε δύο σημεία — ρητή απόφαση χρήστη: ενημερώθηκαν/αντικαταστάθηκαν με τα νέα στοιχεία, ΟΧΙ προσθήκη ως ξεχωριστό κεφάλαιο. §2.3.1 «Φόρτος Ανάθεσης ανά Ημέρα»: το in-house όριο άλλαξε από τον παλιό συνδυασμό «2 ολόκληρα ή 1+2 μισά/last-drop» (με διάκριση μικρό/μεγάλο κτίριο) σε ενιαίο κανόνα «έως 10 όροφοι συνολικά/ημέρα». §2.3.2 «Διάρκεια & Ωράρια Ραντεβού»: η παλιά κατηγοριοποίηση (~3h εξωτερική όδευση/4+ όροφοι, ~2h <4 όροφοι, ~1h last drop/μισά) αντικαταστάθηκε από πιο αναλυτική (1,5-2h BEP-BMO ONLY με όδευση <20μ · 2,5-5h τα υπόλοιπα · ~1h last drop · 30΄-2h μισά έργα) και το ωράριο ραντεβού από «08:30-09:30 & 11:30-12:30» σε «08:00-16:00 in-house / από 09:00 εξωτερικός συνεργάτης / 09:00-17:00 επαρχία (Αίγιο-Τρίπολη-Λαμία)». Τα κεφάλαια 2.3.3-2.3.6 και 4.1-4.3 (επιλογή έργων, προϋποθέσεις FSM, ακυρώσεις wholesale/retail, Hotline) ελέγχθηκαν έναντι του .docx και βρέθηκαν ήδη πιστά ενσωματωμένα — καμία αλλαγή εκεί. Badge «Έκδοση» 1.0→1.1 + νέο badge «Ενημέρωση Αύγ 2026» στο hero, και σημείωση αναθεώρησης κάτω από τη λίστα «Πηγές Σύνθεσης» εξηγώντας ποιες ενότητες άλλαξαν και γιατί. **Τεχνικό σημείωμα:** το `manual-ftth.html` (13MB) δεν μπορεί να επεξεργαστεί in-place μέσα στον φάκελο `optikitec-portal` (ο OneDrive-mounted φάκελος επιτρέπει μόνο δημιουργία/διαγραφή αρχείων, όχι άνοιγμα υπάρχοντος σε write mode) — η επεξεργασία έγινε σε αντίγραφο στο scratch/outputs directory και μετά `rm` + αντικατάσταση του αρχείου στο repo πριν το commit.
  Στόλος Οχημάτων: κοινός στόλος (όχι ανά ομάδα), πλήρες CRUD (`src/fleet.js`). Αναζήτηση με πινακίδα ή τεχνικό (live client-side φίλτρο, ήδη υλοποιημένο). Πεδίο «Τύπος» σε dropdown (`listVehicleTypes()` — μοναδικές τιμές ήδη καταχωρημένες, χωρίς hardcoded λίστα, ίδιο μοτίβο με την Κατηγορία στο Εξοπλισμό) + επιλογή «Άλλος τύπος...». Νέα στήλη «e-pass» στον πίνακα οχημάτων (`listVehicleEpassMap()`) δείχνει αν το όχημα έχει ΤΩΡΑ e-pass ανατεθειμένο πάνω του — πηγή είναι απευθείας το `AssignedVehicleId` του φύλλου `EPass` (όχι το `Χρεώσεις`, που είναι απλά ιστορικό/ειδοποιήσεις). Read-only ένδειξη, καμία ανάθεση e-pass από το tab Στόλου — αυτό παραμένει στο tab e-pass.
  **Χρέωση/αποχρέωση, απαλλαγή, ατυχήματα — ΕΓΙΝΕ** (item #4 στα «Νέα ζητούμενα 10/08/2026», βλ. παρακάτω): πριν την υλοποίηση διαπιστώθηκε ότι η χρέωση/αποχρέωση οχήματος ΗΔΗ περνούσε από τον μηχανισμό `Χρεώσεις` (μέσω `syncVehicleAssignment()`, καλείται από `addVehicle()`/`updateVehicle()` κάθε φορά που αλλάζει το `AssignedTo`) — ίδια επίσημη ανάθεση με ημερομηνία/ιστορικό/εμφάνιση στο «Στην Κατοχή μου» όπως ο Εξοπλισμός, απλά το UI είναι ένα απλό dropdown αντί για ξεχωριστό κουμπί «Ανάθεση». Καμία αλλαγή χρειάστηκε εκεί — η παλιά σημείωση σε αυτό το αρχείο που έλεγε το αντίθετο ήταν ξεπερασμένη.
  Νέο πεδίο **Απαλλαγή** (`Deductible`, ποσό σε €) στη φόρμα οχήματος — σταθερή τιμή ανά όχημα (ρητή απόφαση χρήστη), καθαρά πληροφοριακό, καμία λογική υπολογισμού ασφάλισης. Εμφανίζεται και ως στήλη στον πίνακα οχημάτων, ΚΑΙ στο ιστορικό χρεώσεων του τεχνικού (βλ. Ρόλοι § Τεχνικός).
  Νέο πεδίο **Τρέχοντα χιλιόμετρα** (`CurrentMileage`) στη φόρμα οχήματος — ρητή απαίτηση χρήστη: ο TL το καταχωρεί/διορθώνει ελεύθερα (αρχική τιμή αναφοράς), και ενημερώνεται ΑΥΤΟΜΑΤΑ κάθε φορά που ένας τεχνικός καταγράφει επιστροφή οχήματος (`updateVehicleCurrentMileage()` στο `src/fleet.js`, καλείται από `setVehicleReturnMileage()` στο `src/assignments.js`) — αλυσίδα παραλαβής/επιστροφής ανά τεχνικό, χωρίς να χρειάζεται κάποιος να κοιτάει το ιστορικό για να βρει την τελευταία ένδειξη. Εμφανίζεται και ως στήλη «Χλμ» στον πίνακα οχημάτων.
  **Στοιχεία ασφάλισης οχήματος — ΕΓΙΝΕ** (item #119, 15/08/2026): 3 νέα πεδία στη φόρμα οχήματος — `InsuranceCompany` (ασφαλιστική εταιρεία), `PolicyNumber` (αριθμός συμβολαίου), `InsuranceRenewalDate` (ημερομηνία ανανέωσης) — καθαρά πληροφοριακά, ίδιο μοτίβο με το `Deductible`, ΚΑΜΙΑ λογική alert/ειδοποίησης λήξης (δεν ζητήθηκε, σε αντίθεση με το ΚΤΕΟ/Service — αν χρειαστεί ποτέ, να ζητηθεί ρητά). **Ρητά ΔΕΝ θα προστεθεί ποτέ** (15/08/2026, ρητή απάντηση χρήστη σε πρόταση Claude): οι ασφάλειες πληρώνονται αυτόματα, άρα δεν υπάρχει πραγματικό ρίσκο λήξης που να χρειάζεται ειδοποίηση — διαφορετικό σκεπτικό από το ΚΤΕΟ/Service, που ΔΕΝ ανανεώνεται αυτόματα. Στον πίνακα οχημάτων εμφανίζονται ΜΑΖΙ σε ΜΙΑ στήλη «Ασφάλιση» (`fleetInsuranceCell()` στο `hub.html`, όχι 3 ξεχωριστές στήλες — ο πίνακας ήταν ήδη πυκνός) — η ημερομηνία ανανέωσης δείχνεται ως badge με το ίδιο 30-ημερών κατώφλι/χρωματισμό `fleetDateBadge()` που ήδη χρησιμοποιείται για ΚΤΕΟ/Service. Στην inline επεξεργασία γραμμής, τα 3 πεδία μοιράζονται ΜΙΑ στήλη (3 στοιβαγμένα inputs). Καμία εμφάνιση στο ιστορικό χρεώσεων τεχνικού (δεν ζητήθηκε — το `Deductible` εμφανίζεται εκεί γιατί αφορά ζημιά που μπορεί να προκύψει στον τεχνικό, η ασφάλιση όχι).
  Νέα κάρτα **«Ατυχήματα οχημάτων»** στο κάτω μέρος του tab Στόλος — ο TL/Director επιλέγει όχημα, βλέπει ιστορικό ατυχημάτων και μπορεί να καταγράψει νέο (ημερομηνία, οδηγός τη στιγμή του ατυχήματος — ελεύθερη επιλογή από dropdown τεχνικών, ΜΠΟΡΕΙ να διαφέρει από τον σημερινό ανατεθειμένο οδηγό, σχόλια, προαιρετικό συνημμένο ΕΝΑ αρχείο PDF/εικόνα). Ρητή απόφαση χρήστη: ξεχωριστό/προαιρετικό συμβάν, ΔΕΝ είναι μέρος της φόρμας παραλαβής· καταγράφεται ΜΟΝΟ από TL/Director (ίδιο gate `requireTeamLeader` με το υπόλοιπο tab Στόλος — Backoffice δεν έχει καν πρόσβαση στο tab, συνεπές). Νέο φύλλο `ΑτυχήματαΟχημάτων` (`src/fleet.js` → `reportVehicleAccident()`/`listVehicleAccidents()`/`uploadAccidentAttachment()`/`getAccidentAttachment()`) — χειροκίνητο βήμα, βλ. §Χειροκίνητα βήματα. Συνημμένο σε δεύτερο βήμα μετά τη δημιουργία (`POST /api/fleet/accident` -> `POST /api/fleet/accident/attachment/upload?accidentId=...`), ίδιο δίβημα με το δικαιολογητικό αναρρωτικής άδειας.
  Νέα **«Παραλαβή οχήματος»** (χιλιόμετρα + πολλαπλές φωτογραφίες) — ρητή απόφαση χρήστη: το συμπληρώνει ο ΙΔΙΟΣ ο τεχνικός, όχι ο TL, από το δικό του tab «Στην Κατοχή μου» (βλ. Ρόλοι § Τεχνικός). Νέες στήλες `PickupMileage`/`PickupPhotos` στο φύλλο `Χρεώσεις` (`PickupPhotos` = JSON array από `{fileKey, fileName, fileType}`, πολλαπλές φωτογραφίες ανά χρέωση) — χειροκίνητο βήμα. `src/assignments.js` → `setVehiclePickupMileage()`/`uploadVehiclePickupPhoto()` (καλείται μία φορά ανά αρχείο, ίδιο μοτίβο "μία κλήση ανά φωτογραφία" με τα υπόλοιπα multi-file uploads)/`getVehiclePickupPhoto()`. Ισχύει ΜΟΝΟ για ανοιχτές χρεώσεις `Type === "Όχημα"` — ο έλεγχος ιδιοκτησίας (η χρέωση ανήκει στον συνδεδεμένο τεχνικό) γίνεται μέσα στο `src/assignments.js`, όχι στο `index.js`.
  **«Επιστροφή οχήματος» (χιλιόμετρα) — ΕΓΙΝΕ**, νέα στήλη `ReturnMileage` στο τέλος του `Χρεώσεις` (χειροκίνητο βήμα) — βλ. λεπτομέρειες στο Ρόλοι § Τεχνικός.
  **Αποθήκευση φωτογραφιών/συνημμένων** — νέο KV namespace `FLEET_FILES` (`wrangler.toml`), κοινό και για τις φωτογραφίες παραλαβής και για το συνημμένο ατυχήματος — R2 δεν ήταν ενεργοποιημένο στο Cloudflare account όταν ρωτήθηκε ο χρήστης, ίδια απόφαση/περιορισμός με τα υπόλοιπα KV namespaces συνημμένων.
  **Πρόσβαση λήψης φωτογραφιών παραλαβής**: ο ίδιος ο τεχνικός (owner της χρέωσης) Ή TL/Director — `GET /api/charges/pickup/photo` ελέγχει και τα δύο (`handleChargePickupPhotoFile` στο `src/index.js`), ίδιο σκεπτικό με τα υπόλοιπα συνημμένα όπου συνυπάρχουν owner + staff πρόσβαση. **Προβολή από TL/Director — ΕΓΙΝΕ** (ρητή απαίτηση χρήστη): links προς τις φωτογραφίες (ίδιο endpoint, ανοίγουν σε νέα καρτέλα) εμφανίζονται πλέον και στην «Αναζήτηση τεχνικού» του tab Εξοπλισμός και στο «Προφίλ Τεχνικού» — δίπλα σε κάθε ενεργή χρέωση οχήματος, στη νέα στήλη «Παραλαβή» (`chargeHistoryPickupCell()` στο `hub.html`, reused και στο δικό του «Στην Κατοχή μου»/ιστορικό). Καμία αλλαγή στο backend gate — ήδη επέτρεπε TL/Director, έλειπε μόνο το UI.
  Αξιολόγηση: tab που ανοίγει σε ΝΕΑ καρτέλα το `evaluation.html` (χρήστης το έφτιαξε, το πέρασε στο repo) — αυτόνομη multi-tab φόρμα βαθμολόγησης τεχνικού ανά ειδικότητα (Αυτοψία/Χωματουργικό/Εμφύσηση/Κατασκευή/Κόλληση/Backoffice), 100% client-side (καμία κλήση API/backend, καμία αποθήκευση σε Sheet) — ο TL τη συμπληρώνει και κάνει `window.print()` για PDF. Καμία ενσωμάτωση με δεδομένα του portal ακόμα (π.χ. δεν προσυμπληρώνει όνομα τεχνικού) — αν χρειαστεί ιστορικό/αποθήκευση αξιολογήσεων στο Sheet, είναι ξεχωριστό, μεγαλύτερο feature, να το ζητήσει ρητά ο χρήστης.
  Εξοπλισμός: κοινή λίστα εργαλείων/λοιπών αντικειμένων (όχι ανά ομάδα, ίδιο με Στόλο) — CRUD + ανάθεση σε τεχνικό ανοίγει αυτόματα χρέωση στο `Χρεώσεις` (ίδιος μηχανισμός με τα οχήματα). Νέο φύλλο `Εξοπλισμός` — χειροκίνητο βήμα, βλ. SETUP.md.
  Αναζήτηση τεχνικού + PDF παράδοσης/παραλαβής — ΕΓΙΝΕ: στο κάτω μέρος του tab Εξοπλισμός, ο TL επιλέγει τεχνικό από dropdown και βλέπει ό,τι έχει ΤΩΡΑ πάνω του (`GET /api/equipment/technician-holdings?employeeId=...`, `getTechnicianHoldings()` στο `src/assignments.js` — μόνο ανοιχτές χρεώσεις, δηλ. `ReleasedAt` κενό). Ρητή απόφαση χρήστη: το αποτέλεσμα δείχνει ΟΛΑ όσα έχει πάνω του (οχήματα + εξοπλισμός + e-pass μαζί, ίδιο σύνολο με το δικό του tab «Στην Κατοχή μου»), όχι μόνο εξοπλισμό — απλή αναζήτηση πάνω στο υπάρχον `Χρεώσεις`, καμία αλλαγή σχήματος. Κουμπί «Εξαγωγή PDF παράδοσης/παραλαβής» δημιουργεί το έγγραφο με **browser print** (`window.open` + `window.print()`, ίδιο μοτίβο με το export ιστορικού αδειών τεχνικού στο tab Άδειες Ομάδας και με το `evaluation.html` — καμία εξωτερική βιβλιοθήκη/PDF backend, ρητή απόφαση χρήστη) με πεδία υπογραφής τεχνικού + Team Leader στο κάτω μέρος. Δημιουργείται ΜΟΝΟ on-demand (κανένα αυτόματο PDF σε κάθε ανάθεση/αποχρέωση — ρητή απόφαση χρήστη). Μόνο TL/Director (ίδιο gate `requireTeamLeader` με το υπόλοιπο tab Εξοπλισμός· το Backoffice δεν έχει πρόσβαση σε αυτό το tab, βλ. Ρόλοι § Backoffice).
  **Ψηφιακή υπογραφή στο έγγραφο παράδοσης/παραλαβής — ΕΓΙΝΕ** (15/08/2026, σχεδιάστηκε μέσω διευκρινιστικών ερωτήσεων πριν την υλοποίηση, βλ. `src/handover.js`): ξεχωριστό, ΝΕΟ μηχανισμό από το browser-print PDF παραπάνω (που παραμένει ως έχει, χωρίς αλλαγή) — δίπλα στο κουμπί «Εξαγωγή PDF παράδοσης/παραλαβής» υπάρχει τώρα ενότητα «Έγγραφα παράδοσης (ψηφιακή υπογραφή)» με κουμπί «Δημιουργία νέου εγγράφου προς υπογραφή». **Βασικό σχεδιαστικό σκεπτικό (ρητή διευκρίνιση χρήστη μετά από αρχική ερώτηση):** η αξία της ψηφιακής υπογραφής είναι να ΜΗΝ χρειάζεται φυσική παρουσία TL+τεχνικού μαζί — αν ο τεχνικός υπέγραφε στη συσκευή του TL την ίδια στιγμή, δεν θα διέφερε από χαρτί. Ροή: (1) ο TL/Director δημιουργεί ΕΝΑ έγγραφο από το ΤΡΕΧΟΝ σύνολο κατοχής του τεχνικού («το τρέχον σύνολο κατοχής», ρητή επιλογή χρήστη — snapshot "παγωμένο" τη στιγμή της δημιουργίας, ΔΕΝ ενημερώνεται αν αλλάξουν οι χρεώσεις μετά, ίδιο ιστορικό-snapshot μοτίβο με το `src/crews.js`) — η ίδια η authenticated ενέργεια (Google SSO) ΕΙΝΑΙ η επιβεβαίωση του TL, καμία σχεδιασμένη γραμμή υπογραφής χρειάζεται από τη μεριά του· (2) ο τεχνικός βλέπει το εκκρεμές έγγραφο στο δικό του tab «Στην Κατοχή μου» (νέα κάρτα «Έγγραφα προς υπογραφή», ΜΟΝΟ όταν `CURRENT_WHO.role === 'technician'`) και υπογράφει με το δάχτυλο σε `<canvas>` (custom mouse+touch drawing, καμία εξωτερική βιβλιοθήκη — ίδιο "no external libraries" convention με τα υπόλοιπα custom UI στοιχεία), από όποια συσκευή θέλει, όποτε βολεύει· (3) το έγγραφο κλειδώνει μετά την υπογραφή (immutable — `signHandoverDocument()` απορρίπτει δεύτερη υπογραφή, ίδιο idempotent-lock μοτίβο με τα ερωτήματα διαθεσιμότητας μετά την προθεσμία). Αν χρειαστεί νέο έγγραφο για ενημερωμένη κατοχή, ο TL φτιάχνει καινούριο — τα παλιά μένουν ως ιστορικό (λίστα με Status Εκκρεμεί/Υπογεγραμμένο, ορατή ΚΑΙ στην Αναζήτηση τεχνικού ΚΑΙ στο tab του ίδιου του τεχνικού). Νέο φύλλο `ΕγγραφαΠαράδοσης` (ID/EmployeeID/EmployeeName/HoldingsJSON/Status/CreatedBy/CreatedByName/CreatedAt/SignatureFileKey/SignatureFileName/SignatureFileType/SignedAt) — χειροκίνητο βήμα, βλ. §Χειροκίνητα βήματα. Η εικόνα υπογραφής (canvas PNG, `canvas.toBlob()`) αποθηκεύεται σε ΝΕΟ KV namespace `HANDOVER_FILES` (ίδιο μοτίβο/όριο με τα υπόλοιπα, ξεχωριστό γιατί το access-scope είναι ίδιο με τις φωτογραφίες παραλαβής οχήματος αλλά διαφορετικό concern) — δημιουργήθηκε μέσω Cloudflare MCP, ήδη δηλωμένο στο `wrangler.toml`, ενεργοποιείται αυτόματα στο επόμενο deploy. Endpoints: `POST /api/handover/create` (TL/Director), `GET /api/handover/list?employeeId=...` (TL/Director, Αναζήτηση τεχνικού), `GET /api/handover/my` (μόνο τεχνικός), `POST /api/handover/sign` (raw bytes, ο ίδιος ο τεχνικός/owner, μία φορά), `GET /api/handover/signature?id=...` (dual-auth owner Ή TL/Director, ίδιο gate με τις φωτογραφίες παραλαβής — Content-Disposition **inline**, όχι attachment, ώστε να ενσωματώνεται ως `<img>` στο PDF). Το τελικό PDF (`exportHandoverPdf()` στο `hub.html`, ίδιο browser-print μοτίβο) ενσωματώνει την πραγματική εικόνα υπογραφής αν υπάρχει, αλλιώς δείχνει «εκκρεμεί» — η πλευρά του TL δείχνει κείμενο «Δημιουργήθηκε από: [όνομα] ([ημερομηνία])» αντί για κενή γραμμή υπογραφής, συνεπές με το ότι δεν τραβάει ποτέ γραμμή. Email στον τεχνικό τη στιγμή δημιουργίας (αν έχει καταχωρημένο email) — ίδιο μπλοκάρισμα με όλα τα άλλα emails (σιωπηλό no-op αν λείπουν τα relay secrets). Καμία επίδραση στο ήδη υπάρχον browser-print PDF export παραπάνω — παραμένουν δύο ανεξάρτητοι μηχανισμοί, ο χρήστης διαλέγει ποιον θέλει.
  e-pass: κάρτες διοδίων Αττικής Οδού (`src/epass.js`, νέο φύλλο `EPass`) — ίδιο μοτίβο CRUD με Εξοπλισμός, ΑΛΛΑ μπορεί να ανατεθεί ΚΑΙ σε τεχνικό ΚΑΙ σε όχημα ταυτόχρονα (ρητή απαίτηση χρήστη — π.χ. κάρτα μέσα στο όχημα, με συγκεκριμένο τεχνικό υπεύθυνο). Το φύλλο `Χρεώσεις` επεκτάθηκε με `VehicleId`/`VehiclePlate` (νέες στήλες στο τέλος, χειροκίνητο βήμα) ώστε μια χρέωση e-pass να δείχνει και τα δύο ταυτόχρονα — οι υπόλοιπες χρεώσεις (οχήματα, εξοπλισμός) αφήνουν αυτές τις 2 στήλες κενές. Αν δοθεί μόνο όχημα (χωρίς τεχνικό), η χρέωση ανοίγει κανονικά αλλά δεν στέλνεται email (μόνο τεχνικοί έχουν email καταχωρημένο).
  **Ανακοινώσεις (ερωτήματα διαθεσιμότητας επαρχίας) — ΕΓΙΝΕ** (item #7, 13/08/2026, σχεδιάστηκε μέσω σειράς ερωτήσεων πριν την υλοποίηση — βλ. `src/availability.js`): νέο tab, ΞΕΧΩΡΙΣΤΟ από το «Ενημερώσεις/Οδηγίες» (ρητή απόφαση χρήστη — ίδιο όνομα «Ανακοινώσεις» που είχε προταθεί αρχικά για το παλιό feature, εδώ χρησιμοποιείται για το καινούριο). Ο TL/Director δημιουργεί ένα ερώτημα Ναι/Όχι (ελεύθερο κείμενο) με προθεσμία απάντησης, στοχεύοντας μία ή περισσότερες **ειδικότητες** (πεδίο `Team` του Υπάλληλοι, ίδια έννοια με το tab «Διαθεσιμότητα Τεχνικών» — ρητή διευκρίνιση χρήστη, ΟΧΙ την ομάδα αναφοράς ανά TL/`TeamLeaderEmail`, διαφορετική έννοια αλλού στο ίδιο codebase) μέσω checkboxes + «Όλες οι ειδικότητες» (λίστα από `listTeamOptions()`, ίδιο μοτίβο dropdown με το Νέος Τεχνικός, χωρίς hardcoded λίστα). Ο τεχνικός βλέπει στο δικό του tab «Ανακοινώσεις» μόνο ερωτήματα που ταιριάζουν στη δική του ειδικότητα και απαντάει Ναι/Όχι, με δυνατότητα να αλλάξει γνώμη μέχρι την προθεσμία· μετά κλειδώνει (καμία αλλαγή, ρητή απόφαση χρήστη). **ΟΛΟΙ οι TL/Director βλέπουν ΟΛΑ τα ερωτήματα** (όχι μόνο του δημιουργού τους) με συγκεντρωτικές απαντήσεις (Ναι/Όχι/δεν απάντησαν ακόμα, λίστα ονομάτων) — ρητή απόφαση χρήστη, πλήρης συμμετρία. **Διαγραφή** — ρητή απαίτηση χρήστη μετά από αρχική ερώτηση: επιτρέπεται σε ΟΠΟΙΟΝΔΗΠΟΤΕ TL/Director (όχι μόνο τον δημιουργό, ίδια συμμετρία), πραγματική διαγραφή γραμμής (`deleteRow`, ίδιο με `deleteAnnouncement`) με `confirm()` dialog πριν την ενέργεια· οι τυχόν ήδη καταχωρημένες απαντήσεις ΔΕΝ διαγράφονται μαζί (μένουν αβλαβείς ορφανές γραμμές στο φύλλο απαντήσεων, ποτέ δεν εμφανίζονται πουθενά χωρίς το ερώτημα). Backoffice: **ΔΕΝ έχει πρόσβαση** — ρητή απόφαση χρήστη, το tab δεν είναι καν στο `TOOLS.backoffice`, ίδιο gate `requireTeamLeader` με το υπόλοιπο tab Στόλος/Εξοπλισμός (Director περνάει αυτόματα, όπως παντού αλλού). Email ειδοποίησης στους στοχευμένους τεχνικούς (ενεργούς, με καταχωρημένο email) τη στιγμή δημιουργίας — ρητή απαίτηση χρήστη, ίδιο μοτίβο Reply-To=actor με τις υπόλοιπες ενέργειες. Δύο νέα φύλλα `ΕρωτηματαΔιαθεσιμότητας`/`ΑπαντησειςΔιαθεσιμότητας` — χειροκίνητο βήμα εκκρεμεί, βλ. §Χειροκίνητα βήματα.
  **Επεξεργασία ερωτήματος — ΕΓΙΝΕ** (15/08/2026, ρητή απαίτηση χρήστη): `updateAvailabilityQuery()` (`src/availability.js`) + `POST /api/availability/query/update` — ίδια πλήρης συμμετρία με τη διαγραφή, οποιοσδήποτε TL/Director μπορεί να επεξεργαστεί ΟΠΟΙΟΔΗΠΟΤΕ ερώτημα (όχι μόνο ο δημιουργός), επιτρέπει αλλαγή κειμένου/προθεσμίας/στοχευμένων ειδικοτήτων. Σκόπιμα ΧΩΡΙΣ επικύρωση "προθεσμία όχι στο παρελθόν" (σε αντίθεση με τη δημιουργία) — επιτρέπει να κλείσει κανείς νωρίτερα ένα ερώτημα βάζοντας παρελθοντική ημερομηνία, χωρίς διαγραφή. Σκόπιμα ΧΩΡΙΣ επανα-αποστολή email στην επεξεργασία (εκτός από την περίπτωση «γίνεται δημοσιευμένο τώρα», βλ. παρακάτω) — η ειδοποίηση φεύγει μόνο μία φορά. UI (`hub.html`): κουμπί «Επεξεργασία» σε κάθε κάρτα ερωτήματος ανοίγει/προσυμπληρώνει την ΙΔΙΑ φόρμα «Νέο ερώτημα» (reuse, όχι ξεχωριστό modal) — το κουμπί «Δημιουργία» γίνεται «Αποθήκευση αλλαγών» με κουμπί «Ακύρωση επεξεργασίας» δίπλα.
  **Ετεροχρονισμένη δημοσίευση + «χωρίς λήξη» — ΕΓΙΝΕ** (20/08/2026, ρητή απαίτηση χρήστη, αποφασίστηκε μέσω διευκρινιστικών ερωτήσεων πριν την υλοποίηση): δύο νέα προαιρετικά πεδία στη φόρμα «Νέο ερώτημα». (α) **Δημοσίευση** (`avq-publish-at`, νέο πεδίο `PublishAt` στο Sheet — ημερομηνία, ΟΧΙ ώρα, ρητή επιλογή χρήστη): κενό/σήμερα = άμεση δημοσίευση, ίδια συμπεριφορά με πριν· μελλοντική ημερομηνία = το ερώτημα αποθηκεύεται αλλά μένει ΑΟΡΑΤΟ στους τεχνικούς (`listMyAvailabilityQueries()` φιλτράρει `PublishAt > σήμερα` εντελώς έξω) και ΔΕΝ στέλνεται email μέχρι να έρθει η ημέρα — γίνεται αυτόματα ορατό/στέλνεται το email από νέο καθημερινό Cron Trigger `"0 5 * * *"` (`publishScheduledAvailabilityQueries()`, βλ. `scheduled()` στο `src/index.js`). Νέο πεδίο `EmailSentAt` στο Sheet αποτρέπει διπλή αποστολή. Στο tab TL/Director, τα μη-δημοσιευμένα ερωτήματα φαίνονται σε ξεχωριστή κάρτα «Προγραμματισμένα» πάνω από τα Ενεργά, με badge `Προγραμματισμένο · <ημερομηνία>` — αν ένας TL επεξεργαστεί ένα προγραμματισμένο ερώτημα και φέρει το `PublishAt` σε σήμερα/παρελθόν, το email φεύγει ΑΜΕΣΩΣ μέσα από το `updateAvailabilityQuery()` (δεν περιμένει το επόμενο πέρασμα του cron) — το πεδίο δημοσίευσης στη φόρμα επεξεργασίας είναι επεξεργάσιμο ΜΟΝΟ όσο το ερώτημα είναι ακόμα προγραμματισμένο, disabled/«Ήδη δημοσιευμένο» μετά. (β) **Χωρίς λήξη** (`avq-no-deadline` checkbox): το `Deadline` έγινε προαιρετικό — αν επιλεγεί, το πεδίο ημερομηνίας απενεργοποιείται/αδειάζει, γράφεται κενό `Deadline`, και το ερώτημα παραμένει ΠΑΝΤΑ ενεργό/απαντήσιμο (κανένα κλείδωμα, `submitAvailabilityAnswer()` ελέγχει τη λήξη μόνο αν υπάρχει τιμή) μέχρι κάποιος TL/Director να το διαγράψει χειροκίνητα. Όπου εμφανιζόταν «Προθεσμία <ημερομηνία>» (και στους δύο ρόλους, και στο email ειδοποίησης) δείχνει πλέον «Χωρίς λήξη» αν το πεδίο είναι κενό.
  **Τύπος ανακοίνωσης: Ναι/Όχι ή «Έλαβα γνώση» — ΕΓΙΝΕ** (21/08/2026, ρητή απαίτηση χρήστη, αποφασίστηκε μέσω διευκρινιστικών ερωτήσεων πριν την υλοποίηση): νέο πεδίο επιλογής στη φόρμα «Νέο ερώτημα» — **«Ερώτημα Ναι/Όχι»** (προεπιλογή, ίδια συμπεριφορά με πριν) ή **«Ενημέρωση με Έλαβα γνώση»** (νέο `ResponseMode = "ACK"`, νέο πεδίο `ResponseMode` στο Sheet — χειροκίνητο βήμα, βλ. §Χειροκίνητα βήματα· παλιές γραμμές χωρίς τιμή διαβάζονται σαν `"YESNO"`, καμία διακοπή λειτουργίας). Ρητή απόφαση χρήστη μέσω ερώτησης: όταν `ACK`, ο τεχνικός βλέπει ΕΝΑ κουμπί «👁 Έλαβα γνώση» αντί για Ναι/Όχι — μία εφάπαξ ενέργεια, καμία δυνατότητα «απο-επιβεβαίωσης» μετά το κλικ. Το πεδίο **Προθεσμία απάντησης κρύβεται εντελώς** στη φόρμα όταν επιλεγεί `ACK` (`avq-deadline-field`, ρητή απόφαση χρήστη — «κρύβεται εντελώς», ΟΧΙ απλά προαιρετικό) — ο server (`createAvailabilityQuery()`/`updateAvailabilityQuery()`) επιβάλλει κενό `Deadline` ό,τι κι αν σταλεί, άρα ένα ερώτημα `ACK` είναι ΠΑΝΤΑ «χωρίς λήξη» μέχρι να διαγραφεί χειροκίνητα (ίδιος μηχανισμός με το προαιρετικό `Deadline` του `YESNO`, απλά χωρίς UI επιλογή). Η απάντηση αποθηκεύεται στο ίδιο φύλλο `ΑπαντησειςΔιαθεσιμότητας` με σταθερή τιμή στο `Answer` (`submitAvailabilityAnswer()` αγνοεί ό,τι answer value σταλεί από το client για ερωτήματα `ACK`, γράφει πάντα τη σταθερή τιμή — αποφεύγει client/server drift). Στο tab TL/Director, κάθε κάρτα ερωτήματος δείχνει νέο badge τύπου (🙋 Ναι/Όχι ή 👁 Έλαβα γνώση) και οι συγκεντρωτικές μετρήσεις προσαρμόζονται (`👁 X είδαν` / `⏳ Y δεν έχουν δει ακόμα` αντί για Ναι/Όχι/Δεν απάντησαν — reuse των ίδιων `YesNames`/`PendingNames` πεδίων, το `NoNames` μένει πάντα κενό στο `ACK`). Ισχύει και στο badge εκκρεμοτήτων sidebar του τεχνικού (`refreshNavBadges()` ήδη γενικό, `Active && !MyAnswer`) και στην κάρτα «Απαντήσεις Ανακοινώσεων» του Προφίλ Τεχνικού (`avqAnswerLabel()`, καμία αλλαγή backend εκεί — ήδη reuse του `listMyAvailabilityQueries()`). Email ειδοποίησης προσαρμόζεται (χωρίς γραμμή «Προθεσμία απάντησης», διαφορετικό intro/CTA κείμενο). Ο τύπος (`responseMode`) είναι επεξεργάσιμος και στο «Επεξεργασία ερωτήματος» (ίδια συμμετρία με τα υπόλοιπα πεδία) — σκόπιμα ΧΩΡΙΣ ειδική επικύρωση αν αλλάξει μετά από ήδη καταχωρημένες απαντήσεις (μικρή, αποδεκτή αστάθεια εμφάνισης σε αυτή την ακραία περίπτωση, ίδιο «καμία εξαντλητική επικύρωση» σκεπτικό με άλλα edit σημεία του portal).
  **Τρίτος τύπος «NONE» — καθαρά ενημερωτικό, χωρίς καμία δυνατότητα απάντησης — ΕΓΙΝΕ** (22/08/2026, ρητή απαίτηση χρήστη, επέκταση του παραπάνω): τρίτη επιλογή στη φόρμα «Νέο ερώτημα» — **«Καθαρά ενημερωτικό (χωρίς καμία απάντηση)»** (`ResponseMode = "NONE"`, ίδια στήλη Sheet με το `ACK`, καμία νέα στήλη/χειροκίνητο βήμα χρειάστηκε). Ο τεχνικός βλέπει το κείμενο χωρίς ΚΑΝΕΝΑ κουμπί (ούτε Ναι/Όχι, ούτε «Έλαβα γνώση») — καθαρή ανάγνωση. `submitAvailabilityAnswer()` απορρίπτει ΚΑΘΕ προσπάθεια απάντησης σε ερώτημα `NONE` (defensive backend, ανεξάρτητο από το ότι το UI δεν δείχνει καν κουμπί) — άρα ΠΟΤΕ δεν γράφεται γραμμή στο `ΑπαντησειςΔιαθεσιμότητας` γι' αυτά. Ίδια συμπεριφορά με το `ACK` ως προς την Προθεσμία (κρύβεται εντελώς, πάντα «χωρίς λήξη» μέχρι διαγραφή). Στο tab TL/Director: badge τύπου «ℹ️ Ενημερωτικό», ΚΑΜΙΑ μέτρηση/παρακολούθηση (`listAvailabilityQueries()` αφήνει σκόπιμα κενά τα `YesNames`/`NoNames`/`PendingNames` για `NONE` — δεν έχει νόημα «δεν έχουν δει ακόμα» αφού δεν παρακολουθείται τίποτα), μόνο το πλήθος του στοχευμένου κοινού (`TotalTargeted`) εμφανίζεται πληροφοριακά. Badge εκκρεμοτήτων sidebar του τεχνικού (`refreshNavBadges()`) εξαιρεί ρητά τα `NONE` (θα φούσκωνε μόνιμα αλλιώς, αφού το `MyAnswer` είναι πάντα κενό γι' αυτά). Email ειδοποίησης ίδιο με το `ACK` (χωρίς γραμμή προθεσμίας) αλλά με διαφορετικό intro κείμενο (χωρίς να υπονοεί αναμενόμενη ενέργεια).
  Backup TL: κάθε TL μπορεί να έχει έναν `BackupEmail` στο φύλλο `TeamLeaders`. Ενεργοποιείται ΜΟΝΟ όταν ο κύριος TL πατήσει "Λείπω" στο hub (`Away = TRUE`) — τότε ο backup βλέπει επιπλέον την ομάδα του και μπορεί να εγκρίνει/απορρίψει αιτήσεις της.
  Ο αντικαταστάτης ΔΕΝ είναι προρυθμισμένος στο Sheet — ο TL τον επιλέγει από dropdown (λίστα λοιπών TL) τη στιγμή που πατάει "Λείπω"· το `BackupEmail` γράφεται τότε αυτόματα από το portal.
  **Ειδοποίηση backup TL τη στιγμή του "Λείπω" — ΕΓΙΝΕ** (ρητή απαίτηση χρήστη): `setLeaderAway()` (`src/leaves.js`) στέλνει email στον backup ΤΗ ΣΤΙΓΜΗ που ο κύριος TL ενεργοποιεί το "Λείπω" (away=true), ενημερώνοντάς τον ότι ανέλαβε κάλυψη της ομάδας — ξεχωριστό από το CC που ήδη παίρνει σε emails νέων αιτήσεων/δικαιολογητικών όσο διαρκεί η κάλυψη (`additionalRecipientsForAway()`, βλ. Εκκρεμότητες). Reply-To = ο κύριος TL (actor).
  **Αντίστροφη ειδοποίηση όταν επιστρέφει — ΕΓΙΝΕ** (ρητή απαίτηση χρήστη): όταν ο κύριος TL απενεργοποιεί το "Λείπω" (away: true → false), ο `setLeaderAway()` στέλνει ένα δεύτερο email στον backup που τον κάλυπτε ("Η κάλυψη ολοκληρώθηκε"). Χρησιμοποιεί το `BackupEmail`/`Away` ΠΡΙΝ την αλλαγή (το `BackupEmail` στο Sheet δεν καθαρίζεται όταν κλείνει το "Λείπω", μένει σαν προεπιλογή για την επόμενη φορά) — άρα δείχνει σωστά ποιος κάλυπτε. Καμία ειδοποίηση αν δεν υπήρχε πραγματική αλλαγή (π.χ. ήταν ήδη away=false) ή αν δεν υπάρχει καταχωρημένος backup/email. Σιωπηλό no-op αν λείπουν τα email relay secrets, ίδιο μπλοκάρισμα με όλα τα υπόλοιπα emails.
  Στο tab Άδειες Ομάδας υπάρχει και αναζήτηση αδειών ανά τεχνικό/εύρος ημερομηνιών (πλήρες ιστορικό, όχι μόνο τελευταίες 15) με export σε PDF (browser print, χωρίς εξωτερική βιβλιοθήκη).
  **Redesign 18/08/2026 (πόρτηση από screenshots, καθαρά UI — καμία αλλαγή backend/endpoints):** το παλιό ζεύγος «Προς έγκριση» (card-layout, μόνο εκκρεμείς) + «Πρόσφατες αποφάσεις» (ξεχωριστός πίνακας, μόνο αποφασισμένες) ενοποιήθηκε σε ΕΝΑ section «Αιτήσεις άδειας» (`renderLeaveRequestsTable()` στο `hub.html`, αντικαθιστά τα παλιά `renderPending()`/`renderDecided()`) — ένας πίνακας Τεχνικός/Τύπος/Ημερομηνίες/Κατάσταση/Ενέργειες, εκκρεμείς πρώτα μετά αποφασισμένες, με Έγκριση/Απόρριψη μόνο στις εκκρεμείς («—» στις αποφασισμένες). Ίδια ομαδοποίηση τηλεργασίας (κοινό `GroupID` -> μία γραμμή/ένα ζεύγος κουμπιών) όπως πριν, μόνο για τις εκκρεμείς. Νέα κουμπιά `.btn-leave-approve` (accent μπλε, εικονίδιο check)/`.btn-leave-reject` (dark outline, εικονίδιο x) — ΔΕΝ αγγίζουν τα παλιά `.btn-approve`/`.btn-reject` (πράσινο/κόκκινο), που παραμένουν όπως ήταν για το «Επέστρεψα»/«Επιβεβαίωση» backup TL (άσχετες ενέργειες, ίδια κλάση απλά για ευκολία στυλ). Οι εξωτερικοί `.section-title` πάνω από κάθε card (Ημερολόγιο αδειών, Αιτήσεις άδειας, Καταχώρηση άδειας, Αναζήτηση αδειών τεχνικού) μετακόμισαν ΜΕΣΑ στο card ως νέα κλάση `.card-title` — ίδιο μοτίβο σκόπιμα εφαρμοσμένο ΜΟΝΟ σε αυτό το tab προς το παρόν (όχι global αλλαγή σε όλο το portal, δεν ζητήθηκε ρητά).
  **Fix ίδια μέρα:** τα tiles της Αρχικής (`.dash-tile`, είναι `<button>`) δεν κληρονομούσαν `color:var(--text)` από το `body` (browser default `color:ButtonText` σε κουμπιά) — ο αριθμός (`.num`) εμφανιζόταν μαύρος σε σκούρο φόντο σε dark mode. Προστέθηκε ρητό `color:var(--text)` στο `.dash-tile{}`.
  **card-title παντού + sticky header — ΕΓΙΝΕ** (18/08/2026, ρητή απαίτηση χρήστη, επέκταση του `.card-title` που εφαρμόστηκε αρχικά μόνο στο Άδειες Ομάδας): το global header (`.app-header`) έγινε **sticky** (`position:sticky; top:0`, με τα ίδια αρνητικά margins/padding trick με το sidebar ώστε να «σπάει» έξω από το padding του `<main>` χωρίς κενό/άλμα) — μένει μόνιμα ορατό όσο κάνει scroll το περιεχόμενο, με δικό του `border-bottom` divider (ίδιο στυλ με `.topbar`) και ελαφρώς μικρότερα γράμματα (`h1` 20px→17px, `p` 12.5px→12px). Το `.card-title` πήρε κι αυτό divider (`border-bottom`) — ΟΛΑ τα top-level `section-title`+`card` ζευγάρια του portal (30 σημεία, ΚΑΘΕ tab) μετατράπηκαν σε ένα `card` με εσωτερικό `.card-title` ως πρώτο παιδί (γενικός regex pass, ασφαλής επειδή απαιτούσε section-title ΑΜΕΣΩΣ ακολουθούμενο από card-open — sub-headers ήδη μέσα σε ανοιχτή κάρτα δεν επηρεάστηκαν). Μοναδική χειροκίνητη εξαίρεση: η κάρτα «Κατάσταση» του Προφίλ Τεχνικού (flex row layout) αναδιαρθρώθηκε ώστε το αρχικό flex περιεχόμενο να μπει σε δικό του nested `<div>`, ώστε ο τίτλος να μην γίνει τυφλά ένα ακόμα flex item και σπάσει τη γραμμή. Η Αρχική εξαιρείται από το divider κάτω από το δικό της topbar (ρητή απαίτηση χρήστη — δεν έχει card-title, μένει με το δικό της `renderDashboard()` header pattern). Στο `.dash-tiles` προστέθηκε `margin-bottom:28px` (ρητή απαίτηση χρήστη — «κατέβασε λίγο» την κάρτα «Τελευταία δραστηριότητα», δεν είχε καθόλου απόσταση πριν).
  **Νέο sidebar logo — ΕΓΙΝΕ** (18/08/2026, ρητή απαίτηση χρήστη από screenshot): το παλιό λευκό pill με custom inline SVG (fiber-optic εικονίδιο) + κείμενο «Οπτική Τεχνική» αντικαταστάθηκε από μπλε rounded-square badge (Tabler `ti-share-2`, λευκό εικονίδιο) απευθείας πάνω στο σκούρο sidebar + δίγραμμο κείμενο **OptikiTec** / **PORTAL** (η επωνυμία που ήδη χρησιμοποιείται στον τίτλο σελίδας/σε όλο το codebase — δεν ήταν ρητά ζητημένη η αλλαγή κειμένου, αλλά ήταν απαραίτητη για να ταιριάξει με το mockup που έδειξε ο χρήστης).
  **Προφίλ Τεχνικού redesign — ΕΓΙΝΕ** (18/08/2026, ρητή απαίτηση χρήστη από screenshot, φράστηκε ως ερώτηση «θα μπορούσαμε;»): νέο πάνω μέρος σε 2 στήλες (`.tp-identity-grid`) — αριστερά κάρτα ταυτότητας (avatar με αρχικά ονόματος, όνομα + `Team` ως υπότιτλος «ειδικότητας», γραμμή επαφής με εικονίδια τηλέφωνο/email/διεύθυνση, `leaveBalanceBarHtml()` progress bar από κάτω) — δεξιά συνοπτική κάρτα «Στην κατοχή του» (icon+label λίστα, `tp-holdings-mini`). **Σκόπιμη απόκλιση από το mockup:** το screenshot έδειχνε πλασματικά πεδία όπως «Senior FTTH Technician» (τίτλος θέσης) και «Συνεργείο Α — Δυτική Αττική» (τοποθεσία) που ΔΕΝ υπάρχουν στο schema του `Υπάλληλοι` — δεν προστέθηκαν νέα πεδία/στήλες γι' αυτό (θα ήταν μεγαλύτερη, ξεχωριστή αλλαγή σχήματος), χρησιμοποιήθηκαν μόνο ήδη υπαρκτά πεδία (`Team` στη θέση του «τίτλου», καμία τοποθεσία). Ο πλήρης πίνακας «Στην κατοχή του τώρα» (με ημερομηνία χρέωσης + link φωτογραφίας παραλαβής οχήματος) παραμένει ΑΜΕΤΑΒΛΗΤΟΣ παρακάτω στη σελίδα — η νέα κάρτα είναι συνοπτική προεπισκόπηση, όχι αντικατάσταση (σκόπιμη μικρή επικάλυψη πληροφορίας, ίδιο μοτίβο με τα tiles της Αρχικής που συνοψίζουν κάτι λεπτομερέστερο αλλού). Το `.metrics cols-4` (Ετήσιο δικαίωμα/Χρησιμοποιημένες/Υπόλοιπο/Εκκρεμείς αιτήσεις) παρέμεινε επίσης, μετακινήθηκε απλά κάτω από το νέο 2-column block. Κάθε άλλη κάρτα (Κατάσταση, Έγγραφα, Ιστορικό αδειών, Στην κατοχή του τώρα, Απαντήσεις Ανακοινώσεων) ΑΜΕΤΑΒΛΗΤΗ.
  **Προσωπικά στοιχεία μέσα στην κάρτα ταυτότητας — ΕΓΙΝΕ** (18/08/2026, ρητή απαίτηση χρήστη): η ξεχωριστή κάρτα «Προσωπικά στοιχεία» (form-grid με ΑΜΑ/ΑΜΚΑ/τηλέφωνα/διεύθυνση/ημ. πρόσληψης/διάστημα απασχόλησης/τεχνική κατάρτιση) αφαιρέθηκε — το περιεχόμενό της μετακόμισε ΜΕΣΑ στην κάρτα ταυτότητας (πάνω από το progress bar αδειών), ίδιο icon+κείμενο μοτίβο με το τηλέφωνο/email/διεύθυνση που ήδη υπήρχαν εκεί (`.tp-identity-contact`, τώρα 2-column grid αντί για flex-wrap λόγω περισσότερων στοιχείων). Το προσωπικό `Τηλέφωνο` και το `CompanyPhone` («εταιρικό») εμφανίζονται πλέον ΚΑΙ τα δύο ταυτόχρονα (πριν το identity-card contact row έκανε fallback Phone→CompanyPhone, έδειχνε μόνο ένα) — ίδιο σκεπτικό με το γιατί υπήρχαν ως 2 ξεχωριστά πεδία στην παλιά κάρτα. Η «Τεχνική κατάρτιση» (bullet list, `tpTrainingBullets()`) έμεινε σε δικό της μικρό block (`.tp-identity-training`) κάτω από το contact block — πολυγραμμικό περιεχόμενο, δεν χωράει σε μονογραμμική icon+κείμενο γραμμή σαν τα υπόλοιπα. Καμία αλλαγή backend/δεδομένων — καθαρά αναδιάταξη UI.
  **3 ρητές στήλες — ΕΓΙΝΕ (ΑΝΑΘΕΩΡΕΙ το αρχικό 2-column grid παραπάνω)** (18/08/2026, ρητή απαίτηση χρήστη με ακριβή ομαδοποίηση): το `.tp-identity-contact` έγινε flex row 3 explicit sub-columns (`.tp-contact-col`, ΟΧΙ πια CSS grid — το grid auto-flow δεν μπορεί να ομαδοποιήσει άνισο αριθμό γραμμών ανά στήλη) — στήλη 1: Τηλέφωνο, Εταιρικό τηλέφωνο (δεν ζητήθηκε ρητά σε συγκεκριμένη στήλη, τοποθετήθηκε εδώ μαζί με τα υπόλοιπα στοιχεία επικοινωνίας ώστε να μη χαθεί από την εμφάνιση), Διεύθυνση, Email· στήλη 2: ΑΜΑ, ΑΜΚΑ· στήλη 3: Ημερομηνία πρόσληψης, Διάστημα Απασχόλησης.
  **Επεξεργασία στοιχείων τεχνικού — ΕΓΙΝΕ** (19/08/2026, ρητή απαίτηση χρήστη): νέο κουμπί «Επεξεργασία στοιχείων» στην κάρτα «Κατάσταση» του Προφίλ Τεχνικού (δίπλα στο Reset PIN/Ενεργοποίηση) ανοίγει modal (ίδιο μοτίβο modal με Στόλο/Εξοπλισμό/e-pass, `#tp-edit-modal`) με τα ίδια πεδία της φόρμας «Νέος Τεχνικός» προσυμπληρωμένα με τις τρέχουσες τιμές — ΕΚΤΟΣ `EmployeeID` (immutable — είναι το login username του τεχνικού ΚΑΙ το key του KV `TECHNICIAN_AUTH`, `emp:${employeeId}`· δείχνεται μόνο read-only), `Status`/`InactiveDate` (ξεχωριστό υπάρχον toggle) και τα 3 τρίδυμα συνημμένων (ξεχωριστή ροή upload στην κάρτα «Έγγραφα» του ίδιου tab — σκόπιμα όχι εδώ, ώστε ένα save στοιχείων να μην μπορεί ποτέ να σβήσει κατά λάθος ένα ήδη ανεβασμένο αρχείο). Νέα `updateTechnicianRecord()` (`src/leaves.js`, ίδιο merge-existing μοτίβο με `updateVehicle()`) + `POST /api/employees/update` (`src/index.js`, gate `requireTeamLeader` — ίδιο με το υπόλοιπο tab, αποκλείει Backoffice συνεπές με το ότι δεν έχει καν πρόσβαση στο «Προφίλ Τεχνικού»). Save → κλείνει το modal και ξαναφορτώνει το προφίλ.
  **Α.Φ.Μ. + πάντα ορατό εταιρικό τηλέφωνο — ΕΓΙΝΕ** (19/08/2026, ρητή απαίτηση χρήστη): νέο πεδίο **Α.Φ.Μ.** (`AFM`, νέα στήλη στο τέλος του `Υπάλληλοι` — χειροκίνητο βήμα, βλ. §Χειροκίνητα βήματα) προστέθηκε παντού όπου υπάρχει ΑΜΑ/ΑΜΚΑ: φόρμα «Νέος Τεχνικός» (`nt-afm`), modal «Επεξεργασία στοιχείων» (`tp-edit-afm`), και εμφανίζεται στη 2η στήλη του Προφίλ Τεχνικού κάτω από το ΑΜΚΑ. Επίσης διορθώθηκε το «Εταιρικό τηλέφωνο» στην 1η στήλη — μέχρι τώρα εμφανιζόταν ΜΟΝΟ όταν είχε τιμή (`${emp.CompanyPhone ? ... : ''}`), γεγονός που το έκανε αόρατο για τεχνικούς χωρίς καταχωρημένο εταιρικό τηλέφωνο σαν να μην υπήρχε καν το πεδίο· τώρα εμφανίζεται πάντα με «—» fallback, ίδιο μοτίβο με το προσωπικό Τηλέφωνο/Email.
  **Νέο sidebar logo icon — ΕΓΙΝΕ (ΑΝΑΘΕΩΡΕΙ το Tabler `ti-share-2` παραπάνω)** (18/08/2026, ρητή απαίτηση χρήστη): επαναφέρθηκε το ΠΑΛΙΟ custom SVG εικονίδιο (fiber-optic δίκτυο, dots+lines) που υπήρχε πριν το redesign — αλλά αναχρωματισμένο σε λευκό (`stroke`/`fill="#fff"`, πριν ήταν σχεδιασμένο για λευκό φόντο με dark navy strokes/dots + μπλε/πράσινα accent dots) ώστε να φαίνεται καθαρά μέσα στο νέο μπλε `.logo-badge`. Ίδια θέση/μέγεθος badge, μόνο το εσωτερικό glyph άλλαξε.
  **Τύπος «Αδικαιολογήτως Απών» — ΕΓΙΝΕ** (15/08/2026, ρητή απαίτηση χρήστη): νέα επιλογή ΜΟΝΟ στο dropdown «Τύπος άδειας» της κάρτας «Καταχώρηση άδειας για τεχνικό» (`#tl-type` στο `hub.html`) — ρητά ΔΕΝ προστέθηκε στο dropdown της δικής του «Νέα αίτηση άδειας» (`#ml-type`, tab «Οι Άδειές μου»), άρα ένας τεχνικός δεν μπορεί ποτέ να το επιλέξει ο ίδιος για τον εαυτό του, μόνο ο TL/Director το καταχωρεί εκ μέρους του. Καμία αλλαγή backend χρειάστηκε — το `type` στο `submitLeaveRequest()`/`submitLeaveRequestAsLeader()` (`src/leaves.js`) είναι ήδη ελεύθερο string χωρίς hardcoded λίστα επιτρεπτών τιμών. **Μετράει κανονικά στο ετήσιο υπόλοιπο αδειών** (ρητή απόφαση χρήστη — "δεν εξαιρείται τίποτα, μετράει κανονικά, είναι η άδεια που ο τεχνικός δεν έχει εμφανιστεί στη δουλειά του") — ΔΕΝ προστέθηκε στην εξαίρεση `type !== "Άδεια χωρίς αποδοχές"` του μπλοκαρίσματος υποβολής, άρα συμπεριφέρεται ακριβώς όπως η «Κανονική άδεια»: μπλοκάρει την υποβολή αν δεν επαρκεί υπόλοιπο, μετράει στο `usedDays`/`remainingDays` (`getEmployeeBalance`), εμφανίζεται στο ημερολόγιο ομάδας/ημερολόγιο τεχνικού, στο «Αναλυτικά»/KPI (χρήση αδειών/μήνα) και στη μηνιαία αναφορά λογιστή — όλα ήδη χωρίς φιλτράρισμα κατά τύπο, καμία αλλαγή χρειάστηκε εκεί. **Ροή έγκρισης — δείτε παρακάτω «Αυτόματη έγκριση κατά την καταχώρηση από TL» (ΑΝΑΘΕΩΡΕΙ την αρχική περιγραφή εδώ: δεν περνάει πια από «Προς έγκριση»).** Στο email badge/χρώμα (`leaveTypeColor()` στο `src/reports.js`) πέφτει στο προεπιλεγμένο πράσινο μαζί με «Κανονική άδεια»/«Λοιπές» (δεν ζητήθηκε ξεχωριστό χρώμα).
  **Αυτόματη έγκριση κατά την καταχώρηση από TL/Director — ΕΓΙΝΕ** (15/08/2026, ρητή απαίτηση χρήστη): όταν ο TL/Director καταχωρεί άδεια εκ μέρους τεχνικού μέσω «Καταχώρηση άδειας για τεχνικό» (`submitLeaveRequestAsLeader()`), η αίτηση δημιουργείται ΚΑΤΕΥΘΕΙΑΝ με `Status: "Εγκρίθηκε"` (και `DecisionDate` = τώρα) — ΚΑΜΙΑ πια «Εκκρεμεί»/«Προς έγκριση» ενδιάμεση κατάσταση, ΓΙΑ ΟΛΟΥΣ τους τύπους άδειας (ρητή απόφαση χρήστη, όχι μόνο για το «Αδικαιολογήτως Απών»). Λογική: η ίδια η ενέργεια καταχώρησης από τον TL ΕΙΝΑΙ η απόφαση — δεν έχει νόημα ο ίδιος να «εγκρίνει» μετά κάτι που μόλις κατέγραψε ο ίδιος. Νέο optional param `autoApprove` στο `submitLeaveRequest()` (`src/leaves.js`) — `submitLeaveRequestAsLeader()` το περνάει πάντα `true`· η δική του self-submission του τεχνικού (`handleLeavesSubmit`/`submitLeaveRequest` χωρίς `autoApprove`) παραμένει αμετάβλητη, πάντα «Εκκρεμεί» με email στον εγκριτή. Όταν `autoApprove`, στέλνεται email στον ΤΕΧΝΙΚΟ (αν έχει καταχωρημένο) ενημερώνοντάς τον ότι καταχωρήθηκε ήδη εγκεκριμένη άδεια στο όνομά του — ΟΧΙ email στον εγκριτή (δεν υπάρχει πια τίποτα να εγκρίνει). Ισχύει το ίδιο μπλοκάρισμα υποβολής αν δεν επαρκεί υπόλοιπο (η αυτόματη έγκριση δεν παρακάμπτει τον έλεγχο, απλά παραλείπει το ενδιάμεσο βήμα).
  **Διαγραφή άδειας — ΕΓΙΝΕ** (15/08/2026, ρητή απαίτηση χρήστη): νέα `deleteLeaveRequest()` (`src/leaves.js`) + `POST /api/leaves/delete` (`handleLeavesDelete`, `src/index.js`, ίδιο gate `requireTeamLeader` — καλύπτει αυτόματα και Director) — ο TL/Director μπορεί να διαγράψει ΟΠΟΙΑΔΗΠΟΤΕ άδεια της ομάδας του, ανεξαρτήτως κατάστασης (Εκκρεμεί/Εγκρίθηκε/Απορρίφθηκε), π.χ. διόρθωση λάθους καταχώρησης. Ίδιο authorization με `decideLeaveRequest()` (`isAuthorizedForTeam` — καλύπτει αυτόματα και backup TL όσο διαρκεί το "Λείπω"). Πραγματική διαγραφή γραμμής (`deleteRow`, όχι soft-delete) — ίδιο μοτίβο με `deleteAnnouncement`/`deleteAvailabilityQuery`. Σκόπιμα ΧΩΡΙΣ email ειδοποίηση στον τεχνικό (δεν ζητήθηκε — η διαγραφή είναι συνήθως διόρθωση λάθους, όχι κάτι που πρέπει να μάθει ο τεχνικός). UI: κουμπί «Διαγραφή» ΜΟΝΟ στην «Αναζήτηση αδειών τεχνικού» (πλήρες ιστορικό, `#tls-results` στο `hub.html`) — ρητή επιλογή χρήστη, ΟΧΙ στο «Πρόσφατες αποφάσεις»/«Προς έγκριση». `confirm()` dialog πριν την ενέργεια (μη αναστρέψιμο). `searchTeamLeaves()` (`src/leaves.js`) επεκτάθηκε να επιστρέφει και το `ID` ανά γραμμή (έλειπε πριν, χρειάστηκε για να μπορεί το UI να αναφερθεί σε συγκεκριμένη αίτηση).
  **Πλήρες snapshot στο AuditLog πριν τη διαγραφή — ΕΓΙΝΕ** (πρόταση Claude, αποδεκτή από χρήστη): επειδή η διαγραφή είναι πραγματική/μόνιμη (`deleteRow`, όχι soft-delete, καμία δυνατότητα undo), το `deleteLeaveRequest()` καταγράφει τώρα ΟΛΟΚΛΗΡΗ τη γραμμή (χωρίς το εσωτερικό `_row`) ως `snapshot` μέσα στο `Details` του `appendAuditLog("leave_deleted", ...)`, πέρα από τα βασικά πεδία (employeeId/employeeName/type/status) που ήδη καταγράφονταν. ΔΕΝ προσφέρει restore — μόνο πλήρη ορατότητα στο tab «Ιστορικό Ενεργειών» (Director-only) για το τι ακριβώς διαγράφηκε, αν χρειαστεί ποτέ να το ελέγξει κανείς. Παράλληλα, `auditDetailsText()`/`auditDetailsPlainText()` (`hub.html`) ενημερώθηκαν με νέο `auditDetailValueText()` ώστε nested object τιμές (όπως το `snapshot`) να εμφανίζονται ως JSON.stringify αντί για άχρηστο `[object Object]`.
  **Προφίλ Τεχνικού — ΕΓΙΝΕ** (πρόταση Claude, αποδεκτή από χρήστη): νέο tab `techprofile` (TL + Director, ΟΧΙ Backoffice — συνεπές με απόφαση #1, το Backoffice δεν βλέπει λεπτομέρειες αδειών). Λύνει το πρόβλημα ότι τα στοιχεία ενός τεχνικού ήταν σκόρπια σε 3 σημεία: dropdown επιλογής τεχνικού -> ΜΙΑ σελίδα με ισοζύγιο/ιστορικό αδειών (reuse `getEmployeeBalance()`) + ό,τι έχει ΤΩΡΑ πάνω του, οχήματα/εξοπλισμός/e-pass μαζί (reuse `getTechnicianHoldings()` από `src/assignments.js`, ίδια πηγή με το tab «Στην Κατοχή μου»/«Στοιχεία τεχνικού» του Εξοπλισμού) + κουμπί που ανοίγει το `evaluation.html` σε νέα καρτέλα (καμία προσυμπλήρωση — το evaluation.html παραμένει σκόπιμα αυτόνομο, βλ. παραπάνω). `GET /api/employees/profile` (`src/index.js`, `handleEmployeeProfile`) — χωρίς `employeeId`: λίστα τεχνικών για το dropdown· με `employeeId`: το πλήρες προφίλ. Νέα `getTechnicianLeaveProfile(env, viewerEmail, employeeId)` στο `src/leaves.js`. Καμία νέα αποθήκευση δεδομένων — καθαρός συνδυασμός δύο ήδη υπαρχόντων read paths.
  **Πρόσβαση ανοιγμένη σε ΟΛΟΥΣ τους τεχνικούς — ΕΓΙΝΕ (ΑΝΑΘΕΩΡΕΙ την αρχική)** (15/08/2026, ρητή απαίτηση χρήστη — "ο TL να επιλέγει ποια ομάδα θα βλέπει"): αρχικά ένας TL έβλεπε/έψαχνε ΜΟΝΟ τους δικούς του τεχνικούς εδώ (scoped μέσω `getTeamLeaderData().team`, ίδιο μοτίβο με «Άδειες Ομάδας»), Director έβλεπε όλους. Τώρα ΚΑΘΕ TL/Director βλέπει ΟΠΟΙΟΝΔΗΠΟΤΕ τεχνικό — το dropdown χρησιμοποιεί πάντα `getTechniciansPool()` (χωρίς κλάδο TL/Director), και το `getTechnicianLeaveProfile()` δεν κάνει πια κανέναν έλεγχο `isAuthorizedForTeam()`. Λόγος: το Reset PIN μετακόμισε μέσα σε αυτό το tab (βλ. παρακάτω) και δεν είχε ΠΟΤΕ περιορισμό ομάδας — για να μη χαθεί αυτή η δυνατότητα, άνοιξε και η ίδια η προβολή προφίλ σε όλους. Καμία επίπτωση αλλού (η `getTechnicianLeaveProfile` χρησιμοποιείται μόνο εδώ).
  **Reset PIN — μετακόμισε εδώ, ΕΓΙΝΕ** (15/08/2026, ρητή απαίτηση χρήστη — "στην ίδια γραμμή με την ενεργοποίηση/απενεργοποίηση"): νέο κουμπί «Reset PIN» στην κάρτα «Κατάσταση» του Προφίλ Τεχνικού, δίπλα στο κουμπί Ενεργοποίηση/Απενεργοποίηση — ίδιο backend endpoint `POST /api/employees/reset-pin`, απλά κλήση με το ήδη επιλεγμένο `employeeId` αντί για ξεχωριστό dropdown. Το νέο PIN εμφανίζεται στο ίδιο μήνυμα κατάστασης κάτω από τα δύο κουμπιά. **Η παλιά κάρτα «Reset PIN τεχνικού» στο tab «Νέος Τεχνικός» ΑΦΑΙΡΕΘΗΚΕ** (15/08/2026, ΑΝΑΘΕΩΡΕΙ την αρχική απόφαση από την ίδια μέρα — ρητή απαίτηση χρήστη μετά από follow-up): υπήρχε ΜΟΝΟ εκεί για να μη χάσει το Backoffice τη δυνατότητα (το Backoffice έχει πρόσβαση στο «Νέος Τεχνικός» αλλά ΟΧΙ στο «Προφίλ Τεχνικού»), αλλά ο χρήστης επιβεβαίωσε ρητά ότι το Backoffice ΜΠΟΡΕΙ να χάσει αυτή τη δυνατότητα — άρα η κάρτα αφαιρέθηκε εντελώς από το `renderNewTechnician()` (`hub.html`, μαζί με το `resetTechnicianPin()`/`rp-*` UI) και το backend gate έγινε αυστηρότερο: `requireTeamLeaderOrBackoffice` → `requireTeamLeader` σε ΚΑΙ τα δύο `/api/employees/reset-pin` (GET λίστα + POST reset) στο `handleResetPinInfo`/`handleResetPin` (`src/index.js`) — το Backoffice παίρνει πλέον 403 αν προσπαθήσει να καλέσει το endpoint απευθείας, όχι απλά κρυμμένο UI. Reset PIN είναι πλέον αποκλειστικά TL/Director, μόνο μέσα από το «Προφίλ Τεχνικού».
  **Προσωπικά στοιχεία τεχνικού (ΑΜΑ/ΑΜΚΑ/τηλέφωνο/διεύθυνση/ημ. πρόσληψης/τεχνική κατάρτιση + συνημμένα) — ΕΓΙΝΕ** (ρητή απαίτηση χρήστη, item #3 στα «Νέα ζητούμενα 10/08/2026», βλ. παρακάτω): το tab «Προφίλ Τεχνικού» δείχνει πλέον, πάνω από το ιστορικό αδειών, μια κάρτα «Προσωπικά στοιχεία» (ΑΜΑ, ΑΜΚΑ, Τηλέφωνο, Διεύθυνση, Ημερομηνία πρόσληψης, «Χρόνια στην εταιρεία» — **υπολογιζόμενο client-side από το HireDate, ΔΕΝ αποθηκεύεται πουθενά**, ελεύθερο κείμενο Τεχνική κατάρτιση) και μια κάρτα «Έγγραφα» με 3 σειρές (Βιογραφικό, Ταυτότητα/Διαβατήριο, Άδεια Διαμονής) — κάθε σειρά δείχνει link λήψης αν υπάρχει ήδη αρχείο, και input+κουμπί για ανέβασμα/αντικατάσταση (`hub.html` → `tpDocRow()`/`loadTechProfile()`). Ίδια δεδομένα καταχωρούνται προαιρετικά και στη φόρμα «Νέος Τεχνικός» τη στιγμή της δημιουργίας.
  **Πρόσβαση (ρητή απόφαση χρήστη μέσω ερώτησης πριν την υλοποίηση, λόγω ευαισθησίας ΑΜΚΑ/ταυτότητας/άδειας διαμονής — GDPR):** μόνο TL + Director μπορούν να ΔΟΥΝ/κατεβάσουν αυτά τα στοιχεία (ίδιο gate `requireTeamLeader` με το υπόλοιπο tab Προφίλ Τεχνικού — περνάει και για Director, ΟΧΙ για Backoffice). Ασυμμετρία σκόπιμη και τεκμηριωμένη: το Backoffice ΜΠΟΡΕΙ να τα καταχωρήσει/ανεβάσει τη στιγμή δημιουργίας τεχνικού (η φόρμα «Νέος Τεχνικός» είναι κοινή TL+Backoffice, `requireTeamLeaderOrBackoffice` — δεν ζητήθηκε διαχωρισμός της φόρμας), αλλά ΔΕΝ μπορεί να τα ξαναδεί μετά αφού το tab «Προφίλ Τεχνικού» δεν είναι στο `TOOLS.backoffice`.
  Backend: `src/leaves.js` → `uploadEmployeeDocument()`/`getEmployeeDocument()` (ίδιο μοτίβο raw-bytes σε KV + μόνο metadata στο Sheet με το `uploadLeaveAttachment`/`getLeaveAttachmentById` της αναρρωτικής άδειας), νέο KV namespace `EMPLOYEE_FILES` (`wrangler.toml`, id `114a16a9a00842fb9d60c5ed5ecc9bf2`) — ξεχωριστό από το `LEAVE_FILES`, ακριβώς επειδή το πρόσβασης-scope είναι διαφορετικό (TL+Director εδώ, vs. αιτών+TL της αίτησης στο LEAVE_FILES). `POST /api/employees/document/upload` (raw bytes) + `GET /api/employees/document?employeeId=...&docType=...` (`src/index.js` → `handleEmployeeDocumentUpload`/`handleEmployeeDocumentFile`) — το upload gate είναι `requireTeamLeaderOrBackoffice` (επιτρέπει και τη φόρμα Νέος Τεχνικός), το download gate είναι το αυστηρότερο `requireTeamLeader`. Μόνο PDF/JPEG/PNG, μέγιστο 20MB (`EMPLOYEE_DOCUMENT_ALLOWED_TYPES`).
  `Υπάλληλοι` επεκτάθηκε με 15 νέες στήλες: `AMA, AMKA, Phone, Address, HireDate, TechnicalTraining, CvFileKey, CvFileName, CvFileType, IdFileKey, IdFileName, IdFileType, ResidencePermitFileKey, ResidencePermitFileName, ResidencePermitFileType` — χειροκίνητο βήμα, βλ. §Χειροκίνητα βήματα. Όλα προαιρετικά (κενό string default) — τεχνικοί που δημιουργήθηκαν πριν από αυτό το feature απλά δείχνουν «—» παντού, καμία διακοπή λειτουργίας.
  Στο «Πρόσφατες αποφάσεις»: το δικαιολογητικό αναρρωτικής άδειας έχει τη ΔΙΚΗ ΤΟΥ στήλη «Δικαιολογητικό» (`sickAttachmentCell()` στο `hub.html`) αντί να εμφανίζεται inline δίπλα στον Τύπο — ρητή απαίτηση χρήστη. Το «Προς έγκριση» (card layout, όχι πίνακας) συνεχίζει να δείχνει το badge inline (`sickAttachmentBadge()`), δεν άλλαξε.
  Στο μηνιαίο ημερολόγιο αδειών ομάδας: τα chips τηλεργασίας έχουν ξεχωριστό χρώμα (μπλε, `cal-chip-telework`) αντί για το πράσινο/πορτοκαλί της κανονικής άδειας, με αντίστοιχο τρίτο legend item «Τηλεργασία» — ρητή απαίτηση χρήστη.
- **Backoffice** — Google SSO· email στο φύλλο `Backoffice`. Tabs: Διαθεσιμότητα Τεχνικών, Συνεργεία, Νέος Τεχνικός, Ενημερώσεις/Οδηγίες, Τηλεργασία, Οι Άδειές μου, Στην Κατοχή μου.
  Διαθεσιμότητα Τεχνικών: read-only λίστα ΟΛΩΝ των τεχνικών (όλες οι ομάδες) με κατάσταση σήμερα (Διαθέσιμος/Σε άδεια) + επερχόμενες εγκεκριμένες άδειες 30 ημερών. Ομαδοποιημένη ανά `Team` (π.χ. "Τεχνικός Αυτοψίας", "Τεχνικός Εμφύσησης" — το ίδιο πεδίο Team του Υπάλληλοι, εδώ χρησιμοποιείται ως ειδικότητα, όχι περιοχή). Γραμμές με `Team == "Back Office"` Ή `Team == "Team Leader"` (staff/TL που έχουν δική τους γραμμή στο Υπάλληλοι μόνο για να βλέπουν τη δική τους άδεια, όχι πεδίου τεχνικοί) αφαιρούνται αυτόματα από τη λίστα — διάκριση μέσω της στήλης Team (ΟΧΙ email: στην πράξη πολλοί τεχνικοί μοιράζονται το ίδιο placeholder email, το email αποδείχτηκε αναξιόπιστο διακριτικό, βλ. commit history). Ρητή απαίτηση χρήστη (και τα δύο) — βλ. αποφάσεις #1 παρακάτω για το πώς συνυπάρχει με τον περιορισμό Backoffice.
  Η κατάσταση σήμερα έχει 3 επίπεδα (`getAllTechniciansAvailability`/`computeAvailabilityList` στο `src/leaves.js`): Διαθέσιμος, "Φεύγει σε Χ ημέρες" (εγκεκριμένη άδεια ξεκινά εντός 3 ημερών) και "Σε άδεια · επιστρέφει σε Χ ημέρες" — λογική εμπνευσμένη από εξωτερικό demo app του χρήστη (μόνο η κατηγοριοποίηση, όχι κώδικας/UI/χρώματα).
  Συνεργεία: σύνθεση καθημερινών συνεργείων με drag & drop (`src/crews.js`, νέο φύλλο `Συνεργεία`) — δεξαμενή τεχνικών (ίδια ζωντανή κατάσταση με το Διαθεσιμότητα Τεχνικών, flat χωρίς ομαδοποίηση, με badge διπλώματος) + κάρτες συνεργείων όπου ο Backoffice σέρνει τεχνικούς. Επεξεργάσιμο ΜΟΝΟ για τη σημερινή ημέρα· το dropdown "Ιστορικό" φορτώνει παλαιότερες αποθηκευμένες ημέρες read-only. Αποθήκευση = μία γραμμή ανά ημερομηνία στο Sheet με ολόκληρη τη σύνθεση σε JSON (τελευταία αποθήκευση ισχύει, χωρίς merge — ίδιο μοτίβο με το demo app, αλλά σε Google Sheet αντί για Cloudflare KV, συνεπές με την υπόλοιπη αρχιτεκτονική). Δεν συνδέεται με Χρεώσεις/Άδειες — καθαρά οργάνωση της ημέρας, όχι επίσημη ανάθεση. Μόνο Backoffice φτιάχνει (ρητή απόφαση χρήστη — TL δεν έχει πρόσβαση σε αυτό το tab).
  Τηλεργασία: μόνο Backoffice μπορεί να δηλώσει (`/api/leaves/telework`, ελέγχεται server-side μέσω `getBackofficeByEmail`). Νέο `Type = "Τηλεργασία"` στο ίδιο σύστημα αδειών — reuse 100% του submit/approve/ημερολογίου, μηδενική αλλαγή σχήματος. Ο χρήστης επιλέγει ΠΟΛΛΑΠΛΕΣ μη-συνεχόμενες ημερομηνίες (multi-date picker στο UI, όχι εύρος) — γράφεται ΜΙΑ γραμμή στο `Άδειες` ανά επιλεγμένη ημέρα (StartDate=EndDate=η ίδια μέρα). Χρειάζεται έγκριση TL, ίδιο μηχανισμό με τις άδειες, και φαίνεται στο ημερολόγιο αδειών ομάδας. Η Τηλεργασία **ΔΕΝ** αφαιρείται από το ετήσιο υπόλοιπο αδειών (`getEmployeeBalance`/`getTeamLeaderData` εξαιρούν ρητά `Type !== "Τηλεργασία"` από τον υπολογισμό used days) — ρητή επιβεβαίωση χρήστη.
  Ομαδική έγκριση/απόρριψη τηλεργασίας — ΕΓΙΝΕ: ρητή απαίτηση χρήστη, οι πολλαπλές ημέρες μιας υποβολής αποφασίζονται ΟΛΕΣ μαζί με ΜΙΑ ενέργεια αντί για ξεχωριστά ανά ημέρα. `submitTeleworkRequest()` γράφει τώρα κοινό `GroupID` (νέα στήλη — χειροκίνητο βήμα, βλ. §Χειροκίνητα βήματα) σε όλες τις γραμμές μιας υποβολής, κενό για κανονικές αιτήσεις άδειας. Νέα `decideLeaveRequestGroup()` στο `src/leaves.js` — ίδιο guard/εξουσιοδότηση με `decideLeaveRequest()` αλλά εφαρμοσμένο σε όλες τις γραμμές του group, αγνοεί σιωπηλά όσες έχουν ήδη απαντηθεί (idempotent σε διπλό κλικ), ΕΝΑ συγκεντρωτικό email στον τεχνικό (όχι ένα ανά ημέρα). `POST /api/leaves/decide` δέχεται πλέον είτε `{ requestId }` (κανονική αίτηση) είτε `{ groupId }` (ομαδική τηλεργασία) — αμετάβλητο για τις υπάρχουσες κλήσεις. Στο tab Άδειες Ομάδας, το «Προς έγκριση» ομαδοποιεί client-side (`renderPending()` στο `hub.html`) τις γραμμές τηλεργασίας με το ίδιο `GroupID` σε ΜΙΑ κάρτα με κουμπιά «Έγκριση όλων»/«Απόρριψη όλων». Τα one-click email decide-links (βλ. Εκκρεμότητες #4) πλέον καλύπτουν ΚΑΙ την ομαδική τηλεργασία — βλ. σημείωση εκεί.

- **Director** — Google SSO· email στο νέο φύλλο `Directors` (headers `Email | Name`, ΑΠΟ ΤΗΝ ΑΡΧΗ, χειροκίνητο βήμα — βλ. §Χειροκίνητα βήματα). Ρητή απόφαση χρήστη: **πλήρης εποπτεία** — ό,τι βλέπει Backoffice + Team Leader μαζί (`TOOLS.director` στο `hub.html`, ένωση των δύο λιστών). Τεχνικά, ένας Director περνάει ΚΑΙ το `requireTeamLeader` ΚΑΙ το `requireBackoffice` gate (`src/index.js`) — άρα κάθε endpoint TL-only ή Backoffice-only δουλεύει αυτόματα και για Director, καμία αλλαγή χρειάστηκε στα ίδια τα endpoints.
  Κύριος σκοπός δημιουργίας: **έγκριση αδειών των ίδιων των Team Leaders** — πρόβλημα που ανακαλύφθηκε γιατί δεν υπήρχε κανένας φυσικός εγκριτής γι' αυτές (η αίτηση ενός TL ποτέ δεν εμφανιζόταν πουθενά προς έγκριση). Ο μηχανισμός routing ΔΕΝ άλλαξε — παραμένει το `TeamLeaderEmail` της self-row του TL στο Υπάλληλοι (ίδιο πεδίο με τους τεχνικούς) — απλά το `submitLeaveRequest()` (`src/leaves.js`) τώρα κάνει fallback σε `getDirectorByEmail()` όταν εκείνο το email δεν αντιστοιχεί σε πραγματικό TL. Η ίδια `isAuthorizedForTeam()`/`decideLeaveRequest()`/`getTeamLeaderData()` λειτουργεί απαράλλαχτη — ο Director περνάει το `leaderEmail` παράμετρο σαν να ήταν TL, καμία διάκριση στο backend.
  Tab «Άδειες Team Leaders» (ίδιο key `teamleaves`/component `renderTeamLeaves` με το TL tab «Άδειες Ομάδας», reuse 100%) δείχνει τους TL που έχουν `TeamLeaderEmail` = το email αυτού του Director — δηλαδή χρειάζεται το χειροκίνητο βήμα σε ΚΑΘΕ TL self-row (βλ. §Χειροκίνητα βήματα) για να εμφανιστεί εκεί.
  ΓΝΩΣΤΟ ΚΕΝΟ: το κουμπί "Λείπω" (backup TL) εμφανίζεται και στο Director tab (ίδιο component) αλλά αποτυγχάνει με καθαρό μήνυμα σφάλματος αν πατηθεί — το `setLeaderAway()` ψάχνει αποκλειστικά στο φύλλο TeamLeaders, όχι Directors. Δεν έχει νόημα ουσιαστικά για Director (δεν υπάρχει "backup Director"), οπότε αφέθηκε ως έχει αντί να προστεθεί ειδική περίπτωση.
  Πολλοί Directors υποστηρίζονται ήδη από τον μηχανισμό (απλά περισσότερες γραμμές στο φύλλο) — δεν χρειάστηκε extra δουλειά, αλλά δεν υπάρχει ακόμα κάποιο notion "ποιος Director είναι υπεύθυνος για ποιον TL" — ΟΠΟΙΟΣ Director βλέπει ΟΠΟΙΟΝΔΗΠΟΤΕ TL που δείχνει σε αυτόν μέσω του `TeamLeaderEmail` πεδίου.
  **Ιστορικό Ενεργειών — ΕΓΙΝΕ** (πρόταση Claude, αποδεκτή από χρήστη): νέο tab `auditlog`, read-only προβολή του φύλλου `AuditLog` (`GET /api/audit-log` -> `getAuditLog()` στο `src/sheets.js`) — πριν από αυτό οι καταγραφές `appendAuditLog` (reset PIN, εγκρίσεις/απορρίψεις αδειών, χρεώσεις, δημιουργία τεχνικού, αποθήκευση συνεργείων κλπ.) ήταν ορατές μόνο ανοίγοντας απευθείας το raw Sheet. Αυστηρά μόνο Director (νέο gate `requireDirector`, ΟΧΙ `requireTeamLeaderOrBackoffice` — δεν έχει νόημα να το βλέπουν TL/Backoffice). Τελευταίες 300 εγγραφές, πιο πρόσφατες πρώτα, με client-side text filter (Χρήστης/Ενέργεια/Λεπτομέρειες). Το πεδίο `Details` είναι JSON string στο Sheet — το UI το κάνει parse σε `key: value` λίστα (`auditDetailsText()`), fallback σε raw string αν δεν είναι έγκυρο JSON.
  **Backup Google Sheet — ΕΓΙΝΕ** (15/08/2026, ρητή απαίτηση χρήστη — "κάτι βασικό" που έλειπε, καμία μορφή ασφαλείας πέρα από το Google's native version history μέχρι τώρα): νέο tab `backups`, `src/backup.js`. Δύο ερωτήσεις διευκρίνισης, και τα δύο απαντήθηκαν με την προτεινόμενη επιλογή: **μορφή** = στιγμιότυπο JSON σε Cloudflare KV (νέο namespace `SHEET_BACKUPS`) — ΟΧΙ πραγματικό αντίγραφο Google Sheet μέσω Drive API, γιατί η υπάρχουσα WIF αυθεντικοποίηση (`src/sheets.js`) έχει OAuth scope ΜΟΝΟ Sheets API· ένα Drive-based αντίγραφο θα απαιτούσε νέο scope/δικαίωμα στο Google Cloud, εκτός του Worker — το JSON->KV δεν χρειάζεται ΚΑΝΕΝΑ νέο δικαίωμα. **Συχνότητα/διατήρηση** = καθημερινά (νέο cron `"0 3 * * *"`, 03:00 UTC, off-peak — βλ. `wrangler.toml`/`scheduled()`), κρατάει τελευταία 14 (`createSheetBackup()` κάνει pruning αυτόματα μετά από κάθε νέο backup).
  Περιεχόμενο: `getSheetRawValues()` (νέα, `src/sheets.js`) διαβάζει κάθε φύλλο ως raw 2D array (όχι keyed objects όπως το `readSheetAsObjects()` — μέγιστη πιστότητα για restore) και αποθηκεύεται σε ένα JSON `{createdAt, triggeredBy, sheets: {...}, errors: [...]}` ανά ημέρα (key `backup:YYYY-MM-DD`). Λίστα φύλλων σε backup (`BACKUP_SHEETS` στο `src/backup.js`): `Άδειες, Υπάλληλοι, TeamLeaders, Backoffice, Directors, Στόλος, ΑτυχήματαΟχημάτων, Εξοπλισμός, EPass, Χρεώσεις, Συνεργεία, Ενημερώσεις, ΕρωτηματαΔιαθεσιμότητας, ΑπαντησειςΔιαθεσιμότητας, ΕγγραφαΠαράδοσης, AuditLog`. Κάθε φύλλο διαβάζεται ξεχωριστά με try/catch — αν ένα λείπει/αποτύχει δεν μπλοκάρει τα υπόλοιπα, καταγράφεται στο `errors[]` του backup (ορατό στο UI). Εύρημα κατά τη διερεύνηση: το `Ρυθμίσεις` που αναφέρεται στη λίστα «Φύλλα στο Sheet» παραπάνω ΔΕΝ βρέθηκε πουθενά σε χρήση στον κώδικα (κανένα `const SHEET_`/reference) — αφαιρέθηκε από τη λίστα backup ως πιθανό stale/ανενεργό leftover· αν αποδειχτεί ότι υπάρχει πραγματικά, να προστεθεί.
  **Όριο περιεχομένου (ρητά τεκμηριωμένο):** το backup ΔΕΝ περιλαμβάνει τα ίδια τα ανεβασμένα αρχεία (φωτογραφίες παραλαβής, δικαιολογητικά, υπογραφές, συνημμένα ατυχημάτων/ανακοινώσεων κλπ.) που ζουν σε ξεχωριστά KV namespaces (`LEAVE_FILES`/`EMPLOYEE_FILES`/`FLEET_FILES`/`ANNOUNCEMENTS_FILES`/`HANDOVER_FILES`) — τα φύλλα κρατούν μόνο `FileKey` αναφορές σε αυτά, όχι τα ίδια τα bytes. Αν χρειαστεί ποτέ πλήρες backup και των αρχείων, είναι ξεχωριστό/μεγαλύτερο feature.
  **ΣΚΟΠΙΜΑ καμία αυτόματη "restore σε ζωντανό Sheet" λειτουργία** — ρίσκο να αντικατασταθούν εν αγνοία πρόσφατα δεδομένα χωρίς κανένα ανθρώπινο έλεγχο. Restore = λήψη (download) του JSON + χειροκίνητη επανεισαγωγή αν χρειαστεί ποτέ πραγματικά. Αν ζητηθεί αυτόματο restore στο μέλλον, να σχεδιαστεί ρητά ως δικό του feature (ίδια λογική με το γιατί το Εξοδολόγιο αναβλήθηκε για δική του συνεδρία).
  Πρόσβαση: αυστηρά μόνο Director (`requireDirector`, ίδιο gate με το Ιστορικό Ενεργειών — δεν ζητήθηκε TL/Backoffice πρόσβαση, και είναι πιο ευαίσθητο από το Ιστορικό αφού περιέχει ΟΛΑ τα δεδομένα προσωπικού, π.χ. ΑΜΚΑ). Endpoints: `GET /api/backups` (λίστα μεταδεδομένων), `POST /api/backups/create` (χειροκίνητη δημιουργία τώρα — π.χ. πριν από ρισκαρισμένη μαζική αλλαγή), `GET /api/backups/download?date=...` (λήψη ενός backup ως αρχείο JSON, `Content-Disposition: attachment`). UI (`renderBackups()` στο `hub.html`): πίνακας backups (ημερομηνία/φύλλα/σφάλματα/μέγεθος/πηγή) + κουμπί «Λήψη» ανά γραμμή + κουμπί «Δημιουργία backup τώρα». Καμία εξάρτηση από τα email relay secrets — το backup δεν στέλνει τίποτα, τρέχει ανεξάρτητα.

**Αρχική / Dashboard** (tab, πρώτο στο sidebar για TL/Backoffice/Director — ΟΧΙ τεχνικός) — ΕΓΙΝΕ (πρόταση Claude, αποδεκτή από χρήστη): γρήγορη εικόνα με tiles (`renderDashboard()` στο `hub.html`), γίνεται αυτόματα η προεπιλεγμένη οθόνη μετά το login αφού είναι το πρώτο entry σε κάθε `TOOLS[role]` array. Καμία νέα λογική backend — τα tiles φτιάχνονται από ήδη υπάρχοντα endpoints, καλούνται παράλληλα (`Promise.all`): "Εκκρεμείς αιτήσεις άδειας" + "ΚΤΕΟ/Service λήγουν ή έληξαν" (από `/api/leaves/team`, `/api/fleet` — TL/Director), "Τεχνικοί σε άδεια σήμερα" + "Τεχνικοί σε συνεργείο σήμερα" (από `/api/backoffice/availability`, `/api/crews?date=σήμερα` — Backoffice/Director). Κάθε tile είναι clickable (`goToTool()`) και ανοίγει το αντίστοιχο tab, ενημερώνοντας και το active state του sidebar — reuse του ίδιου `openTool()` που ήδη χρησιμοποιεί το sidebar. Ο υπολογισμός ΚΤΕΟ/service alert count reuse κάνει την ήδη υπάρχουσα `fleetDateAlertLevel()` (ίδιο 30-ημερών κατώφλι με το badge του tab Στόλος) — fix: μετρούσε λάθος και τα οχήματα χωρίς καμία καταχωρημένη ημερομηνία ΚΤΕΟ/service σαν "συναγερμό" (`'none' !== 'ok'`), τώρα μετράει μόνο πραγματικά `'warn'`/`'bad'` (ίδιο fix και στο tile του tab Στόλος).
  **Πιο επαγγελματική εμφάνιση — ΕΓΙΝΕ** (ρητή απαίτηση χρήστη): αντί για το γενικό `topbar()`/`.metrics` που χρησιμοποιούν όλα τα άλλα tabs, η Αρχική έχει δικό της hero header (χαιρετισμός ανά ώρα ημέρας + μικρό όνομα χρήστη από `CURRENT_WHO.name`, σημερινή ημερομηνία στα ελληνικά, και μια γραμμή κατάστασης "όλα καλά"/"υπάρχουν εκκρεμότητες" ανάλογα αν κάποιο tile έχει tone `warn`/`bad`) και νέα κλάση tiles `.dash-tile` (όχι `.metric`) με εικονίδιο-badge χρωματισμένο ανά νόημα (πράσινο = εντάξει, πορτοκαλί = χρειάζεται προσοχή, κόκκινο = πρόβλημα, μπλε = ουδέτερη πληροφορία), υποκείμενο κείμενο κατάστασης, και ελαφρύ hover animation. Καθαρά CSS/markup αλλαγή στο `renderDashboard()` — καμία αλλαγή endpoints/δεδομένων.
  **Σύμβαση ονομάτων (fix, ρητή διόρθωση χρήστη):** τα ονόματα στο Sheet γράφονται πάντα "Επίθετο Όνομα" — ο χαιρετισμός έπαιρνε αρχικά το ΠΡΩΤΟ κομμάτι (`.split(' ')[0]`, το επίθετο) αντί για το μικρό όνομα. Διορθώθηκε να παίρνει το ΤΕΛΕΥΤΑΙΟ κομμάτι, ίδια σύμβαση με το `suggestEmployeeId()` του tab Νέος Τεχνικός (που ήδη υπέθετε σωστά `parts[0] = επίθετο`). Το compact chip τεχνικού στο ημερολόγιο αδειών ομάδας (`dayLeaves.map`) συνεχίζει σκόπιμα να δείχνει το ΠΡΩΤΟ κομμάτι (επίθετο) — δεν αγγίχτηκε, δεν αναφέρθηκε πρόβλημα εκεί.
  **Redesign 18/08/2026 (πόρτηση από Lovable prototype, βλ. §Στυλ):** το hero header περιγραφή παραπάνω (χαιρετισμός ανά ώρα ημέρας + κατάσταση) αντικαταστάθηκε από το ίδιο απλό `topbar()`-στυλ pattern με τα υπόλοιπα tabs (`<h1>${tool.name}</h1><p>...</p>`, χωρίς role-badge — βλ. global header decisions στο §Στυλ) — μόνο η γραμμή κατάστασης "όλα καλά"/"υπάρχουν εκκρεμότητες" παρέμεινε, ως υπότιτλος.
  **«Τελευταία δραστηριότητα» — ΕΓΙΝΕ** (18/08/2026, ρητή απαίτηση χρήστη μετά από πρόταση Claude, βλ. Lovable screenshot): νέα κάρτα κάτω από τα tiles, πίνακας Εγγραφή/Ενέργεια/Τομέας/Πότε με τις 8 πιο πρόσφατες «ενδιαφέρουσες» ενέργειες. Backend: νέο `GET /api/dashboard/activity` (`handleDashboardActivity` στο `src/index.js`), gate `requireTeamLeaderOrBackoffice` (TL+Backoffice+Director, ίδιο audience με το Dashboard) — **ΞΕΧΩΡΙΣΤΟ και ΜΙΚΡΟΤΕΡΟ endpoint από το `/api/audit-log`**, που παραμένει αμετάβλητο (πλήρες raw log, 300 εγγραφές, αυστηρά μόνο Director). Reuse του ήδη υπάρχοντος `getAuditLog()`, φιλτραρισμένο μέσω νέου `ACTIVITY_ACTION_META` (χάρτης action → {κατηγορία, επιτρεπόμενοι ρόλοι, human-readable label}).
  **Φιλτράρισμα ανά ρόλο (σχεδιαστική απόφαση Claude, ΔΕΝ ρωτήθηκε ρητά η λεπτομέρεια — τηρεί όμως αυστηρά τις ήδη τεκμηριωμένες αποφάσεις πρόσβασης):** οι καταγραφές αδειών/τηλεργασίας (`leave_submitted`/`leave_decided`/`leave_decided_group`/`leave_deleted`/`telework_submitted`) εμφανίζονται **ΜΟΝΟ σε Director** — αυτό το widget είναι εταιρικό/μη scoped ανά ομάδα, και η απόφαση #1 («Αποφάσεις που ΔΕΝ αλλάζουμε») λέει ρητά ότι το Backoffice δεν βλέπει άδειες άλλων και ένας TL βλέπει μόνο τη δική του ομάδα — άρα καμία άδεια δεν έπρεπε να διαρρεύσει εδώ σε TL/Backoffice χωρίς scoping ανά ομάδα, που θα ήταν μεγαλύτερο, ξεχωριστό feature. Κάθε άλλη κατηγορία (Στόλος/Εξοπλισμός/e-pass/Χρεώσεις/Παράδοση/Ανακοινώσεις: μόνο TL+Director· Συνεργεία: μόνο Backoffice+Director· Ενημερώσεις/Προσωπικό-νέος τεχνικός: και οι τρεις ρόλοι) φιλτράρεται ώστε να φαίνεται μόνο σε ρόλους που ήδη έχουν πρόσβαση στο αντίστοιχο tab αλλού στο portal — καμία νέα ορατότητα πέρα από όσα ήδη βλέπει ο ρόλος κάπου αλλού. Εξαιρούνται εντελώς (δεν υπάρχουν στο `ACTIVITY_ACTION_META`, αγνοούνται σιωπηλά): `technician_pin_reset`, `employee_status_changed`, `leave_attachment_uploaded` (δικαιολογητικό αναρρωτικής), `availability_answer_submitted` (ατομική απάντηση Ναι/Όχι τεχνικού), και κάθε `*_email_failed`.
  UI (`renderDashboard()` στο `hub.html`): fetch `/api/dashboard/activity` μαζί με τα υπόλοιπα tile-fetches (`Promise.all`) — πάντα, ανεξαρτήτου ρόλου, αφού το backend κάνει ήδη το φιλτράρισμα. Χρωματιστό badge ανά κατηγορία (`dashActivityTone()`) — reuse των ήδη dark-mode-safe τόνων `.status-pill` (`st-ok`/`st-warn`) + νέος ουδέτερος τόνος `st-info` (`var(--accent-bg)`/`var(--accent-dark)`, ήδη dark-safe χωρίς επιπλέον override, ίδιο μοτίβο με το `.app-avatar`) — καμία νέα hardcoded τιμή χρώματος. Κενή κατάσταση: «Καμία πρόσφατη δραστηριότητα.» Το εμφανιζόμενο όνομα ανά εγγραφή προκύπτει από batch email→όνομα lookup (μία ανάγνωση ανά φύλλο TeamLeaders/Backoffice/Υπάλληλοι, όχι ανά εγγραφή — ίδια πηγή/προτεραιότητα με το `resolveActorName()` του `src/email.js`, αλλά χωρίς το κόστος επαναλαμβανόμενων αναγνώσεων).

**Εξαγωγή CSV** (Στόλος, Εξοπλισμός, Ιστορικό Ενεργειών) — ΕΓΙΝΕ (πρόταση Claude, αποδεκτή από χρήστη, 15/08/2026, item #120): γενικό `exportTableToCsv(filename, headers, rows)` στο `hub.html` — καθαρά client-side (χωρίς backend endpoint/βιβλιοθήκη), φτιάχνει `Blob` και κατεβάζει με προσωρινό `<a download>`. UTF-8 BOM (`String.fromCharCode(0xFEFF)`) στην αρχή του αρχείου ώστε τα ελληνικά να ανοίγουν σωστά στο Excel (χωρίς BOM το Excel υποθέτει λάθος encoding και δείχνει "αλλοιωμένους" χαρακτήρες). Κουμπί «Εξαγωγή CSV» σε 3 σημεία: (1) tab Στόλος (`exportFleetCsv`, ΟΛΑ τα οχήματα — ανεξάρτητο από το φίλτρο αναζήτησης), (2) tab Εξοπλισμός (`exportEquipmentCsv`, νέα global `EQUIPMENT_ALL_ITEMS`), (3) tab Ιστορικό Ενεργειών/Director (`exportAuditLogCsv` — εξάγει ό,τι είναι ΤΩΡΑ φιλτραρισμένο/ορατό μέσω του υπάρχοντος text filter, όχι πάντα και τις 300 εγγραφές· `auditDetailsPlainText()` νέο, ίδιο parsing με το `auditDetailsText()` του UI αλλά χωρίς HTML). Καμία αλλαγή backend/Sheet.
  **Εξαγωγή PDF στα ίδια 3 σημεία — ΕΓΙΝΕ** (item #122, 15/08/2026, ρητή απαίτηση χρήστη — ήθελε "επιλογή για PDF ή Excel" δίπλα στο CSV): νέο κουμπί «Εξαγωγή PDF» δίπλα σε κάθε «Εξαγωγή CSV». Γενικό `exportTableToPdf(title, headers, rows)` στο `hub.html`, ίδιο μοτίβο browser print (`window.open` + `window.print()`, χωρίς εξωτερική βιβλιοθήκη) με τα υπόλοιπα PDF exports του portal (Δελτίο Παράδοσης/Παραλαβής, ιστορικό αδειών τεχνικού, `evaluation.html`). Τα headers/rows είναι ήδη έτοιμα από τα CSV exporters — κοινοί `*_EXPORT_HEADERS`/row-builders όπου τα δεδομένα δεν έχουν ημερομηνίες (Εξοπλισμός, Ιστορικό Ενεργειών)· στο Στόλο οι δύο μορφές έχουν ΞΕΧΩΡΙΣΤΟ row-builder για τις ημερομηνίες (`exportFleetCsv` κρατά raw ISO τιμές από το Sheet, κατάλληλο για επεξεργασία σε Excel· `exportFleetPdf` περνάει τις ημερομηνίες από `fmtDate()` σε ελληνική μορφή, κατάλληλο για ανάγνωση/εκτύπωση) — σκόπιμη ασυμμετρία, όχι απλά reuse.

**Badges εκκρεμοτήτων στο sidebar** — ΕΓΙΝΕ (πρόταση Claude, αποδεκτή από χρήστη, 15/08/2026, item #121): μικρό κόκκινο pill αριθμού δίπλα σε 2 nav-items, `.nav-badge` (`hub.html`) — καμία νέα λογική backend, καθαρό reuse ήδη υπάρχοντων endpoints. (1) **«Άδειες Ομάδας»/«Άδειες Team Leaders»** (`teamleaves`, TL+Director) — δείχνει `pendingCount` από το ήδη υπάρχον `GET /api/leaves/team` (ίδιο πεδίο με το tile «Εκκρεμείς αιτήσεις άδειας» της Αρχικής). (2) **«Ανακοινώσεις»** (`availreq`, ΜΟΝΟ τεχνικός) — πλήθος ενεργών ερωτημάτων διαθεσιμότητας χωρίς δική του απάντηση ακόμα (`Active && !MyAnswer`) από το ήδη υπάρχον `GET /api/availability/my-queries`. `refreshNavBadges()` καλείται στο login (`init()`) και ξανά μετά από ενέργειες που αλλάζουν τον αριθμό (`decideRequest`/`decideRequestGroup` — έγκριση/απόρριψη άδειας, `submitAvailabilityAnswerUi` — απάντηση ερωτήματος) ώστε το badge να μένει σωστό χωρίς reload σελίδας. Σιωπηλή αποτυχία αν κάποιο fetch αποτύχει (το badge απλά δεν ενημερώνεται, καμία διακοπή λειτουργίας). Backoffice δεν παίρνει κανένα badge (δεν έχει το `teamleaves` tool, και το `availreq` του Backoffice είναι δημιουργία ερωτημάτων όχι απάντηση).

**Αναλυτικά / KPI** (tab, TL+Director — ΟΧΙ Backoffice) — ΕΓΙΝΕ (πρόταση Claude, αποδεκτή από χρήστη, 15/08/2026): νέο `src/analytics.js` + `GET /api/analytics/kpi` (`handleAnalyticsKpi` στο `src/index.js`, ίδιο gate `requireTeamLeader` με το tab Στόλος/Εξοπλισμός — καλύπτει αυτόματα και Director). **Ρητή απόφαση χρήστη:** εταιρικά στοιχεία, ΙΔΙΑ και για τους δύο ρόλους — ΟΧΙ scoped ανά ομάδα TL (σε αντίθεση με το tab Άδειες Ομάδας). 3 metrics, καθαρός συνδυασμός ήδη υπαρχόντων read paths, καμία νέα αποθήκευση: (1) **Χρήση αδειών/μήνα** (τελευταίοι 6 μήνες) — σύνολο εργάσιμων ημερών ΕΓΚΕΚΡΙΜΕΝΗΣ άδειας ΟΛΩΝ των τύπων (ΣΥΜΠΕΡΙΛΑΜΒΑΝΕΙ Τηλεργασία εδώ, σε αντίθεση με το ατομικό υπόλοιπο αδειών — είναι εταιρικό trend, όχι υπολογισμός υπολοίπου· ίδιο scope με τη μηνιαία αναφορά λογιστή, βλ. `sendAccountantMonthlyReport`), reuse `getApprovedLeavesInRange()`/`calculateWorkDays()` από το `src/leaves.js`, κάθε μήνας μετριέται clipped στα δικά του όρια. Εμφανίζεται ως απλό CSS bar chart (χωρίς εξωτερική βιβλιοθήκη — ίδια λογική με browser-print PDF αντί για pdf lib αλλού στο portal). (2) **Ποσοστό χρήσης στόλου** — % οχημάτων με `AssignedTo` συμπληρωμένο ΤΩΡΑ, reuse `listVehicles()` από το `src/fleet.js`. (3) **Μ.ο. τεχνικών/ημέρα σε συνεργεία** — νέα `getCrewsKpiSummary()` στο `src/crews.js` (ΜΙΑ ανάγνωση του φύλλου `Συνεργεία`, όχι N κλήσεις `getCrewsForDate`), μέσος όρος πάνω στις τελευταίες (έως) 30 αποθηκευμένες ημέρες σύνθεσης συνεργείων. Καμία αλλαγή Sheet/χειροκίνητο βήμα.

**Ενημερώσεις/Οδηγίες** (tab, όλοι οι ρόλοι — δημιουργός/διαγραφή μόνο TL + Backoffice, βλέπουν όλοι): απλή χρονολογική λίστα ανακοινώσεων/οδηγιών εργασίας προς το προσωπικό (`src/announcements.js`, νέο φύλλο `Ενημερώσεις` — χειροκίνητο βήμα, βλ. SETUP.md). Κάθε ενημέρωση έχει τίτλο, προαιρετικό κείμενο, προαιρετικό συνημμένο αρχείο (μόνο PDF/Word, μέγιστο 20MB). Καμία παρακολούθηση "το είδα/το διάβασα" (ρητή απόφαση χρήστη). Το ίδιο το αρχείο ΔΕΝ αποθηκεύεται στο Sheet (μόνο μεταδεδομένα) — αποθηκεύεται στο Cloudflare KV `ANNOUNCEMENTS_FILES` σαν raw bytes (νέο KV namespace, δηλωμένο στο `wrangler.toml`). Θα προτιμούσαμε Cloudflare R2 (πιο κατάλληλο για αρχεία), αλλά το R2 δεν είναι ενεργοποιημένο στο Cloudflare account (χρειάζεται χειροκίνητη ενεργοποίηση από το Dashboard, δεν γίνεται προγραμματιστικά) — pivot σε KV χωρίς να χρειαστεί να διακοπεί η δουλειά. Αν ο χρήστης ενεργοποιήσει αργότερα το R2 από το Dashboard, μπορεί να ζητηθεί μεταφορά.
  **Στοχευμένη δημοσίευση ανά ειδικότητα — ΕΓΙΝΕ** (20/08/2026, ρητή απαίτηση χρήστη, αποφασίστηκε μέσω διευκρινιστικών ερωτήσεων πριν την υλοποίηση): νέο προαιρετικό πεδίο `TargetTeams` στη φόρμα «Νέα ενημέρωση» — ίδιο σχήμα/UI (checkboxes «Όλοι» + λίστα ειδικοτήτων από `listTeamOptions()`) με το `TargetTeams` του «Ανακοινώσεις» (`src/availability.js`). Κενό/«Όλοι» = ίδια συμπεριφορά με πριν, ορατό σε ΟΛΟΥΣ (τεχνικούς + staff), ΚΑΜΙΑ αλλαγή email (καμία αποστολή, όπως πάντα). Συγκεκριμένες ειδικότητες = η ενημέρωση φαίνεται ΜΟΝΟ στους τεχνικούς εκείνων των ειδικοτήτων — `GET /api/announcements` πλέον διακλαδίζεται ανά ρόλο: τεχνικός καλεί νέα `listAnnouncementsForTechnician()` (φιλτραρισμένη βάσει `Team` του Υπάλληλοι, ίδιο μοτίβο `teamsMatch()`), TL/Backoffice/Director συνεχίζουν να καλούν το ίδιο `listAnnouncements()` χωρίς φίλτρο — **ρητή ασυμμετρία**: το staff δεν έχει ειδικότητα με αυτή την έννοια, άρα βλέπει ΠΑΝΤΑ τα πάντα ό,τι κι αν επιλεγεί ως στόχευση (ίδιο σκεπτικό ασυμμετρίας δημιουργού/θεατή με το «Ανακοινώσεις»). Στοχευμένη δημοσίευση στέλνει ΚΑΙ email ειδοποίησης στους στοχευμένους τεχνικούς (νέα `notifyAnnouncementTargets()`, ίδιο Reply-To=actor μοτίβο) — ρητή απόφαση χρήστη, ΜΟΝΟ για τη νέα στοχευμένη περίπτωση, οι broadcast σε «Όλοι» ΔΕΝ αποκτούν email (παραμένουν όπως ήταν, καμία αναδρομική αλλαγή). Παλιές γραμμές πριν το feature (κενό `TargetTeams`) γίνονται parse ως `["ALL"]` — παραμένουν ορατές σε όλους όπως πριν, καμία «κρυφή» απώλεια ορατότητας. Στη λίστα, κάθε στοχευμένη ενημέρωση δείχνει `Στοχευμένο: <ειδικότητες>` δίπλα στα μεταδεδομένα (και στους δύο ρόλους) — οι broadcast δεν δείχνουν τίποτα επιπλέον.

**Νέος Τεχνικός** (tab, TL + Backoffice): φόρμα προσθήκης τεχνικού — γράφει γραμμή στο `Υπάλληλοι`, φτιάχνει τυχαίο 4ψήφιο PIN + το κάνει seed στο KV `TECHNICIAN_AUTH` (ίδιος μηχανισμός με `/api/admin/set-pin`, χωρίς `ADMIN_SECRET` — η εξουσιοδότηση είναι το session role check), και στέλνει email με τα credentials (αν δόθηκε email — no-op μέχρι να μπουν τα Apps Script relay secrets, βλ. Εκκρεμότητες). Το PIN εμφανίζεται ΠΑΝΤΑ μία φορά στην οθόνη ως fallback. Και οι δύο ρόλοι (TL, Backoffice) επιλέγουν ελεύθερα σε ΠΟΙΟΝ Team Leader ανήκει ο νέος τεχνικός από dropdown — ρητή απαίτηση χρήστη, δεν είναι πλέον fixed στον εαυτό του για TL (απλά προεπιλέγεται). Το πεδίο Περιοχή/Team είναι dropdown με τις ήδη υπάρχουσες τιμές (από Υπάλληλοι+TeamLeaders, χωρίς hardcoded λίστα) + επιλογή "Άλλη περιοχή..." για νέα τιμή. Πεδίο "Δίπλωμα οδήγησης" (Ναι/Όχι, απλό dropdown χωρίς παρακολούθηση λήξης — ρητή απαίτηση χρήστη) γράφεται στη νέα στήλη `HasDrivingLicense` του Υπάλληλοι. Endpoint `/api/employees/create` (GET φόρτωμα φόρμας, POST καταχώρηση) — ΔΕΝ είναι στα 5 Access-protected paths, βασίζεται στο session-based role check (ίδιο μοτίβο με `/api/leaves/submit`, `/api/charges`).
  **Reset PIN τεχνικού ("Ξέχασες τον κωδικό;") — ΕΓΙΝΕ, ΜΕΤΑΚΟΜΙΣΕ στο Προφίλ Τεχνικού 15/08/2026** (item #5, αρχικό σχέδιο: βλ. Εκκρεμότητες για το σκεπτικό): φτιάχνει νέο τυχαίο 4ψήφιο PIN, το κάνει seed στο KV `TECHNICIAN_AUTH` (ίδιος μηχανισμός/`hashPin` με "Νέος Τεχνικός"), γράφει `appendAuditLog("technician_pin_reset", ...)`, και στέλνει email στον τεχνικό αν έχει καταχωρημένο email (Reply-To = actor, ίδιο μοτίβο). Το νέο PIN εμφανίζεται ΠΑΝΤΑ μία φορά στην οθόνη ως fallback. Επιβεβαιωτικό `confirm()` dialog στο UI πριν την ενέργεια (μη αναστρέψιμη — παλιό PIN πάψει να δουλεύει αμέσως). **ΔΕΝ είναι πια εδώ στο "Νέος Τεχνικός"** — η κάρτα μετακόμισε στο tab «Προφίλ Τεχνικού» (βλ. Ρόλοι § Team Leader → Προφίλ Τεχνικού § Reset PIN) και το auth gate έγινε αυστηρότερο `requireTeamLeader` (ΟΧΙ πια Backoffice, ρητή απόφαση χρήστη — βλ. εκεί).

**Μπλοκάρισμα υποβολής άδειας χωρίς επαρκές υπόλοιπο** — ΕΓΙΝΕ (ρητή απαίτηση χρήστη): `submitLeaveRequest()` (`src/leaves.js`) απορρίπτει την υποβολή αν οι ζητούμενες εργάσιμες ημέρες ξεπερνούν το `remainingDays` του τεχνικού (`getEmployeeBalance()`, ίδιος ορισμός με αυτό που ήδη βλέπει ο τεχνικός στο tab «Οι Άδειές μου»). Εξαιρέσεις: `LEAVE_TYPES_EXEMPT_FROM_BALANCE_CAP` (`src/leaves.js`) — **ΕΠΕΚΤΑΘΗΚΕ 19/08/2026** (ρητή απαίτηση χρήστη, ΑΝΑΘΕΩΡΕΙ την αρχική μόνο-`"Άδεια χωρίς αποδοχές"` εξαίρεση): τώρα περιλαμβάνει ΚΑΙ την `"Αναρρωτική"` — σκεπτικό χρήστη: μαζί με την «Άνευ αποδοχών» (δεν "καταναλώνει" πραγματικό υπόλοιπο) και οτιδήποτε άλλο απαιτεί δικαιολογητικό δεν πρέπει να μπλοκάρεται από υπόλοιπο, η Αναρρωτική τεκμηριώνεται ήδη με το υπάρχον μηχανισμό ανεβάσματος PDF (`uploadLeaveAttachment`, βλ. Επόμενα ζητούμενα #1) — αν προστεθεί στο μέλλον νέος τύπος άδειας που επίσης απαιτεί δικαιολογητικό, προστίθεται στην ίδια λίστα, ρητή οδηγία χρήστη ("ή άλλο..."). Ισχύει ΚΑΙ όταν καταχωρεί ο TL εκ μέρους τεχνικού (`submitLeaveRequestAsLeader` καλεί την ίδια συνάρτηση) — καμία αλλαγή χρειάστηκε εκεί. Δεν αγγίζει την Τηλεργασία (ξεχωριστή συνάρτηση `submitTeleworkRequest`, ήδη εκτός ετήσιου υπολοίπου) ούτε τα εκκρεμή αιτήματα άλλων τύπων (ο έλεγχος μετράει μόνο ήδη εγκεκριμένες ημέρες, ίδιο με το `usedDays` του `getEmployeeBalance` — δεν μπλοκάρει σωρευτικά πολλαπλά *εκκρεμή* αιτήματα που μαζί θα ξεπερνούσαν το υπόλοιπο, μόνο τη μεμονωμένη υποβολή σε σχέση με ό,τι είναι ήδη εγκεκριμένο).

**Προσαύξηση αδειών (proration) για υπαλλήλους <1 έτος στην εταιρεία — ΕΓΙΝΕ** (19/08/2026, ρητή απαίτηση χρήστη, αποφασίστηκε μέσω διευκρινιστικών ερωτήσεων πριν την υλοποίηση): νέα `computeAccruedAnnualDays(emp)` (`src/leaves.js`), καλείται μέσα στο `getEmployeeBalance()` αντί για το ωμό `emp.AnnualDays` — άρα διαπερνάει αυτόματα ΠΑΝΤΟΥ όπου το `remainingDays`/`annualDays` ήδη χρησιμοποιείται (tab «Οι Άδειές μου», μπλοκάρισμα υποβολής παραπάνω, Προφίλ Τεχνικού, Ημερολόγιο τεχνικού), χωρίς καμία αλλαγή σε αυτά τα σημεία. Λογική: για υπάλληλο με `HireDate` λιγότερο από 12 μήνες πριν, το ετήσιο δικαίωμα ΔΕΝ είναι το πλήρες `AnnualDays` (π.χ. 22) — αντ' αυτού προσαυξάνεται **1,7 ημέρες ανά συμπληρωμένο μήνα**. Τρεις ρητές αποφάσεις χρήστη μέσω ερωτήσεων: (α) μόλις συμπληρωθεί το 1 έτος, το δικαίωμα περνάει ΑΜΕΣΩΣ στο πλήρες `AnnualDays` (όχι προοδευτική συνέχιση της προσαύξησης)· (β) υπάλληλοι ΧΩΡΙΣ καταχωρημένη `HireDate` (προαιρετικό πεδίο, πολλοί παλιοί τεχνικοί δεν το έχουν) κάνουν σιωπηλά fallback στο πλήρες `AnnualDays` όπως πριν — ρητή δήλωση χρήστη ότι θα καταχωρήσει ο ίδιος χειροκίνητα το σωστό υπόλοιπο για αυτούς· (γ) «συμπληρωμένος μήνας» = **απλοποιημένη διαφορά ημερολογιακών μηνών** από την HireDate (`(έτοςΣήμερα-έτοςΠρόσληψης)*12 + (μήναςΣήμερα-μήναςΠρόσληψης)`), ΟΧΙ ακριβής ημερήσια επέτειος — ρητά αποδεκτή απλοποίηση, π.χ. πρόσληψη 30 Ιανουαρίου δίνει ήδη 1,7 ημέρες μόλις μπει ο Φεβρουάριος. Το αποτέλεσμα στρογγυλοποιείται σε 1 δεκαδικό (αποφεύγει floating-point artifacts όπως 5.099999...) και ΠΟΤΕ δεν ξεπερνά το καταχωρημένο `AnnualDays` του υπαλλήλου (safety cap, για την περίπτωση π.χ. part-time υπαλλήλου με μικρότερο annual). Καμία αλλαγή Sheet/χειροκίνητο βήμα — χρησιμοποιεί το ήδη υπάρχον πεδίο `HireDate`.

Ο ρόλος κρίνεται στο `resolveStaffRole()`: TeamLeaders πρώτα, μετά Backoffice, μετά Director. Αν κάποιος είναι καταχωρημένος σε πάνω από ένα φύλλο, κερδίζει το πρώτο που ταιριάζει (π.χ. TL+Director μαζί θα εμφανιστεί σαν TL, όχι Director) — δεν προέκυψε τέτοια ανάγκη ακόμα, αν χρειαστεί ρώτα πρώτα.

**Μαζική άδεια σε όλους τους υπαλλήλους — ΕΓΙΝΕ** (19/08/2026, ρητή απαίτηση χρήστη, αποφασίστηκε μέσω διευκρινιστικών ερωτήσεων πριν την υλοποίηση): νέα κάρτα «Μαζική άδεια (όλοι οι υπάλληλοι)» στο tab «Άδειες Team Leaders» (`renderTeamLeaves()` στο `hub.html`, ίδιο component με το TL tab «Άδειες Ομάδας» — reuse 100%, ορατή ΜΟΝΟ όταν `CURRENT_WHO.staffRole === 'director'`, ΚΑΘΕ Director μπορεί να τη χρησιμοποιήσει, ΟΧΙ owner-only σαν το κουμπί bulk-welcome). Χρησιμοποιείται για περιπτώσεις όπως εταιρικές διακοπές/διακοπή λειτουργίας — καταχωρεί μία ΠΡΑΓΜΑΤΙΚΗ, ΗΔΗ ΕΓΚΕΚΡΙΜΕΝΗ αίτηση άδειας (τύπος/ημερομηνίες επιλέγονται από dropdown, ίδιες επιλογές με το «Καταχώρηση άδειας για τεχνικό») ταυτόχρονα σε ΟΛΟΥΣ τους ενεργούς υπαλλήλους του φύλλου `Υπάλληλοι` — τεχνικούς ΚΑΙ τις self-rows TL/Backoffice/Director μαζί (ρητή απόφαση χρήστη: «απολύτως όλοι»), εξαιρουμένων μόνο των `Status === "Ανενεργός"`.
  Backend: νέα `submitBulkLeave(env, {type, startDate, endDate, note, enteredBy})` (`src/leaves.js`) — καθαρό reuse του `submitLeaveRequest()` σε loop, `autoApprove: true` πάντα (ίδια λογική με `submitLeaveRequestAsLeader`: η ίδια η μαζική καταχώρηση από τον Director ΕΙΝΑΙ η απόφαση). `POST /api/admin/bulk-leave` (`handleBulkLeave`, gate `requireDirector`). Καμία ειδική εξαίρεση από το μπλοκάρισμα υπολοίπου — ισχύει κανονικά το `LEAVE_TYPES_EXEMPT_FROM_BALANCE_CAP` ανά τύπο, όπως παντού· αν κάποιος υπάλληλος δεν έχει αρκετό υπόλοιπο ΜΟΝΟ η δική του καταχώρηση αποτυγχάνει (per-employee try/catch, συλλέγεται σε `failed[]` με λόγο), όλοι οι υπόλοιποι προχωράνε κανονικά — το UI δείχνει `X/Y επιτυχείς` + ονόματα/λόγους αποτυχίας. Κάθε επιτυχής καταχώρηση στέλνει το ήδη υπάρχον email αυτόματης έγκρισης στον υπάλληλο (ίδιο μηχανισμό με κάθε άλλη `autoApprove` καταχώρηση, καμία αλλαγή εκεί) — σε ~40 υπαλλήλους δεν υπάρχει πρόβλημα με το όριο του relay (1.500-2.000/ημέρα). `confirm()` dialog στο UI πριν την αποστολή (μη αναστρέψιμη μαζικά — χρειάζεται ξεχωριστή διαγραφή ανά αίτηση αν χρειαστεί αναίρεση, ίδιο υπάρχον μηχανισμό `deleteLeaveRequest`). `appendAuditLog(env, "bulk_leave_created", ...)` για ορατότητα στο Ιστορικό Ενεργειών.

**Μαζική αποστολή καλωσορίσματος — επιλογή παραληπτών ΕΓΙΝΕ (20/08/2026, ρητή απαίτηση χρήστη, ΑΝΑΘΕΩΡΕΙ το αρχικό all-or-nothing)**: η κάρτα «Μαζική αποστολή καλωσορίσματος» (`bulkWelcomeCardHtml()`/`wireBulkWelcomeButtons()` στο `hub.html`, ορατή αποκλειστικά σε `BULK_WELCOME_OWNER_EMAIL = "s.xronis@optikitec.gr"` — owner-only, ΟΧΙ ανά ρόλο Director, βλ. `requireBulkWelcomeOwner()` στο `src/index.js`· reused σε 2 tabs, «Νέος Τεχνικός» + «Αρχική») είχε αρχικά δύο κουμπιά που έστελναν αμέσως σε ΟΛΟΥΣ (τεχνικούς με μαζικό PIN reset, ή Backoffice/TL/Director με απλό ενημερωτικό email) — πρόβλημα: αν προστεθεί ΕΝΑΣ νέος Backoffice/TL/Director, δεν υπήρχε τρόπος να καλωσοριστεί εκείνος μόνος του χωρίς να ξαναενοχλήσει όλους τους υπόλοιπους. Λύση: **και τα δύο κουμπιά** (ρητή επιβεβαίωση χρήστη μέσω ερώτησης) ανοίγουν πλέον ένα modal με λίστα υποψήφιων παραληπτών (checkbox ανά άτομο + «Επιλογή όλων»), και η αποστολή γίνεται ΜΟΝΟ στους επιλεγμένους.
  Backend: νέα `GET /api/admin/bulk-welcome/technicians`/`GET /api/admin/bulk-welcome/staff` (`handleBulkWelcomeTechniciansList`/`handleBulkWelcomeStaffList`, ίδιο gate `requireBulkWelcomeOwner`) επιστρέφουν τη λίστα υποψήφιων — reuse των ίδιων φίλτρων με το POST μέσω νέων κοινών `getBulkWelcomeTechnicianTargets(env)`/`getBulkWelcomeStaffTargets(env, ownerEmail)` (εξήχθησαν από το σώμα των υπαρχόντων POST handlers, ώστε GET/POST να ΜΗΝ αποκλίνουν ποτέ). Τα υπάρχοντα `POST /api/admin/bulk-welcome/technicians`/`.../staff` δέχονται πλέον προαιρετικό body `{employeeIds:[...]}`/`{emails:[...]}` — όταν υπάρχει, η αποστολή περιορίζεται στους επιλεγμένους (400 αν το φιλτραρισμένο σύνολο είναι κενό)· όταν ΛΕΙΠΕΙ το body εντελώς (ή είναι άκυρο JSON), στέλνει σε ΟΛΟΥΣ όπως πριν — σκόπιμη backward-compatible πτώση, δεν χρειάστηκε στην πράξη αφού το UI στέλνει πάντα επιλογή πλέον.
  Frontend: το modal ακολουθεί την ήδη υπάρχουσα γενική μηχανή modal (`.modal-overlay`/`data-modal-open`/`data-modal-close`/`openModal()`/`closeModal()`) — δύο νέα modals `#bw-tech-modal`/`#bw-staff-modal`, γεμίζουν δυναμικά μέσω fetch GET τη στιγμή του κλικ (loading state, error state). Το κουμπί «Επιλογή όλων» τσεκάρει/ξετσεκάρει όλα τα ορατά checkboxes· το κουμπί επιβεβαίωσης μέσα στο modal μαζεύει τα επιλεγμένα, κλείνει το modal, δείχνει `confirm()` με το πλήθος, και POSTάρει το αντίστοιχο body. Καμία αλλαγή στο μήνυμα αποτελέσματος (`#bw-msg`, `X/Y στάλθηκαν` + λίστα αποτυχιών) — ίδιο με πριν.

## Αποφάσεις που ΔΕΝ αλλάζουμε χωρίς να ρωτηθεί ο χρήστης

1. **Backoffice δεν καταχωρεί/βλέπει άδειες άλλων.** Μόνο οι TL, και μόνο για τη δική τους ομάδα. Ρητή απαίτηση.
   Refinement (ρητό αίτημα χρήστη): το Backoffice βλέπει πλέον ΜΙΑ read-only σύνοψη διαθεσιμότητας (tab "Διαθεσιμότητα Τεχνικών") — κατάσταση σήμερα + επερχόμενες άδειες όλων των τεχνικών, όλων των ομάδων. Αυτό ΔΕΝ περιλαμβάνει λεπτομέρειες/σημειώσεις αίτησης, ιστορικό, ούτε δυνατότητα έγκρισης/απόρριψης ή καταχώρησης — αυτά παραμένουν αποκλειστικά TL. Αν χρειαστεί περαιτέρω αλλαγή στο πόσο βλέπει το Backoffice, ρώτα πρώτα.
2. **Καμία αλλαγή σε org policies του Google Cloud.** Γι' αυτό χρησιμοποιούμε WIF αντί για service account key — το `iam.disableServiceAccountKeyCreation` παραμένει ενεργό.
3. **Cloudflare Access προστατεύει ΜΟΝΟ 5 paths** (όριο destinations): `api/leaves/team`, `api/leaves/decide`, `api/leaves/submit-for-team`, `api/fleet/*`, `api/auth/staff-login`.
   Το `/hub` και τα κοινά endpoints **πρέπει να μείνουν ανοιχτά** — αλλιώς κλειδώνονται έξω οι τεχνικοί (δεν έχουν Google λογαριασμό). Η ασφάλεια γίνεται στον Worker (`resolveIdentity`).
   Νέα features που χρειάζονται προστατευμένο write στο TL scope κάνουν **reuse υπάρχοντος path με διαφορετικό HTTP method** αντί για νέο path (π.χ. `POST /api/leaves/team` για το backup-TL toggle, δίπλα στο υπάρχον `GET`) — υπόθεση ότι το Access matching είναι path-based, ανεξάρτητο του method. Αν αποδειχτεί λάθος, χρειάζεται αναθεώρηση.
4. `/api/auth/staff-login` είναι το μόνο Access-protected gate: κάνει mint δικό μας session cookie και redirect στο `/hub`.

## Χειροκίνητα βήματα στο Google Sheet (εκκρεμούν — πρέπει να τα κάνει ο χρήστης)

- **Φύλλο `Στόλος`**: πρόσθεσε επικεφαλίδες `KteoDate` και `ServiceDate` στα κελιά I1/J1 (για τις προειδοποιήσεις λήξης ΚΤΕΟ/service). Χωρίς αυτές οι ημερομηνίες δεν διαβάζονται πίσω σωστά.
- **Φύλλο `TeamLeaders`**: πρόσθεσε επικεφαλίδες `BackupEmail` και `Away` (για το backup TL feature). Χωρίς αυτές το feature είναι σιωπηλά ανενεργό (ασφαλές, απλά δεν κάνει τίποτα).
- **Νέο φύλλο `Εξοπλισμός`**: δημιούργησέ το ΑΠΟ ΤΗΝ ΑΡΧΗ (δεν υπήρχε πριν) με headers `ID | Name | Category | AssignedTo | Status | Note | UpdatedBy | UpdatedAt`. Χωρίς αυτό, το tab "Εξοπλισμός" θα αποτυγχάνει εντελώς (όχι σιωπηλά ανενεργό — θα δείχνει σφάλμα).
- **Φύλλο `Υπάλληλοι`**: πρόσθεσε επικεφαλίδη `HasDrivingLicense` στο τέλος (νέα στήλη, μετά το `Team`) — για το πεδίο "Δίπλωμα οδήγησης" στη φόρμα Νέος Τεχνικός. Χωρίς αυτή, η τιμή απλά δεν αποθηκεύεται πουθενά (σιωπηλά ανενεργό, καμία διακοπή λειτουργίας).
- **Νέο φύλλο `EPass`**: δημιούργησέ το ΑΠΟ ΤΗΝ ΑΡΧΗ με headers `ID | Label | AssignedTechnicianId | AssignedTechnicianName | AssignedVehicleId | AssignedVehiclePlate | Status | Note | UpdatedBy | UpdatedAt`. Χωρίς αυτό, το tab "e-pass" θα αποτυγχάνει εντελώς (θα δείχνει σφάλμα, όχι σιωπηλά ανενεργό).
- **Φύλλο `Χρεώσεις`**: πρόσθεσε επικεφαλίδες `VehicleId` και `VehiclePlate` στο τέλος (νέες στήλες) — για τις χρεώσεις e-pass που δείχνουν ΚΑΙ σε τεχνικό ΚΑΙ σε όχημα ταυτόχρονα. Χωρίς αυτές, οι χρεώσεις e-pass γράφονται κανονικά αλλά χωρίς το κομμάτι του οχήματος (σιωπηλά ημιτελές, καμία διακοπή λειτουργίας).
- **Φύλλο `Άδειες`**: πρόσθεσε επικεφαλίδες `FileKey`, `FileName`, `FileType` στο τέλος (νέες στήλες) — για το δικαιολογητικό PDF αναρρωτικής άδειας (βλ. Εκκρεμότητες → #1 έγινε). Χωρίς αυτές, το ανέβασμα/λήψη δικαιολογητικού θα αποτυγχάνει (θα δείχνει σφάλμα, όχι σιωπηλά ανενεργό — γράφει positionally στη 15η/16η/17η στήλη ό,τι κι αν υπάρχει εκεί).
- **Φύλλο `Άδειες`**: πρόσθεσε επικεφαλίδη `GroupID` στο τέλος (νέα στήλη, μετά το `FileType`) — για την ομαδική έγκριση/απόρριψη τηλεργασίας (βλ. Ρόλοι § Backoffice). Χωρίς αυτή, η τιμή γράφεται κανονικά στη θέση της (το `appendRow` γράφει positionally, δεν κοιτάζει τις επικεφαλίδες), αλλά χωρίς το header η `readSheetAsObjects()` δεν θα την αναγνωρίζει ως `GroupID` — η ομαδοποίηση στο UI σιωπηλά δεν θα βρίσκει καμία, κάθε ημέρα τηλεργασίας θα εμφανίζεται ξανά ξεχωριστά στο «Προς έγκριση» (ασφαλές fallback, καμία διακοπή λειτουργίας αλλού).
- **Νέο φύλλο `Directors`**: δημιούργησέ το ΑΠΟ ΤΗΝ ΑΡΧΗ με headers `Email | Name` και πρόσθεσε μία γραμμή για κάθε Director (π.χ. το δικό σου email). Χωρίς αυτό, ο ρόλος Director είναι σιωπηλά ανενεργό — κανείς δεν θα εμφανίζεται σαν Director, καμία διακοπή λειτουργίας αλλού.
- **Φύλλο `Υπάλληλοι`**: για κάθε self-row Team Leader, βάλε στο `TeamLeaderEmail` το email ενός καταχωρημένου Director (αντί για κενό/άκυρη τιμή) — ώστε η δική του αίτηση άδειας να δρομολογείται σωστά και να εμφανίζεται στο Director στο tab «Άδειες Team Leaders». Χωρίς αυτό, η υποβολή άδειας από TL θα αποτυγχάνει με σφάλμα "Δεν βρέθηκε εγκριτής" (όχι σιωπηλά ανενεργό).
- **Νέο φύλλο `Συνεργεία`**: δημιούργησέ το ΑΠΟ ΤΗΝ ΑΡΧΗ με headers `Date | CrewsJSON | SavedBy | SavedAt`. Χωρίς αυτό, το tab "Συνεργεία" θα αποτυγχάνει εντελώς (θα δείχνει σφάλμα, όχι σιωπηλά ανενεργό).
- **Νέο φύλλο `Ενημερώσεις`**: δημιούργησέ το ΑΠΟ ΤΗΝ ΑΡΧΗ με headers `ID | Title | Body | FileKey | FileName | FileType | PostedBy | PostedByName | PostedAt`. Χωρίς αυτό, το tab "Ενημερώσεις/Οδηγίες" θα αποτυγχάνει εντελώς (θα δείχνει σφάλμα, όχι σιωπηλά ανενεργό). Το KV namespace `ANNOUNCEMENTS_FILES` για τα συνημμένα είναι ήδη δηλωμένο στο `wrangler.toml` — ενεργοποιείται αυτόματα στο επόμενο deploy, δεν χρειάζεται κάτι επιπλέον.
- **Φύλλο `Υπάλληλοι`**: πρόσθεσε 15 νέες επικεφαλίδες στο τέλος — `AMA | AMKA | Phone | Address | HireDate | TechnicalTraining | CvFileKey | CvFileName | CvFileType | IdFileKey | IdFileName | IdFileType | ResidencePermitFileKey | ResidencePermitFileName | ResidencePermitFileType` — για τα προσωπικά στοιχεία/συνημμένα τεχνικού (βλ. Ρόλοι § Team Leader → Προφίλ Τεχνικού). Χωρίς αυτές, οι τιμές γράφονται positionally στη λάθος/ανύπαρκτη θέση (`appendRow` δεν κοιτάζει headers) — σιωπηλά δεν θα εμφανίζονται σωστά στο Προφίλ Τεχνικού, καμία διακοπή αλλού.
- **Φύλλο `Στόλος`**: πρόσθεσε επικεφαλίδη `Deductible` στο τέλος (νέα στήλη, μετά το `ServiceDate`) — για το πεδίο «Απαλλαγή» (ποσό ασφάλισης σε €). Χωρίς αυτή, η τιμή γράφεται positionally στη λάθος θέση — σιωπηλά δεν θα εμφανίζεται σωστά, καμία διακοπή αλλού.
- **Φύλλο `Χρεώσεις`**: πρόσθεσε επικεφαλίδες `PickupMileage` και `PickupPhotos` στο τέλος (νέες στήλες, μετά το `VehiclePlate`) — για την «Παραλαβή οχήματος» (χιλιόμετρα + φωτογραφίες, βλ. Ρόλοι § Τεχνικός). Χωρίς αυτές, το tab "Καταγραφή παραλαβής" θα αποτυγχάνει με σφάλμα (το `updateCell` γράφει positionally στη λάθος/ανύπαρκτη στήλη ό,τι κι αν υπάρχει εκεί) — όχι σιωπηλά ανενεργό.
- **Νέο φύλλο `ΑτυχήματαΟχημάτων`**: δημιούργησέ το ΑΠΟ ΤΗΝ ΑΡΧΗ με headers `ID | VehicleID | VehiclePlate | EmployeeID | EmployeeName | Date | Comments | FileKey | FileName | FileType | ReportedBy | ReportedAt`. Χωρίς αυτό, η κάρτα "Ατυχήματα οχημάτων" στο tab Στόλος θα αποτυγχάνει εντελώς (θα δείχνει σφάλμα, όχι σιωπηλά ανενεργό). Το KV namespace `FLEET_FILES` για τις φωτογραφίες/συνημμένα είναι ήδη δηλωμένο στο `wrangler.toml` — ενεργοποιείται αυτόματα στο επόμενο deploy, δεν χρειάζεται κάτι επιπλέον.
- **Φύλλο `Χρεώσεις`**: πρόσθεσε επικεφαλίδη `ReturnMileage` στο τέλος (νέα στήλη, μετά το `PickupPhotos`) — για την «Επιστροφή οχήματος» (χιλιόμετρα, βλ. Ρόλοι § Τεχνικός). Χωρίς αυτή, το κουμπί "Καταγραφή επιστροφής" θα αποτυγχάνει με σφάλμα (το `updateCell` γράφει positionally στη λάθος/ανύπαρκτη στήλη) — όχι σιωπηλά ανενεργό.
- **Φύλλο `Στόλος`**: πρόσθεσε επικεφαλίδη `CurrentMileage` στο τέλος (νέα στήλη, μετά το `Deductible`) — για το πεδίο «Τρέχοντα χιλιόμετρα» (βλ. Ρόλοι § Team Leader → Στόλος Οχημάτων). Χωρίς αυτή, η τιμή γράφεται positionally στη λάθος θέση — σιωπηλά δεν θα εμφανίζεται σωστά (ούτε η αρχική καταχώρηση από τον TL ούτε η αυτόματη ενημέρωση από τις επιστροφές), καμία διακοπή αλλού.
- **Φύλλο `Υπάλληλοι`**: πρόσθεσε επικεφαλίδες `Status` και `InactiveDate` στο τέλος (νέες στήλες, μετά το `CompanyPhone`) — για το Ενεργός/Ανενεργός τεχνικός (13/08/2026, βλ. §Νέα ζητούμενα 13/08/2026 #5). Χωρίς αυτές, οι τιμές γράφονται positionally στη λάθος/ανύπαρκτη θέση — το login block/η απόκρυψη από δεξαμενές θα σιωπηλά ΔΕΝ θα δουλεύουν σωστά (θα διαβάζουν πάντα κενό = "Ενεργός"), καμία διακοπή αλλού.
- **Νέο φύλλο `ΕρωτηματαΔιαθεσιμότητας`**: δημιούργησέ το ΑΠΟ ΤΗΝ ΑΡΧΗ με headers `ID | Question | Deadline | TargetTeams | CreatedBy | CreatedByName | CreatedAt`. Χωρίς αυτό, το tab "Ανακοινώσεις" θα αποτυγχάνει εντελώς (θα δείχνει σφάλμα, όχι σιωπηλά ανενεργό).
- **Φύλλο `ΕρωτηματαΔιαθεσιμότητας`**: πρόσθεσε επικεφαλίδες `PublishAt` και `EmailSentAt` στο τέλος (νέες στήλες, 20/08/2026) — για την ετεροχρονισμένη δημοσίευση ερωτημάτων (βλ. Ρόλοι § Team Leader → Ανακοινώσεις). Χωρίς αυτές, η δημιουργία/επεξεργασία ερωτήματος θα αποτυγχάνει με σφάλμα (το `appendRow`/`updateRow` γράφει positionally στη λάθος/ανύπαρκτη στήλη ό,τι κι αν υπάρχει εκεί) — όχι σιωπηλά ανενεργό.
- **Νέο φύλλο `ΑπαντησειςΔιαθεσιμότητας`**: δημιούργησέ το ΑΠΟ ΤΗΝ ΑΡΧΗ με headers `ID | QueryID | EmployeeID | EmployeeName | Answer | AnsweredAt`. Χωρίς αυτό, η απάντηση Ναι/Όχι ενός τεχνικού θα αποτυγχάνει με σφάλμα (η δημιουργία/λίστα ερωτημάτων θα δουλεύει κανονικά, μόνο η καταχώρηση απάντησης πλήττεται).
- **Φύλλο `Χρεώσεις`**: πρόσθεσε επικεφαλίδες `PickupDate` και `ReturnDate` στο τέλος (νέες στήλες, μετά το `ReturnMileage`) — για το tab «Ημερολόγιο» του τεχνικού (βλ. Ρόλοι § Τεχνικός → Ημερολόγιο). Χωρίς αυτές, τα κουμπιά «Καταγραφή παραλαβής»/«Καταγραφή επιστροφής» θα αποτυγχάνουν με σφάλμα (το `updateCell` γράφει positionally στη λάθος/ανύπαρκτη στήλη) — όχι σιωπηλά ανενεργό.
- **Φύλλο `Εξοπλισμός`**: πρόσθεσε επικεφαλίδη `SerialNumber` στο τέλος (νέα στήλη, μετά το `UpdatedAt`) — για το πεδίο «Σειριακός/Κωδικός» (IMEI/σειριακός αριθμός, βλ. §Προς Συζήτηση #6). Χωρίς αυτή, η τιμή γράφεται positionally στη λάθος/ανύπαρκτη θέση — σιωπηλά δεν θα εμφανίζεται σωστά, καμία διακοπή αλλού.
- **Φύλλο `Χρεώσεις`**: πρόσθεσε επικεφαλίδες `OdometerPickupPhotoKey`, `OdometerPickupPhotoName`, `OdometerPickupPhotoType`, `OdometerReturnPhotoKey`, `OdometerReturnPhotoName`, `OdometerReturnPhotoType` στο τέλος (νέες στήλες, μετά το `ReturnDate`) — για την υποχρεωτική φωτογραφία κοντέρ στην Παραλαβή/Επιστροφή οχήματος (βλ. §Νέα ζητούμενα 13/08/2026 #1). Χωρίς αυτές, τα κουμπιά «Καταγραφή παραλαβής»/«Καταγραφή επιστροφής» θα αποτυγχάνουν με σφάλμα κατά το ανέβασμα της φωτογραφίας (το `updateCell` γράφει positionally στη λάθος/ανύπαρκτη στήλη) — όχι σιωπηλά ανενεργό, η καταγραφή χιλιομέτρων θα έχει ήδη πετύχει πριν σκάσει το upload.
- **Φύλλο `Στόλος`**: πρόσθεσε επικεφαλίδες `InsuranceCompany`, `PolicyNumber`, `InsuranceRenewalDate` στο τέλος (νέες στήλες, μετά το `CurrentMileage`) — για τα «Στοιχεία ασφάλισης οχήματος» (item #119). Χωρίς αυτές, οι τιμές γράφονται positionally στη λάθος θέση — σιωπηλά δεν θα εμφανίζονται σωστά, καμία διακοπή αλλού.
- **Νέο φύλλο `ΕγγραφαΠαράδοσης`**: δημιούργησέ το ΑΠΟ ΤΗΝ ΑΡΧΗ με headers `ID | EmployeeID | EmployeeName | HoldingsJSON | Status | CreatedBy | CreatedByName | CreatedAt | SignatureFileKey | SignatureFileName | SignatureFileType | SignedAt` — για την «Ψηφιακή υπογραφή στο έγγραφο παράδοσης/παραλαβής» (βλ. Ρόλοι § Team Leader → Εξοπλισμός). Χωρίς αυτό, η ενότητα "Έγγραφα παράδοσης (ψηφιακή υπογραφή)" θα αποτυγχάνει εντελώς (θα δείχνει σφάλμα, όχι σιωπηλά ανενεργό). Το KV namespace `HANDOVER_FILES` για τις εικόνες υπογραφής είναι ήδη δηλωμένο στο `wrangler.toml` — ενεργοποιείται αυτόματα στο επόμενο deploy, δεν χρειάζεται κάτι επιπλέον.
- **Φύλλο `Υπάλληλοι`**: πρόσθεσε επικεφαλίδη `AFM` στο τέλος (νέα στήλη, μετά το `InactiveDate`) — για το πεδίο «Α.Φ.Μ.» στα προσωπικά στοιχεία τεχνικού (19/08/2026, βλ. Ρόλοι § Team Leader → Προφίλ Τεχνικού). Χωρίς αυτή, η τιμή γράφεται positionally στη λάθος/ανύπαρκτη θέση — σιωπηλά δεν θα εμφανίζεται σωστά, καμία διακοπή αλλού.
- **Φύλλο `Ενημερώσεις`**: πρόσθεσε επικεφαλίδη `TargetTeams` στο τέλος (νέα στήλη, μετά το `PostedAt`) — για τη στοχευμένη δημοσίευση ανά ειδικότητα (20/08/2026, βλ. §Ενημερώσεις/Οδηγίες). Χωρίς αυτή, η δημιουργία ενημέρωσης θα αποτυγχάνει με σφάλμα (το `appendRow` γράφει positionally στη λάθος/ανύπαρκτη στήλη ό,τι κι αν υπάρχει εκεί) — όχι σιωπηλά ανενεργό.
- **Φύλλο `ΕρωτηματαΔιαθεσιμότητας`**: πρόσθεσε επικεφαλίδη `ResponseMode` στο τέλος (νέα στήλη, μετά το `EmailSentAt`) — για τον τύπο ανακοίνωσης Ναι/Όχι ή «Έλαβα γνώση» (21/08/2026, βλ. Ρόλοι § Team Leader → Ανακοινώσεις). Χωρίς αυτή, η δημιουργία/επεξεργασία ερωτήματος θα αποτυγχάνει με σφάλμα (το `appendRow`/`updateRow` γράφει positionally στη λάθος/ανύπαρκτη στήλη ό,τι κι αν υπάρχει εκεί) — όχι σιωπηλά ανενεργό.
- **Φύλλο `Υπάλληλοι`**: πρόσθεσε επικεφαλίδες `HuskiesUsername` και `HuskiesPassword` στο τέλος (νέες στήλες, μετά το `AFM`) — για τα κωδικοί Huskies (24/08/2026, βλ. §Huskies credentials παρακάτω). Χωρίς αυτές, οι τιμές γράφονται positionally στη λάθος/ανύπαρκτη θέση — σιωπηλά δεν θα εμφανίζονται/αποθηκεύονται σωστά, καμία διακοπή αλλού.
- **Φύλλο `Υπάλληλοι`**: πρόσθεσε επικεφαλίδη `PriorExperience` στο τέλος (νέα στήλη, μετά το `HuskiesPassword`) — για το πεδίο «Προϋπηρεσία» στο Προφίλ Τεχνικού (24/08/2026, ελεύθερο κείμενο, εμφανίζεται κάτω από την Απασχόληση). Χωρίς αυτή, η τιμή γράφεται positionally στη λάθος/ανύπαρκτη θέση — σιωπηλά δεν θα εμφανίζεται/αποθηκεύεται σωστά, καμία διακοπή αλλού.
- **Φύλλο `Υπάλληλοι`**: πρόσθεσε επικεφαλίδες `FatherName` και `IdNumber` στο τέλος (νέες στήλες, μετά το `PriorExperience`) — για «Πατρώνυμο»/«Αρ. Α.Δ.Τ.», μόνιμα προαιρετικά πεδία προφίλ που αυτοσυμπληρώνουν το tab «Έγγραφα» (31/08/2026, βλ. §Tab «Έγγραφα» παρακάτω). Χωρίς αυτές, οι τιμές γράφονται positionally στη λάθος/ανύπαρκτη θέση — σιωπηλά δεν θα εμφανίζονται/αποθηκεύονται σωστά, καμία διακοπή αλλού.
- **Νέο φύλλο `Έγγραφα`**: δημιούργησέ το ΑΠΟ ΤΗΝ ΑΡΧΗ με headers `ID | EmployeeID | EmployeeName | DocType | FieldsJSON | CreatedBy | CreatedByName | CreatedAt` — για το tab «Έγγραφα» (31/08/2026, βλ. §Tab «Έγγραφα» παρακάτω). Χωρίς αυτό, το tab θα αποτυγχάνει εντελώς (θα δείχνει σφάλμα, όχι σιωπηλά ανενεργό).
- **Φύλλο `Εξοπλισμός`**: πρόσθεσε επικεφαλίδες `Model` και `IMEI` στο τέλος (νέες στήλες, μετά το `SerialNumber`) — για τον διαχωρισμό Σειριακού Αριθμού/Μοντέλου/IMEI σε 3 ξεχωριστά πεδία (03/09/2026, βλ. §Εξοπλισμός: διαχωρισμός Σειριακού/Μοντέλου + IMEI παρακάτω). Χωρίς αυτές, οι τιμές γράφονται positionally στη λάθος/ανύπαρκτη θέση — σιωπηλά δεν θα εμφανίζονται/αποθηκεύονται σωστά, καμία διακοπή αλλού.
- **Φύλλο `Στόλος`**: πρόσθεσε επικεφαλίδη `ServiceEnteredAt` στο τέλος (νέα στήλη, μετά το `InsuranceRenewalDate`) — για τον μετρητή «Σε συνεργείο από... · X ημέρες» (04/09/2026, βλ. §Στόλος Οχημάτων: μετρητής ημερών «Σε συνεργείο» παραπάνω). Χωρίς αυτή, η τιμή γράφεται positionally στη λάθος/ανύπαρκτη θέση — το badge σιωπηλά δεν θα εμφανίζεται ποτέ (καμία διακοπή αλλού, όλα τα υπόλοιπα πεδία οχήματος συνεχίζουν να δουλεύουν κανονικά).
- **Νέο φύλλο `SIMs`**: δημιούργησέ το ΑΠΟ ΤΗΝ ΑΡΧΗ με headers `ID | CardNumber | PIN | PUK | Tablet | Note | UpdatedBy | UpdatedAt` — για το νέο tab «SIMs» (05/09/2026, βλ. §Νέο tab «SIMs» παρακάτω). Χωρίς αυτό, το tab θα αποτυγχάνει εντελώς (θα δείχνει σφάλμα, όχι σιωπηλά ανενεργό).
- Λεπτομέρειες και στο SETUP.md.

## Tab «Έγγραφα» — ΕΓΙΝΕ (31/08/2026)

Ρητή απαίτηση χρήστη: νέο tab «Έγγραφα» όπου μπαίνουν αυτοματοποιημένα
επίσημα έγγραφα προσωπικού (αξιολογήσεις, Αίτηση Άδειας Άνευ Αποδοχών, «ό,τι
άλλο χρειαστεί»). Τρόπος συμπλήρωσης: ο TL/Director επιλέγει τεχνικό, τα
στοιχεία που ήδη υπάρχουν στη βάση συμπληρώνονται αυτόματα, τα υπόλοιπα τα
συμπληρώνει ο χρήστης με το χέρι. Σχεδιάστηκε μέσω preview (widget mockup)
πριν την υλοποίηση, μετά διευκρινιστικές ερωτήσεις — ρητές αποφάσεις χρήστη:

1. **Πρόσβαση: ΜΟΝΟ TL + Director** (ίδιο gate `requireTeamLeader` με το tab
   Προφίλ Τεχνικού — Backoffice δεν έχει καν πρόσβαση, καμία αλλαγή στο
   `TOOLS.backoffice`).
2. **Αποθήκευση + ιστορικό ανά τεχνικό** (όχι απλά άμεσο PDF χωρίς ίχνος) —
   νέο φύλλο `Έγγραφα`, ίδιο snapshot/ιστορικό μοτίβο με `src/handover.js`
   αλλά ΧΩΡΙΣ ψηφιακή υπογραφή/κλείδωμα (βλ. #4 παρακάτω).
3. **Η ήδη υπάρχουσα Αξιολόγηση (evaluation.html) ΔΕΝ εντάχθηκε ακόμα** —
   ρητή απόφαση χρήστη "θα το δούμε αργότερα" — παραμένει σκόπιμα εκτός
   scope αυτού του περάσματος, το tab «Αξιολόγηση» (ανοίγει `evaluation.html`
   σε νέα καρτέλα, χωρίς προσυμπλήρωση) παραμένει ΑΜΕΤΑΒΛΗΤΟ όπως πριν.
4. **Καμία ψηφιακή υπογραφή** — η ενότητα «Απάντηση Εταιρείας» (έγκριση/
   σφραγίδα/υπογραφή εργαζομένου) μένει κενό πεδίο για ΦΥΣΙΚΗ υπογραφή/
   σφραγίδα στο τυπωμένο χαρτί, ρητή απόφαση χρήστη — ρητά διαφορετικό
   μηχανισμό από το `src/handover.js` (canvas signature), που λύνει άλλο
   πρόβλημα (απομακρυσμένη υπογραφή χωρίς φυσική παρουσία TL+τεχνικού μαζί).
   Συνεπώς το νέο `src/documents.js` δεν στέλνει κανένα email/ειδοποίηση —
   το έγγραφο είναι έτοιμο προς εκτύπωση/φυσική υπογραφή αμέσως μετά τη
   δημιουργία, καμία εκκρεμότητα από τη μεριά του τεχνικού.

**Αρχιτεκτονική (ρητή απαίτηση χρήστη — extensible, "ό,τι άλλο χρειαστεί να
προσθέσουμε"):** το backend (`src/documents.js`) είναι πλήρως agnostic ως
προς τον τύπο εγγράφου — αποθηκεύει απλά `{EmployeeID, DocType, FieldsJSON}`
(`DocType` = το registry key, π.χ. `"leave_without_pay"`), καμία επικύρωση
περιεχομένου ανά τύπο. `createDocument()`/`listDocumentsForEmployee()`/
`listAllDocuments()`/`getDocumentById()`, ίδιο μοτίβο toDocView/parseFields
με το `src/handover.js`. Όλη η γνώση ανά τύπο εγγράφου (ποια πεδία είναι
`auto` από το προφίλ τεχνικού vs `manual`, και το ίδιο το HTML/CSS προς
εκτύπωση) ζει ΜΟΝΟ στο frontend, στο νέο `DOCUMENT_TYPES` registry object
(`hub.html`) — για να προστεθεί νέος τύπος εγγράφου στο μέλλον αρκεί μία
νέα καταχώρηση εκεί (`{label, fields:[{key,label,auto(emp)},...], render(fields)}`),
καμία αλλαγή backend.

**Πρώτος τύπος: «Αίτηση Άδειας Άνευ Αποδοχών»** — ο χρήστης έδωσε το
ΑΚΡΙΒΕΣ πρότυπο HTML/CSS (δικό του υπάρχον χειροκίνητο έγγραφο, browser-
print με λογότυπο OptikiTec) και το `render()` το αναπαράγει σχεδόν
αυτούσιο (ίδιο layout/στυλ/κείμενο, ίδιο footer με στοιχεία εταιρείας) — με
τα placeholders `[...]` αντικατεστημένα από πραγματικές τιμές. Πεδία:
`fullName`/`fatherName`/`idNumber`/`cityArea` auto από το προφίλ τεχνικού
(`emp.Name`/`emp.FatherName`/`emp.IdNumber`/`emp.Address`) αν υπάρχουν ήδη
καταχωρημένα (νέα πεδία `FatherName`/`IdNumber`, βλ. §Χειροκίνητα βήματα) —
αλλιώς εμφανίζονται κενά προς συμπλήρωση, το UI δείχνει badge «αυτόματα»/
«συμπληρώνετε» ανά πεδίο. **Σκόπιμη απλοποίηση:** το πρότυπο έχει 2
ξεχωριστά κενά διεύθυνσης (`ΠΟΛΗ/ΠΕΡΙΟΧΗ` και `ΟΔΟΣ ΑΡ., Τ.Κ.`) αλλά το
προφίλ τεχνικού έχει ΜΟΝΟ ένα ενιαίο πεδίο `Address` — το `cityArea`
αντλεί ολόκληρη τη διεύθυνση από εκεί (auto αν υπάρχει), το `streetInfo`
μένει πάντα manual (δεν υπάρχει δομημένο δεύτερο πεδίο στη βάση). Τα
`startDate`/`endDate`/`reason` είναι πάντα manual — αφορούν τη
συγκεκριμένη αίτηση, όχι στοιχείο προφίλ. ΟΛΑ τα πεδία (και τα auto)
παραμένουν επεξεργάσιμα στη φόρμα πριν τη δημιουργία — απλή προεπισκόπηση/
προσυμπλήρωση, όχι κλείδωμα. Αφαιρέθηκε το ενσωματωμένο base64 λογότυπο
του αρχικού αρχείου του χρήστη (κρατήθηκε μόνο το κείμενο header/footer)
— σκόπιμη απλοποίηση για να μην πρηστεί το ήδη μεγάλο `hub.html` με
binary data, δεν ζητήθηκε ρητά να διατηρηθεί η εικόνα.

**UI (`hub.html`, tab `documents`, TL+Director sidebar «Ομάδα»):**
`renderDocuments()` — κάρτα «Νέο έγγραφο» (dropdown τύπου + dropdown
τεχνικού, ο τεχνικός φορτώνεται μέσω του ήδη υπάρχοντος `GET
/api/employees/profile?employeeId=...`, reuse 100%, καμία νέα κλήση για το
προφίλ) → πίνακας πεδίων με badge αυτόματα/συμπληρώνετε → «Δημιουργία
εγγράφου» αποθηκεύει (`POST /api/documents/create`) ΚΑΙ ανοίγει αμέσως το
παράθυρο εκτύπωσης (`printDocumentType()`, ίδιο μοτίβο `window.open` +
`document.write` + αυτόματο `win.print()` με `exportHandoverPdf`/
`exportTechnicianHoldingsPdf`). Κάρτα «Ιστορικό εγγράφων» — γενική λίστα
(`GET /api/documents/list` χωρίς `employeeId`, cap 200, πιο πρόσφατα
πρώτα), κάθε γραμμή έχει «Προβολή/Εκτύπωση» που ξαναφέρνει το έγγραφο
(`GET /api/documents/get?id=...`) και το ξανατυπώνει με τις ίδιες
αποθηκευμένες τιμές — καμία ανάγκη να ξαναγραφτούν τα στοιχεία.

**Backend endpoints** (`src/index.js`, ίδιο gate `requireTeamLeader` και
στα 3): `POST /api/documents/create`, `GET /api/documents/list`
(προαιρετικό `?employeeId=` — με αυτό φιλτράρει σε έναν τεχνικό, χωρίς αυτό
επιστρέφει το γενικό ιστορικό), `GET /api/documents/get?id=...`. Νέα
καταχώρηση `document_created` στο `ACTIVITY_ACTION_META` (widget
«Τελευταία δραστηριότητα» της Αρχικής, ορατό μόνο σε leader/director —
συνεπές με το ότι μόνο αυτοί έχουν πρόσβαση στο ίδιο το tab).

**Fix: πεδία φόρμας εμφανίζονταν λευκά/unstyled — ΕΓΙΝΕ** (05/09/2026,
ρητή αναφορά χρήστη με screenshot): τα `<input>` του πίνακα πεδίων (κάρτα
«Νέο έγγραφο», `refreshDocFieldsArea()`) δεν ήταν μέσα σε `.field` wrapper
div — το portal-wide στυλ πεδίων (`.field select, .field input, .field
textarea{...}`) είναι scoped ΜΟΝΟ σε απογόνους `.field`, άρα αυτά τα
inputs εμφανίζονταν με το προεπιλεγμένο στυλ του browser (λευκό φόντο,
μαύρο κείμενο) — ασυνεπές με το σκούρο θέμα του portal. Fix: νέα standalone
CSS κλάση `.doc-field-input` (ίδιο styling με `.field input`, ίδιο μοτίβο
με το ήδη υπάρχον standalone στυλ του `#tp-employee` select στο Προφίλ
Τεχνικού) αντικατέστησε το ανύπαρκτο `style="width:100%;"` στο input —
συν `html[data-theme="dark"] .doc-field-input::-webkit-calendar-picker-
indicator{filter:invert(1);}` (ίδιο μοτίβο με το ήδη υπάρχον αντίστοιχο
dark-mode fix του inline-edit ΚΤΕΟ/service input στο Στόλο) ώστε το
εικονίδιο ημερολογίου του πεδίου ημερομηνίας να παραμένει ορατό σε dark
mode. Καμία αλλαγή backend/δεδομένων.

**Προεπισκόπηση εγγράφου πριν τη δημιουργία — ΕΓΙΝΕ** (05/09/2026, ρητή
απαίτηση χρήστη): νέο κουμπί «Προεπισκόπηση» στην κάρτα «Νέο έγγραφο»,
τοποθετημένο ΠΡΙΝ το «Δημιουργία εγγράφου» (ρητή θέση χρήστη) — ενεργό/
ανενεργό μαζί με αυτό (ίδιος έλεγχος `docType && currentEmployee` μέσα στο
`refreshDocFieldsArea()`/το `change` handler του `doc-employee-select`).
Στο κλικ, μαζεύει τις τρέχουσες τιμές των `[data-doc-field]` inputs (ίδιο
`fields` collection με το `doc-create-btn` handler, αλλά ΧΩΡΙΣ έλεγχο
υποχρεωτικών πεδίων — η προεπισκόπηση επιτρέπεται ακόμα και με κενά πεδία,
απλά θα δείξει κενά εκεί) και καλεί απευθείας `docType.render(fields)` —
ΚΑΜΙΑ κλήση `POST /api/documents/create`, καμία αποθήκευση, καθαρά
client-side. Το αποτέλεσμα (πλήρες HTML προς εκτύπωση, ίδιο `render()` που
ήδη χρησιμοποιεί το `printDocumentType()`) μπαίνει σε νέο `<iframe
id="doc-preview-frame" sandbox="">` μέσω `srcdoc` — `sandbox=""` σκόπιμα
(άμυνα σε βάθος, το περιεχόμενο δεν έχει ΚΑΙ ΔΕΝ χρειάζεται scripts/forms)
μέσα σε νέο modal `#doc-preview-modal` (ίδια γενική μηχανή modal
`openModal()`/`data-modal-close`, καλείται όμως προγραμματιστικά αντί με
`data-modal-open` αφού πρέπει πρώτα να υπολογιστεί το `srcdoc`). Νέα CSS
κλάση `.modal-box-wide{max-width:900px;}` (το προεπιλεγμένο `.modal-box`
έχει `max-width:680px` — πολύ στενό για προεπισκόπηση εγγράφου Α4/800px
πλάτους). Η προεπισκόπηση είναι ανεξάρτητη ενέργεια από τη «Δημιουργία
εγγράφου» — ο χρήστης μπορεί να ανοίξει/κλείσει το modal όσες φορές θέλει
πριν αποφασίσει να δημιουργήσει/αποθηκεύσει το έγγραφο, χωρίς καμία
πλευρική επίδραση. Καμία αλλαγή backend/δεδομένων — καθαρά frontend,
επεκτείνεται αυτόματα σε ΚΑΘΕ μελλοντικό τύπο εγγράφου που προστεθεί στο
`DOCUMENT_TYPES` registry (καμία αλλαγή χρειάζεται εκεί, το preview
χρησιμοποιεί ήδη το γενικό `docType.render(fields)`).

## Δύο μικρές διορθώσεις UI — ΕΓΙΝΕ (02/09/2026)

Δύο ξεχωριστά, ρητά αιτήματα χρήστη στο ίδιο μήνυμα:

**1. Μαζική αποστολή καλωσορίσματος — αφαιρέθηκε από την Αρχική.** Η κάρτα
ήταν reused σε 2 σημεία (`bulkWelcomeCardHtml()`/`wireBulkWelcomeButtons()`
στο `hub.html`, ορατή μόνο σε `BULK_WELCOME_OWNER_EMAIL`) — tab «Νέος
Τεχνικός» ΚΑΙ tab «Αρχική» (προστέθηκε εκεί 19/08/2026). Ο χρήστης ζήτησε
αρχικά να αφαιρεθεί από το «Προφίλ Τεχνικού» — έλεγχος (Grep) έδειξε ότι
δεν υπήρχε ποτέ εκεί, μόνο στα 2 παραπάνω σημεία. Μετά από διευκρινιστική
ερώτηση, ρητή απόφαση χρήστη: **«Μόνο από την Αρχική»** — η κάρτα ΠΑΡΑΜΕΝΕΙ
στο «Νέος Τεχνικός» (φυσικό σημείο, αμέσως μετά τη δημιουργία τεχνικού),
αφαιρέθηκε ΜΟΝΟ το `${bulkWelcomeCardHtml()}`/`wireBulkWelcomeButtons();`
από το `renderDashboard()` — ώστε το Dashboard να μείνει καθαρά KPI
tiles + «Τελευταία δραστηριότητα». Καμία αλλαγή backend.

**2. «Αναζήτηση αδειών τεχνικού» δείχνει ΜΟΝΟ άδειες, ΟΧΙ τηλεργασία.**
Ρητό αίτημα χρήστη. Αρχικά δοκιμάστηκε ευρύτερη ερμηνεία (εξαίρεση
τηλεργασίας ΚΑΙ από τον κύριο πίνακα «Αιτήσεις άδειας» του tab «Άδειες
Ομάδας») — επισημάνθηκε στον χρήστη ότι αυτό θα άφηνε τον TL ΧΩΡΙΣ κανέναν
τρόπο να εγκρίνει/απορρίψει τηλεργασία μέσα από το portal (μόνο το
one-click email link θα απέμενε), αφού η έγκριση τηλεργασίας περνάει
αποκλειστικά από εκείνον τον πίνακα. Ρητή διευκρίνιση χρήστη μετά την
ερώτηση: η εξαίρεση αφορά **ΜΟΝΟ** την κάρτα «Αναζήτηση αδειών τεχνικού»
(`searchTechnicianLeaves()`/`#tls-results`) — ο πίνακας «Αιτήσεις άδειας»
(`renderLeaveRequestsTable()`, εκκρεμείς+αποφασισμένες, με ομαδοποίηση
τηλεργασίας ανά `GroupID`) παραμένει ΑΜΕΤΑΒΛΗΤΟΣ, η τηλεργασία συνεχίζει να
εγκρίνεται/απορρίπτεται κανονικά από εκεί.
Υλοποίηση: `searchTechnicianLeaves()` φιλτράρει `Type !== 'Τηλεργασία'`
client-side, αμέσως μετά το `fetch('/api/leaves/team?...')` (η ίδια
`searchTeamLeaves()` του backend συνεχίζει να επιστρέφει ΟΛΟΥΣ τους τύπους
— καμία αλλαγή backend, το φιλτράρισμα είναι καθαρά προβολής) — το
φιλτραρισμένο σύνολο (`results`) χρησιμοποιείται ΚΑΙ για τον πίνακα
αποτελεσμάτων ΚΑΙ για το `TLS_LAST_RESULTS` (άρα και το PDF export
`exportTechnicianLeavesPdf()` συμφωνεί αυτόματα, καμία ξεχωριστή αλλαγή
χρειάστηκε εκεί). Το μηνιαίο ημερολόγιο αδειών ομάδας (chips τηλεργασίας)
δεν αγγίχτηκε — άσχετο σημείο/σκοπός.

## Redesign κάρτας ταυτότητας Προφίλ Τεχνικού — ΕΓΙΝΕ (02/09/2026)

Ρητή αναφορά χρήστη: τα στοιχεία στο tab «Προφίλ Τεχνικού» εμφανίζονταν
«ανακατεμένα» — δύσκολα στην ανάγνωση. Πριν από οποιαδήποτε αλλαγή,
παρουσιάστηκαν 4 εναλλακτικές διατάξεις μέσω preview (widget mockups, με
εικονικά δεδομένα): (Α) στοιβαγμένες ενότητες με mini-header ανά ενότητα +
grid label-πάνω-από-τιμή, (Β) sidebar δύο στηλών (επικοινωνία αριστερά,
ταυτότητα/απασχόληση δεξιά), (Γ) καρτέλες εναλλαγής ανάμεσα στις 3
ενότητες, (Δ) ενιαίος πίνακας ετικέτα/τιμή στυλ φύλλου προσωπικού. Ρητή
επιλογή χρήστη: **Επιλογή Α**.

Υλοποιήθηκε ΜΟΝΟ η κάρτα ταυτότητας (`loadTechProfile()` στο `hub.html`) —
καθαρά αναδιάταξη UI, καμία αλλαγή backend/δεδομένων. Το παλιό
`.tp-identity-contact`/`.tp-contact-col` (3 flex στήλες, κάθε πεδίο ως
`<i>` εικονίδιο + «Ετικέτα: τιμή» inline) αντικαταστάθηκε από 3 (ή 4, με
την Τεχνική κατάρτιση) σαφώς διαχωρισμένες ενότητες μέσα στην ίδια κάρτα,
η καθεμία με δικό της μικρό uppercase section-label (εικονίδιο + τίτλος,
`.tp-section-label`) και divider από κάτω (`border-bottom`, εκτός της
τελευταίας) — αντί για το ένα μεγάλο κείμενο-blob πριν. Κάθε πεδίο μέσα σε
ενότητα δείχνεται πλέον ως ετικέτα ΠΑΝΩ από την τιμή (`tpField()` helper —
`.tp-field-label`/`.tp-field-value`, grid `auto-fit minmax(150px,1fr)`) αντί
για icon+colon-text σε μια γραμμή — πιο κοντά σε "φόρμα/spec sheet" παρά σε
πρόταση.

Ενότητες (`tpSection(icon, title, innerHtml)` helper, `hub.html`):
- **Επικοινωνία** (`ti-address-book`): Τηλέφωνο, Εταιρικό τηλέφωνο, Email,
  Διεύθυνση — ίδια 4 πεδία με πριν, ίδια σειρά προσαρμοσμένη ώστε το Email
  να μην κόβεται σε στενή στήλη (η παλιά διάταξη είχε τη Διεύθυνση
  conditional/optional στη μέση της στήλης, τώρα είναι σταθερό 4ο πεδίο με
  «—» fallback αν λείπει, ίδιο μοτίβο με τα υπόλοιπα).
- **Στοιχεία ταυτότητας** (`ti-id-badge-2`): ΑΜΑ, ΑΜΚΑ, Α.Φ.Μ., Πατρώνυμο,
  Αρ. Α.Δ.Τ. — ίδια 5 πεδία, ίδια σειρά με πριν.
- **Απασχόληση** (`ti-briefcase`): Ημ/νία πρόσληψης, Απασχόληση (με το ίδιο
  "(ως ανενεργός)" suffix όταν ισχύει), Προϋπηρεσία — ίδια 3 πεδία/λογική
  (`tpEmploymentDuration()` αμετάβλητη).
- **Τεχνική κατάρτιση** (`ti-certificate`, μόνο αν υπάρχει τιμή): ίδιο
  `tpTrainingBullets()` bullet list από πριν, τώρα μέσα στο ίδιο οπτικό
  σύστημα ενοτήτων (`tp-section-training` — παραλείπει το grid, δείχνει
  απευθείας τη λίστα) αντί για το παλιό ξεχωριστό `.tp-identity-training`/
  `.card-title-mini` στυλ.

Καμία αλλαγή στα δεδομένα/τιμές που εμφανίζονται, στο modal «Επεξεργασία
στοιχείων», στη μπάρα ισοζυγίου αδειών από κάτω, ή σε οποιοδήποτε άλλο tab
— μόνο η δομή/CSS αυτής της μίας κάρτας.

**Αναθεώρηση ίδια μέρα — «σε στήλες»:** μετά το αρχικό preview, ο χρήστης
ζήτησε να δει τις ίδιες 3 ενότητες (Επικοινωνία/Στοιχεία ταυτότητας/
Απασχόληση) σε εναλλακτική διάταξη — παρουσιάστηκε preview με τις ίδιες
ενότητες η μία δίπλα στην άλλη σε στήλες αντί για στοιβαγμένες, και ρητή
επιλογή χρήστη να προχωρήσει με αυτή. Νέο `tpSectionCol(icon, title,
fieldsHtml)` helper (`hub.html`) — ίδιο section-label header με το
`tpSection()`, αλλά τα πεδία στοιβάζονται κάθετα (`.tp-field-list`) μέσα σε
`.tp-section-col`, 3 στήλες δίπλα-δίπλα μέσα σε `.tp-section-columns`
(CSS grid 3 ίσων στηλών, κάθετοι διαχωριστές `border-left` ανάμεσά τους).
Η Τεχνική κατάρτιση παραμένει πλήρους πλάτους από κάτω (δεν χωράει καλά σε
στενή στήλη ως bullet list) — χρησιμοποιεί ακόμα το παλιό `tpSection()`.
Responsive: κάτω από 640px οι 3 στήλες σπάνε σε στοίβα (κάθετοι
διαχωριστές γίνονται οριζόντιοι), ίδιο breakpoint με το παλιό
`.tp-identity-contact` πριν το redesign. Το `tpSection()`/`.tp-field-grid`
(auto-fit grid) παραμένουν στον κώδικα για την Τεχνική κατάρτιση — δεν
αφαιρέθηκαν.

**Fix ίδια μέρα — Τεχνική κατάρτιση πάντα ορατή:** ο χρήστης ανέφερε "δεν
έχεις προσθέσει την κατάρτιση" — διευκρινίστηκε μέσω ερώτησης ότι επρόκειτο
για τον ήδη υπάρχοντα conditional (`${emp.TechnicalTraining ? tpSection(...) : ''}`,
ίδια συμπεριφορά με πριν το redesign — η ενότητα κρυβόταν εντελώς αν το
πεδίο ήταν κενό στο Sheet για τον συγκεκριμένο τεχνικό, δεν ήταν bug του
redesign) και ρητή απόφαση χρήστη: η ενότητα να δείχνεται ΠΑΝΤΑ, με «—» όταν
είναι κενή (το `tpTrainingBullets()` επιστρέφει ήδη '—' για κενή τιμή — απλά
αφαιρέθηκε το εξωτερικό `${emp.TechnicalTraining ? ... : ''}` wrapper). Η
CSS `.tp-section-training ul{font-size:...}` μετακινήθηκε στο γονικό
`.tp-section-training{font-size:12.5px; color:var(--text-dim); line-height:1.7;}`
ώστε το fallback κείμενο «—» (bare text, όχι μέσα σε `<ul>`) να κληρονομεί
το ίδιο στυλ με τα bullets αντί να εμφανίζεται σε default μέγεθος/χρώμα.

**Αναθεώρηση ίδια μέρα — «πιο επαγγελματικό» (Επιλογή 6) — ΕΓΙΝΕ (02/09/2026,
ρητή απαίτηση χρήστη, προηγήθηκαν 8 preview mockups):** ο χρήστης ρώτησε
υποθετικά «πώς θα φαινόταν πιο επαγγελματικό;» — παρουσιάστηκαν, με preview
πρώτα (widget mockups, καμία υλοποίηση πριν την επιλογή), 8 αριθμημένες
εναλλακτικές διατάξεις για την κάρτα ταυτότητας: (1) στοιβαγμένες ενότητες
[το αρχικό Επιλογή Α], (2) sidebar δύο στηλών, (3) καρτέλες εναλλαγής,
(4) ενιαίος πίνακας ετικέτα/τιμή, (5) σε στήλες [η τότε ζωντανή έκδοση],
(6) πιο επαγγελματικό (accent μπάρα + τετράγωνο avatar + monospace πεδία),
(7) navy banner header, (8) field tiles. Ενδιάμεσα ζητήθηκε και δεύτερη
γνώμη από το Lovable (`optiki-pro-portal` project) για σύγκριση — μπλοκαρίστηκε
δύο φορές (workspace χωρίς credits για `send_message`, και το preview URL
απαιτούσε lovable.dev login που δεν υπήρχε διαθέσιμο) — αναφέρθηκε στον
χρήστη, συνεχίσαμε με δικά μας mockups. Ρητή επιλογή χρήστη: **«6»**.

Υλοποιήθηκε στο `hub.html`, πάνω στην ήδη υπάρχουσα δομή σε στήλες (§παραπάνω)
— καμία αλλαγή backend/δεδομένων:
- Η κάρτα ταυτότητας τυλίγεται τώρα σε `.tp-identity-card` (χωρίς εσωτερικό
  `padding`, `overflow:hidden`) με μια λεπτή έγχρωμη μπάρα `.tp-identity-accent`
  (3px, `background:var(--accent)`) στην κορυφή — ίδιο "accent bar" μοτίβο
  με άλλα σημεία του portal, εδώ εφαρμοσμένο για πρώτη φορά σε κάρτα προφίλ.
  Το υπόλοιπο περιεχόμενο μπήκε σε εσωτερικό `.tp-identity-body` (το
  `padding:20px` που είχε πριν το `.card`).
- Το avatar έγινε τετράγωνο με στρογγυλεμένες γωνίες (`border-radius:10px`,
  56px, μεγαλύτερο από πριν) αντί για κυκλικό — οπτική διαφοροποίηση από
  τα μικρά κυκλικά `.app-avatar` badges αλλού στο header/sidebar.
- Το status badge (`tpStatusBadge()`) μετακόμισε δίπλα στο όνομα, στην ίδια
  γραμμή (flex row) αντί για floated στη δεξιά άκρη της κάρτας — το `Team`
  έγινε δικιά του υποτιτλική γραμμή, και το `EmployeeID` πήρε δική του
  γραμμή με νέα κλάση `.tp-emp-code.tp-mono` (μικρό, γκρι, monospace — σαν
  «κωδικός υπαλλήλου» badge).
- Νέα `.tp-mono` κλάση (`font-family:'SFMono-Regular',Consolas,'Liberation
  Mono',Menlo,monospace` — δεν υπήρχε προηγουμένως καμία monospace γραμματοσειρά
  πουθενά στο portal, το `hub.html` χρησιμοποιεί παντού `'Inter',sans-serif`)
  εφαρμόστηκε επιλεκτικά, μέσω νέου προαιρετικού 3ου ορίσματος `mono` στο
  `tpField(label, value, mono)`, ΜΟΝΟ σε πεδία-κωδικούς: Τηλέφωνο, Εταιρικό
  τηλέφωνο, ΑΜΑ, ΑΜΚΑ, Α.Φ.Μ., Αρ. Α.Δ.Τ. — ΟΧΙ σε Email/Διεύθυνση/Πατρώνυμο/
  ημερομηνίες/Απασχόληση/Προϋπηρεσία (ελεύθερο κείμενο/ημερομηνίες, δεν
  ωφελούνται από monospace).
- Το `.tp-section-columns` (3 στήλες) πήρε νέα κλάση `.tp-panel`
  (`background:var(--field-bg); border-radius:10px; padding:16px 0;
  border-bottom:none` — αφαιρεί το προηγούμενο κάτω divider αφού το panel
  background παίζει ήδη τον ρόλο του οπτικού διαχωρισμού) ώστε οι 3 στήλες
  να φαίνονται σαν ενιαίο tinted block μέσα στην κάρτα.
- Νέα `tpTrainingChips(text)` (`hub.html`) — ίδιο split-by-comma με το
  `tpTrainingBullets()`, αλλά κάθε στοιχείο γίνεται μικρό pill (`.tp-chip`:
  `background:var(--field-bg); color:var(--text-dim); border-radius:8px;
  padding:4px 10px;`) σε `flex-wrap` σειρά αντί για κάθετη λίστα με bullets
  — αντικατέστησε το `tpTrainingBullets()` στο μοναδικό call site
  (`tpSection('ti-certificate', 'Τεχνική κατάρτιση', ...)`). Ίδιο "δεν
  αφαιρούνται παλιά helpers" μοτίβο με το `tpSection()`/`.tp-field-grid`
  παραπάνω — το `tpTrainingBullets()` παραμένει στον κώδικα αχρησιμοποίητο.
- Τα κουμπιά (Επεξεργασία στοιχείων/Reset PIN/Ενεργοποίηση-Απενεργοποίηση),
  το μήνυμα κατάστασης, και η μπάρα ισοζυγίου αδειών παρέμειναν λειτουργικά
  αμετάβλητα — ίδια IDs, καμία αλλαγή JS wiring εκτός των παραπάνω helper
  function calls.

**Σειρά πεδίων + μετονομασία στη στήλη «Στοιχεία ταυτότητας» — ΕΓΙΝΕ
(02/09/2026, ρητή απαίτηση χρήστη):** στην κάρτα ταυτότητας του Προφίλ
Τεχνικού, το Πατρώνυμο και το Α.Δ.Τ. μετακινήθηκαν πρώτα στη στήλη
«Στοιχεία ταυτότητας» (πριν ΑΜΑ/ΑΜΚΑ/ΑΦΜ, όχι μετά όπως ήταν) — νέα σειρά:
Πατρώνυμο, Α.Δ.Τ./Διαβατήριο, ΑΜΑ, ΑΜΚΑ, Α.Φ.Μ. Το label «Αρ. Α.Δ.Τ.»
μετονομάστηκε σε «Α.Δ.Τ./Διαβατήριο» — **μόνο σε αυτό το σημείο εμφάνισης**
(`hub.html`, `loadTechProfile()`), ρητά περιορισμένο scope· τα υπόλοιπα 3
σημεία που ήδη χρησιμοποιούν το ίδιο label («Αρ. Α.Δ.Τ.» στη φόρμα «Νέος
Τεχνικός», στο modal «Επεξεργασία στοιχείων», και στο `DOCUMENT_TYPES`
registry/κείμενο του εγγράφου «Αίτηση Άδειας Άνευ Αποδοχών») ΔΕΝ αγγίχτηκαν
— δεν ζητήθηκε ρητά αλλαγή εκεί, αν χρειαστεί συνέπεια παντού να ζητηθεί.

**Αναίρεση της Επιλογής 6, ΑΥΘΗΜΕΡΟΝ (02/09/2026, ρητή απαίτηση χρήστη):**
αφού είδε την Επιλογή 6 live, ζητήθηκαν κι άλλες εναλλακτικές (παρουσιάστηκαν
4 ακόμα preview mockups: cover stripe banner, minimal compact, left-rail
accent, spec-sheet με ορατά κελιά) — καμία δεν άρεσε, ρητή απόφαση χρήστη
**"κανένα δεν μ'ενδιαφερει μπορουμε να επιστρέψουμε στο προηγουμενο που
είχαμε"**. Η κάρτα ταυτότητας επαναφέρθηκε ΑΚΡΙΒΩΣ στη μορφή του commit
`93243b9` («σε στήλες», Επιλογή Α με το fix «Τεχνική κατάρτιση πάντα
ορατή») — αφαιρέθηκαν: το `tp-identity-accent`/`tp-identity-body` wrapper,
το τετράγωνο avatar (επέστρεψε σε κυκλικό 52px), το inline status badge
δίπλα στο όνομα (επέστρεψε floated στη δεξιά άκρη), η μπάρα κωδικού
υπαλλήλου (`tp-emp-code`/`tp-mono`, επέστρεψε στην απλή γραμμή
«Ομάδα · EmployeeID»), το `tp-panel` tinted background στις 3 στήλες, το
monospace στυλ στα πεδία-κωδικούς, και τα chips της Τεχνικής κατάρτισης
(επέστρεψε σε `tpTrainingBullets()`). Η αναίρεση έγινε με `git show
93243b9:hub.html` πάνω στο τρέχον `hub.html` (καθαρή επαναφορά ενός
αρχείου, καμία χειροκίνητη επεξεργασία) — το `93243b9` άγγιζε ΜΟΝΟ το
`hub.html`, καμία επίδραση σε backend/Sheet. Οι νέοι helpers/CSS classes
της Επιλογής 6 (`tpTrainingChips()`, `.tp-identity-card`, `.tp-mono`, κλπ.)
αφαιρέθηκαν εντελώς μαζί με τον κώδικα που τους χρησιμοποιούσε — σε
αντίθεση με το συνήθες μοτίβο «δεν αφαιρούνται παλιά helpers» του portal,
εδώ ολόκληρο το πείραμα αναιρέθηκε ρητά, όχι απλά αντικαταστάθηκε ένα call
site. Η ζωντανή έκδοση της κάρτας ταυτότητας παραμένει η «σε στήλες»
(Επιλογή Α + fix ορατότητας κατάρτισης), όπως πριν την Επιλογή 6.
Responsive breakpoint (640px, στοίβαξη 3 στηλών) παρέμεινε αμετάβλητο —

**Μικρή μεμονωμένη προσαρμογή, ΑΥΘΗΜΕΡΟΝ (02/09/2026, ρητή απαίτηση
χρήστη):** μετά την πλήρη αναίρεση της Επιλογής 6 παραπάνω, ο χρήστης
ζήτησε ΜΟΝΟ ένα μικρό στοιχείο από αυτήν πίσω — το avatar της κάρτας
ταυτότητας να είναι τετράγωνο (rounded) αντί για κυκλικό. Προστέθηκε
`border-radius:10px` στο inline style του `.app-avatar` (52px, όπως ήταν
πριν) — καμία άλλη αλλαγή από την «σε στήλες» έκδοση (όχι η accent μπάρα,
όχι το inline status badge, όχι το monospace/τα chips — μόνο το σχήμα του
avatar).

## Στόλος Οχημάτων: αφαίρεση στήλης «Ασφάλιση» από τον πίνακα — ΕΓΙΝΕ (03/09/2026)

Ρητό αίτημα χρήστη: αφαίρεση της στήλης «Ασφάλιση» από τον πίνακα Οχημάτων
του tab Στόλος, για περισσότερο χώρο στις υπόλοιπες στήλες. Αφαιρέθηκε το
`<th>Ασφάλιση</th>` και το αντίστοιχο `<td>${fleetInsuranceCell(v)}</td>`
από την read-only γραμμή (`renderFleetList()`) — το ελεύθερο 10% πλάτους
αναδιανεμήθηκε στο `<colgroup>` κυρίως σε Ανατεθειμένος (9%→12%) και
Σημείωση (9%→12%, μεταβλητού μήκους ελεύθερο κείμενο, ίδιο σκεπτικό με το
προηγούμενο column-width fix §Fix: πίνακας Οχημάτων), και μικρότερα +1%
σε Πινακίδα/Τύπος/Κατάσταση/Χλμ. Το inline «Επεξεργασία» row
(`editFleetVehicle()`) έπρεπε να χάσει το ίδιο td (3 inputs: εταιρεία/
αριθμός συμβολαίου/ημερομηνία ανανέωσης) ώστε ο αριθμός στηλών να
συνεχίσει να ταιριάζει με το `<colgroup>` σε edit mode — αντίστοιχα
αφαιρέθηκαν οι 3 γραμμές ανάγνωσης `ed-insurance-*` από το
`saveFleetVehicle()` και τα 3 πεδία από το POST body.

**Συνέπεια (ρητά αποδεκτή, καθαρά αποτέλεσμα της αφαίρεσης στήλης, όχι
ξεχωριστή απόφαση):** το backend `updateVehicle()` (`src/fleet.js`)
κάνει ήδη merge με fallback στην υπάρχουσα τιμή όταν ένα πεδίο λείπει από
το request body (`updates.insuranceCompany !== undefined ? ... :
existing.InsuranceCompany`) — άρα οι ήδη καταχωρημένες τιμές ασφάλισης
ΔΕΝ σβήνονται όταν αποθηκεύεται μια αλλαγή σε άλλο πεδίο του οχήματος,
απλά δεν είναι πια επεξεργάσιμες μέσω του inline πίνακα. Η **φόρμα
«Προσθήκη οχήματος»** (πάνω από τον πίνακα, `fl-insurance-company`/
`fl-policy-number`/`fl-insurance-renewal`) ΔΕΝ αγγίχτηκε — παραμένει ο
μόνος τρόπος να καταχωρηθεί ασφάλιση, μόνο τη στιγμή δημιουργίας νέου
οχήματος. Αν χρειαστεί ξανά επεξεργασία μετά τη δημιουργία, θα χρειαστεί
είτε επαναφορά της στήλης είτε ξεχωριστό modal/mechanism — να ζητηθεί
ρητά. Η `fleetInsuranceCell()` και τα `InsuranceCompany`/`PolicyNumber`/
`InsuranceRenewalDate` πεδία στο Sheet παραμένουν αμετάβλητα — ίδιο «δεν
αφαιρούνται παλιά helpers/δεδομένα» μοτίβο με άλλα σημεία του portal, η
function είναι απλά αχρησιμοποίητη προς το παρόν. Οι εξαγωγές CSV/PDF
(`exportFleetCsv`/`exportFleetPdf`, `FLEET_EXPORT_HEADERS`) ΔΕΝ αγγίχτηκαν
— δεν ζητήθηκε, συνεχίζουν να περιλαμβάνουν τα 3 πεδία ασφάλισης.
δεν επηρεάστηκε από τις αλλαγές αυτού του περάσματος.

## Στόλος Οχημάτων: fix επικάλυψης στηλών στον πίνακα Οχημάτων — ΕΓΙΝΕ (03/09/2026)

Ρητή αναφορά χρήστη: στον πίνακα Οχημάτων του tab Στόλος, στήλες «έπεφταν η
μια πάνω στην άλλη» (οπτική επικάλυψη περιεχομένου). Διερεύνηση εντόπισε
δύο ανεξάρτητες ρίζες, όχι απλά λάθος ποσοστά πλάτους:

1. **`td.fl-actions{white-space:nowrap;}`** — τα 2 κουμπιά «Επεξεργασία»/
   «Διαγραφή» (με borders/padding) χρειάζονται μαζί ~170-190px, αλλά η
   στήλη Ενεργειών (colspan 2) είχε μόνο 16% πλάτους πίνακα — με `nowrap`
   ΔΕΝ επιτρεπόταν αναδίπλωση, οπότε σε στενότερα παράθυρα τα κουμπιά
   υπερχείλιζαν οπτικά προς τα αριστερά, πάνω στη στήλη «Χλμ»/«Απαλλαγή»
   δίπλα τους — αυτή είναι η πιο πιθανή ρίζα του «πέφτει η μια πάνω στην
   άλλη». 2. Με `table-layout:fixed`, περιεχόμενο ΧΩΡΙΣ φυσικά σημεία
   αναδίπλωσης (π.χ. αριθμός χιλιομέτρων «134580», μακρύ status-pill
   κείμενο «Εκτός λειτουργίας») δεν αναδιπλωνόταν αυτόματα σε στενή στήλη
   — υπερχειλίζει έξω από το κελί αντί να σπάσει σε 2η γραμμή, ανεξάρτητα
   από το πόσο σωστά υπολογίζονται τα ποσοστά πλάτους.

**Fix (3 μέρη, portal-wide όπου εφαρμόστηκαν CSS κλάσεις — όχι μόνο στο
Στόλο, ωφελεί αυτόματα και Εξοπλισμό/e-pass που μοιράζονται τις ίδιες
κλάσεις `fl-table`/`fl-actions`/`status-pill`):**
- `overflow-wrap:anywhere` προστέθηκε σε γενικά `td`/`th` (ΟΛΟΙ οι πίνακες
  του portal) ΚΑΙ στο `.status-pill` (με `max-width:100%`) — αναγκάζει
  αναδίπλωση ΚΑΙ μέσα σε "λέξεις" όταν δεν χωράει αλλιώς, αντί για
  υπερχείλιση έξω από το κελί.
- `td.fl-actions` έχασε το `white-space:nowrap` — τα 2 κουμπιά αναδιπλώνονται
  πλέον σε 2η γραμμή αν δεν χωράνε σε μία (νέο `margin-top:4px` στο
  `.icon-btn` για κενό ανάμεσα στις 2 γραμμές), ΠΟΤΕ πια δεν υπερχειλίζουν
  προς τα αριστερά πάνω σε άλλη στήλη.
- `table.fl-table{min-width:820px}` + `.card:has(table.fl-table){overflow-x:auto}`
  (πλέον ΓΙΑ ΟΛΑ τα πλάτη οθόνης, όχι μόνο mobile breakpoint όπως πριν) —
  αν η κάρτα είναι στενότερη από 820px, ο πίνακας κάνει οριζόντιο scroll
  αντί να συμπιέζει τις στήλες μέχρι να γίνουν αδιάβαστες/να υπερχειλίζουν.

**Rebalance `<colgroup>` πίνακα Οχημάτων** (πλάτη σε % πίνακα, αθροίζουν σε
100%): Πινακίδα 8, Τύπος 8, Ανατεθειμένος 11 (12→11), e-pass 6 (7→6),
Κατάσταση 8 (7→8, χρειάζεται περισσότερο χώρο για «Εκτός λειτουργίας»),
Σημείωση 10 (12→10), ΚΤΕΟ 9, Service 9, Απαλλαγή 6, Χλμ 7 (6→7, αριθμοί
χωρίς φυσικά σημεία αναδίπλωσης), Ενέργειες colspan2 18 (16→18, βλ. #1
παραπάνω — ο κύριος λόγος της επικάλυψης). Καμία αλλαγή στα Εξοπλισμός/
e-pass `<colgroup>` — δεν ζητήθηκε, τα CSS wrap-safety fixes παραπάνω
ωφελούν ήδη κι εκείνα χωρίς να χρειαστεί αλλαγή πλατών εκεί. Καμία αλλαγή
backend/δεδομένων.

**Επέκταση ίδια μέρα — κεντράρισμα όλων των στηλών δεδομένων:** ρητό αίτημα
χρήστη μετά το παραπάνω fix. Οι υπόλοιπες αριστερά-στοιχισμένες στήλες
(Πινακίδα, Τύπος, Ανατεθειμένος, Σημείωση — που στο fix του 25/08/2026
είχαν σκόπιμα μείνει αριστερά ως "μεταβλητού μήκους ελεύθερο κείμενο")
πήραν κι αυτές `class="ta-center"`, σε header ΚΑΙ read-only γραμμή
(`renderFleetList()`) ΚΑΙ inline «Επεξεργασία» (`editFleetVehicle()` — το
κελί e-pass εκεί δεν είχε καν `ta-center` πριν, ασυνέπεια με τη read-only
γραμμή, διορθώθηκε παράλληλα· τα κελιά Απαλλαγή/Χλμ σε edit mode πήραν
επίσης `ta-center` ώστε τα στενά `width:80px` number inputs να κεντράρονται
μέσα στο κελί αντί να κολλάνε αριστερά). Η στήλη Ενέργειες (colspan 2)
ΠΑΡΕΜΕΙΝΕ `ta-right` — σκόπιμη εξαίρεση, δεξιά στοίχιση κουμπιών ενεργειών
είναι established UI σύμβαση, δεν θεωρήθηκε "στήλη δεδομένων". Καθαρά
CSS/markup, καμία αλλαγή backend.

**Ίδιο κεντράρισμα και στο tab e-pass — ΕΓΙΝΕ, ίδια μέρα** (ρητό αίτημα
χρήστη "κάνε το ίδιο και για το tab e-pass"): header (`Ετικέτα`/`Τεχνικός`/
`Όχημα`/`Σημείωση`) και read-only γραμμή (`renderEpassList()`) πήραν
`class="ta-center"` — ίδιο μοτίβο, ίδια εξαίρεση για Ενέργειες (`ta-right`).
Το inline «Επεξεργασία» (`editEpassItem()`) δεν χρειάστηκε αλλαγή — όλα τα
πεδία εκεί είναι ήδη `td.fl-edit` με input/select `width:100%` (καμία
στενή fixed-width numeric είσοδος σαν το Απαλλαγή/Χλμ του Στόλου). Το
CSS wrap-safety fix (`overflow-wrap:anywhere`, `td.fl-actions` χωρίς
`nowrap`, `table.fl-table{min-width:820px}`) είναι ήδη global από το
προηγούμενο πέρασμα — ωφελεί αυτόματα και το e-pass.

**Rebalance `<colgroup>` e-pass — ΕΓΙΝΕ, ίδια μέρα** (ρητό αίτημα χρήστη
"ρύθμισε και το πλάτος για τις στήλες"): οι παλιές αναλογίες (Ετικέτα 16,
Τεχνικός 20, Όχημα 16, Κατάσταση 12, Σημείωση 18, Ενέργειες colspan2 18)
έδιναν άσκοπα πολύ χώρο σε στήλες με σύντομο/σταθερού μήκους περιεχόμενο
(Ετικέτα/κωδικός, πινακίδα οχήματος, badge κατάστασης "Ενεργό"/"Ανενεργό")
και σχετικά λίγο στη Σημείωση (ελεύθερο κείμενο, το πιο μεταβλητού μήκους
πεδίο) και στις Ενέργειες (2 κουμπιά, ίδιο ρίσκο με το προηγούμενο fix
Στόλου). Νέα αναλογία (αθροίζει σε 100%): Ετικέτα 12 (16→12), Τεχνικός 20
(αμετάβλητο, ονοματεπώνυμα χρειάζονται χώρο), Όχημα 10 (16→10, πινακίδα
σύντομη), Κατάσταση 10 (12→10), Σημείωση 28 (18→28), Ενέργειες colspan2 20
(18→20, 10+10). Καμία αλλαγή backend/δεδομένων.

## Fix: status-pill badge σπάει/επικαλύπτεται σε στενή στήλη (tab Εξοπλισμός) — ΕΓΙΝΕ (03/09/2026)

Ρητή αναφορά χρήστη με screenshot: στον πίνακα «Αντικείμενα» (tab
Εξοπλισμός), το badge Κατάστασης («Διαθέσιμο», «Σε χρήση») εμφανιζόταν
σπασμένο/παραμορφωμένο (π.χ. «Διαθέσμο» αντί για «Διαθέσιμο»). Ζητήθηκε
ρητά preview πριν από οποιαδήποτε αλλαγή — παρουσιάστηκε mockup «Πριν/Μετά»
(`mcp__visualize__show_widget`) πριν την υλοποίηση, εγκρίθηκε από τον
χρήστη.

**Ρίζα:** το προηγούμενο fix «στήλες πέφτουν η μια πάνω στην άλλη» (βλ.
παραπάνω, ίδια μέρα) πρόσθεσε `overflow-wrap:anywhere` στο `.status-pill`
ώστε μονολεκτικές τιμές (π.χ. «Διαθέσιμο», χωρίς κενά) να μην ξεχειλίζουν
έξω από στενή στήλη — αλλά αυτό επέτρεψε στο badge να σπάσει σε 2 γραμμές
ΜΕΣΑ στο πλήρες pill σχήμα (`border-radius:100px`), που δεν έχει αρκετό
ύψος/line-height για δύο γραμμές κειμένου — οι δύο γραμμές οπτικά
επικαλύπτονται. Στον πίνακα Αντικειμένων η στήλη Κατάσταση ήταν ιδιαίτερα
στενή (8%), οπότε το πρόβλημα ήταν πιο συχνό/ορατό εκεί — αλλά η ίδια ρίζα
υπήρχε δυνητικά και στο Στόλο/e-pass (μοιράζονται το ίδιο `.status-pill`).

**Fix (global CSS, `.status-pill`):** αντικαταστάθηκε το `overflow-wrap:
anywhere` με `white-space:nowrap` (το badge ΔΕΝ σπάει ΠΟΤΕ πια σε 2ή γραμμή)
και το `border-radius` άλλαξε από 100px (πλήρες pill) σε 7px (rounded rect)
— πιο κατάλληλο σχήμα για badge μονής γραμμής σε οποιοδήποτε πλάτος
στήλης, χωρίς να χρειάζεται να «μαντέψουμε» το ακριβές πλάτος για κάθε
πιθανή τιμή κατάστασης. Ισχύει ΠΑΝΤΟΥ όπου εμφανίζεται `.status-pill` στο
portal (Στόλος/Εξοπλισμός/e-pass/Άδειες/κλπ) — καθαρά θετική, συνεπής
αλλαγή, καμία αρνητική επίπτωση αλλού.

**Rebalance `<colgroup>` πίνακα Αντικειμένων** (πλάτη σε % πίνακα,
αθροίζουν σε 100%): Όνομα 12 (16→12), Κατηγορία 8 (10→8), Σειριακός 9
(10→9), Μοντέλο 9 (10→9), IMEI 9 (10→9), Ανατεθειμένος 10 (13→10),
Κατάσταση 14 (8→14, ώστε να χωράει «Εκτός λειτουργίας» σε μία γραμμή χωρίς
να ξεχειλίζει), Σημείωση 9 (11→9), Ενέργειες colspan2 20 (12→20, 10+10 —
ίδιο σκεπτικό με το προηγούμενο fix Στόλου, τα 2 κουμπιά χρειάζονται χώρο).
Καμία αλλαγή backend/δεδομένων.

## Στόλος/Εξοπλισμός/e-pass: κουμπιά Ενεργειών σε icon-only — ΕΓΙΝΕ (03/09/2026)

Ρητό αίτημα χρήστη, με preview πριν την υλοποίηση (`mcp__visualize`, mockup
«πριν/μετά» με προτεινόμενη εκδοχή εικονίδιο+tooltip) — εγκρίθηκε ("ναι
προχώρα"). Τα κουμπιά "Επεξεργασία"/"Διαγραφή" (read-only γραμμή) και
"Αποθήκευση"/"Άκυρο" (inline επεξεργασία γραμμής) στη στήλη Ενέργειες και
των 3 πινάκων `fl-table` (Στόλος, Εξοπλισμός, e-pass — 6 ζευγάρια/12
κουμπιά συνολικά) έγιναν icon-only (Tabler: `ti-edit`/`ti-trash`/
`ti-check`/`ti-x`) με `title`/`aria-label` για προσβασιμότητα, αντί για
κείμενο — ίδιο σκεπτικό με τα ήδη υπάρχοντα icon-only κουμπιά πλοήγησης
ημερολογίου (`cal-prev`/`cal-next`).

Νέα CSS κλάση `.icon-btn-sq` (τετράγωνο 30x30px, flex-centered, χωρίς
κείμενο) εφαρμόστηκε ΜΟΝΟ σε αυτά τα 12 κουμπιά — **σκόπιμα ΟΧΙ αλλαγή του
γενικού `.icon-btn`**, που εξακολουθεί να χρησιμοποιείται με κείμενο σε
πολλά άλλα σημεία του portal (π.χ. "Διαγραφή" σε αιτήσεις άδειας/
ανακοινώσεις, "Προβολή/Εκτύπωση" σε έγγραφα παράδοσης/`Έγγραφα`,
"Καταγραφή παραλαβής/επιστροφής" — αυτά ΔΕΝ αγγίχτηκαν, εκτός scope αυτού
του αιτήματος που αφορούσε ρητά τους 3 fl-table πίνακες). Το χρώμα των
danger (Διαγραφή) icon-btn παραμένει hover-only κόκκινο (`.icon-btn.danger
:hover`), ίδια established συμπεριφορά με πριν — δεν άλλαξε σε πάντα-
κόκκινο, ώστε να μην αποκλίνει από τη σύμβαση των υπόλοιπων danger
κουμπιών του portal.

**Rebalance `<colgroup>` και στους 3 πίνακες** (η στήλη Ενέργειες, πλέον
πολύ πιο στενή αφού τα κουμπιά δεν έχουν κείμενο, συρρικνώθηκε από
18-20% σε 12% συνολικά — το ελευθερωμένο πλάτος αναδιανεμήθηκε στις
στήλες με μεταβλητού μήκους περιεχόμενο):
- **Στόλος**: Ενέργειες 18→12 (6+6). +6 αναδιανεμήθηκαν: Πινακίδα 8→9,
  Ανατεθειμένος 11→13, Σημείωση 10→13.
- **Εξοπλισμός**: Ενέργειες 20→12 (6+6). +8 αναδιανεμήθηκαν: Όνομα 12→14,
  Σειριακός 9→11, Μοντέλο 9→11, Σημείωση 9→11.
- **e-pass**: Ενέργειες 20→12 (6+6). +8 αναδιανεμήθηκαν: Ετικέτα 12→14,
  Τεχνικός 20→22, Σημείωση 28→32.

Όλα τα ποσοστά επαληθεύτηκαν προγραμματιστικά ότι αθροίζουν σε ακριβώς
100% ανά πίνακα. Καμία αλλαγή backend/δεδομένων — καθαρά CSS/markup.

## Εξοπλισμός: διαχωρισμός Σειριακού/Μοντέλου + προσθήκη IMEI — ΕΓΙΝΕ (03/09/2026)

Ρητό αίτημα χρήστη: στο tab «Εξοπλισμός» (φόρμα «Προσθήκη αντικειμένου» +
πίνακας), το ενιαίο πεδίο «Σειριακός/Κωδικός» (με δυναμικό label ανάλογα
την Κατηγορία — «IMEI» για Κινητό, «Σειριακός αριθμός» για Tablet, γενικό
αλλού, βλ. §Προς Συζήτηση #6, 15/08/2026) αντικαταστάθηκε από **3 ρητά
ξεχωριστά, πάντα-ορατά πεδία**: Σειριακός Αριθμός, Μοντέλο, IMEI — και τα
3 προαιρετικά, κοινά για όλες τις κατηγορίες (όχι μόνο κινητά/tablet).
**Αναθεωρεί** την απόφαση #6 — εκεί το ρητό σκεπτικό ήταν "ΕΝΑ γενικό
πεδίο, όχι ξεχωριστή στήλη ανά κατηγορία, ώστε να μη χρειάζεται αλλαγή
σχήματος όταν προστεθεί νέα κατηγορία" — ο χρήστης τώρα προτίμησε την
απλή/ρητή έκδοση με 3 σταθερά πεδία αντί για το έξυπνο dynamic-label
μηχανισμό.

**Backend** (`src/equipment.js`): νέες στήλες `Model`/`IMEI` στο τέλος του
`EQUIPMENT_HEADER_ORDER` (μετά το ήδη υπάρχον `SerialNumber`) — χειροκίνητο
βήμα στο Sheet, βλ. §Χειροκίνητα βήματα. `addEquipment()`/`updateEquipment()`
δέχονται τα νέα `model`/`imei` παραμέτρους, ίδιο optional/merge μοτίβο με
το `serialNumber` (`updates.model !== undefined ? ... : existing.Model`).
`src/index.js` → `handleEquipmentAdd`/`handleEquipmentUpdate` περνάνε τα 2
νέα πεδία από το request body στις παραπάνω συναρτήσεις, καμία άλλη αλλαγή
(ίδιο gate `requireTeamLeader`).

**Frontend** (`hub.html`): η φόρμα «Προσθήκη αντικειμένου» έχει πλέον 3
ξεχωριστά inputs (`eq-serial`/`eq-model`/`eq-imei`) αντί για το ένα με
dynamic label. Ο πίνακας Αντικειμένων πήρε 2 νέες στήλες «Μοντέλο»/«IMEI»
δίπλα στο ήδη υπάρχον «Σειριακός» (`<colgroup>` αναδιαμορφώθηκε από 6+2 σε
8+2 στήλες, βλ. αναλογίες στο commit) — το inline «Επεξεργασία» row
(`editEquipmentItem()`) και το `saveEquipmentItem()` πήραν τα αντίστοιχα
2 νέα πεδία/inputs. Οι `eqSerialLabel()`/`updateEqSerialLabel()` (το παλιό
dynamic-label μηχανισμό, μαζί με τα event listeners στο category-select
που τις καλούσαν στη φόρμα προσθήκης ΚΑΙ στο inline edit) **αφαιρέθηκαν
εντελώς** — σκόπιμη εξαίρεση από το συνήθες «δεν αφαιρούνται παλιά
helpers» μοτίβο του portal (βλ. π.χ. `tpSection()`/`tpTrainingBullets()`),
επειδή εδώ δεν πρόκειται για εναλλακτική μορφή εμφάνισης των ίδιων
δεδομένων (που θα μπορούσε να χρειαστεί ξανά) αλλά για πλήρη αλλαγή
μοντέλου πεδίων — δεν υπάρχει πλέον κανένα σενάριο όπου θα ξαναχρειαστεί
το dynamic label. `EQUIPMENT_EXPORT_HEADERS`/`equipmentExportRows()`
(CSV+PDF exports) επεκτάθηκαν με τα 2 νέα πεδία, ίδια θέση με τον πίνακα
(μετά το Σειριακός, πριν το Ανατεθειμένος).

## Εξοπλισμός: κεντράρισμα στηλών (Κατηγορία/Σειριακός/Μοντέλο/IMEI) — ΕΓΙΝΕ (03/09/2026)

Ρητό αίτημα χρήστη ("κεντράρισε τις στήλες να φαίνεται πιο όμορφο"), με preview
πριν την υλοποίηση (`mcp__visualize`, mockup «πριν/μετά») — εγκρίθηκε ("ναι
προχώρα"). Στον πίνακα Αντικειμένων του tab Εξοπλισμός, οι στήλες Κατηγορία/
Σειριακός/Μοντέλο/IMEI (header `<th>` ΚΑΙ read-only γραμμή, `renderEquipmentList()`)
πήραν `class="ta-center"` — ίδιο σκεπτικό με το ήδη κεντραρισμένο Κατάσταση.
Το Όνομα (μεταβλητού μήκους ελεύθερο κείμενο) και το Ανατεθειμένος/Σημείωση
ΠΑΡΕΜΕΙΝΑΝ αριστερά στοιχισμένα — ίδια σύμβαση με το Πινακίδα/Ανατεθειμένος/
Σημείωση του Στόλου (fix 03/09/2026 παραπάνω): μεταβλητού μήκους ονόματα/
ελεύθερο κείμενο δεν κεντράρονται, μόνο κωδικοί/badges/σύντομες τιμές. Η
inline «Επεξεργασία» γραμμή (`editEquipmentItem()`) ΔΕΝ χρειάστηκε αλλαγή —
τα `td.fl-edit` κελιά εκεί περιέχουν ήδη `input`/`select` πλήρους πλάτους
(`width:100%`, block-level), οπότε το `text-align` του γονικού `td` δεν έχει
οπτικό αποτέλεσμα (ίδιο σκεπτικό με τα αντίστοιχα μη-αριθμητικά πεδία του
Στόλου, που επίσης δεν χρειάστηκαν `ta-center`). Καμία αλλαγή backend/
δεδομένων — καθαρά CSS/markup.

## Εξοπλισμός: μείωση πλάτους Σημείωση + κεντράρισμα Ανατεθειμένος — ΕΓΙΝΕ (03/09/2026)

Ρητό αίτημα χρήστη με screenshot: η στήλη Σημείωση ήταν άσκοπα φαρδιά ενώ
στην πράξη είναι σχεδόν πάντα κενή («—»), και ζητήθηκε επίσης κεντράρισμα
του Ανατεθειμένος (που στο προηγούμενο πέρασμα είχε σκόπιμα μείνει αριστερά
μαζί με το Όνομα, ως μεταβλητού μήκους ελεύθερο κείμενο — εδώ ο χρήστης
προτίμησε ρητά το κεντράρισμα). `<colgroup>`: Σημείωση 11→6, το
ελευθερωμένο +5 αναδιανεμήθηκε σε Όνομα 14→15, Σειριακός 11→12, Μοντέλο
11→12, IMEI 9→10, Ανατεθειμένος 10→11 (άθροισμα παραμένει 100%). Το Όνομα
παρέμεινε το μοναδικό αριστερά στοιχισμένο πεδίο πλέον (μεταβλητού μήκους
ελεύθερο κείμενο) — όλα τα υπόλοιπα δεδομένα (Κατηγορία/Σειριακός/Μοντέλο/
IMEI/Ανατεθειμένος/Κατάσταση) είναι πλέον κεντραρισμένα, μόνο η Σημείωση
έμεινε αριστερά (ελεύθερο κείμενο, αν και σπάνια συμπληρωμένο). Καμία
αλλαγή backend/δεδομένων.

## Εξοπλισμός: χειροκίνητη ρύθμιση πλατών στηλών από τον χρήστη — ΕΓΙΝΕ (03/09/2026)

Ο χρήστης ζήτησε συγκεκριμένα πλάτη στηλών, ρητά με τα δικά του νούμερα
(αντί για δικές μου προτάσεις). Πρώτη απόπειρα αθροίζε σε 92% — το
επεσήμανα με preview πριν αλλάξω οτιδήποτε, ο χρήστης έδωσε τη διαφορά
(Σειριακός +2, IMEI +2, Σημείωση +4) ως προσθήκη πάνω στα αρχικά νούμερα.
Τελικό `<colgroup>` (αθροίζει ακριβώς σε 100%): Όνομα 10, Κατηγορία 8,
Σειριακός 14, Μοντέλο 10, IMEI 14, Ανατεθειμένος 10, Κατάσταση 12,
Σημείωση 10, Ενέργειες 12 (6+6). Καμία αλλαγή backend/δεδομένων.

## Εξοπλισμός: κεντράρισμα Σημείωση (χωρίς αλλαγή Κατάσταση/Ενέργειες) — ΕΓΙΝΕ (03/09/2026)

Ρητό αίτημα χρήστη: κεντράρισμα της στήλης Σημείωση στον πίνακα Αντικειμένων,
ρητά χωρίς να αγγιχτούν οι στήλες Κατάσταση/Ενέργειες (που ήταν ήδη
κεντραρισμένη/δεξιά αντίστοιχα). Προστέθηκε `class="ta-center"` μόνο στο
`<th>Σημείωση</th>` και στο αντίστοιχο `<td>` (`renderEquipmentList()`) —
καμία άλλη στήλη/κλάση άλλαξε. Καμία αλλαγή backend/δεδομένων.

## Fix: Στόλος — στήλη e-pass επικαλύπτεται με τη διπλανή Κατάσταση — ΕΓΙΝΕ (03/09/2026)

Ρητή αναφορά χρήστη με screenshot: το badge της στήλης e-pass (μακρύς
αριθμητικός κωδικός κάρτας, π.χ. "0032614034267") εμφανιζόταν οπτικά
ανακατεμένο/επικαλυπτόμενο με το badge «Ενεργό» της διπλανής στήλης
Κατάσταση.

**Ρίζα:** συνέπεια του προηγούμενου fix για το bug "Διαθέσμο" (βλ. §Fix:
status-pill badge σπάει/επικαλύπτεται παραπάνω) — το `.status-pill` πήρε
τότε `white-space:nowrap` ώστε να ΜΗΝ σπάει ποτέ σε 2 γραμμές, αλλά αυτό
από μόνο του δεν εμποδίζει ένα πολύ μακρύ κείμενο (σε αντίθεση με μια
μονολεκτική κατάσταση) να υπερχειλίσει ΟΡΙΖΟΝΤΙΑ έξω από το στενό του `td`
(η στήλη e-pass είναι μόλις 6% πλάτος) — το badge απλώς προεξείχε πάνω
στη διπλανή στήλη Κατάσταση, της οποίας το δικό της badge («Ενεργό»)
ζωγραφιζόταν στη σωστή του θέση αλλά οπτικά ανακατεμένο με το προεξέχον
e-pass badge.

**Fix (global, `.status-pill`):** προστέθηκε `overflow:hidden;
text-overflow:ellipsis;` — οποιοδήποτε badge δεν χωράει πια στο κελί του
κόβεται καθαρά με «...» αντί να υπερχειλίζει στη διπλανή στήλη. Ισχύει
παντού όπου εμφανίζεται `.status-pill` (Στόλος/Εξοπλισμός/e-pass/Άδειες
κλπ) — καθαρά θετική, προληπτική αλλαγή, καμία αρνητική επίπτωση σε ήδη
σωστά badges (η περικοπή ενεργοποιείται ΜΟΝΟ όταν το περιεχόμενο
πραγματικά δεν χωράει). Το e-pass badge πήρε επίσης `title="..."` με το
πλήρες κείμενο (tooltip σε hover, ώστε ο πλήρης κωδικός να παραμένει
προσβάσιμος παρά την περικοπή) — και `escapeHtml()` στο `v.EpassLabel`
(έλειπε πριν, ίδιο pattern με το code review ασφαλείας του 25/08/2026,
`EpassLabel` είναι ελεύθερο κείμενο από το φύλλο `EPass`).

## Στόλος: Πινακίδα αριστερά + ΚΤΕΟ/Service/Απαλλαγή σε μέγεθος περιεχομένου — ΕΓΙΝΕ (03/09/2026)

Ρητό αίτημα χρήστη: (1) η στήλη Πινακίδα να «κολλήσει τέρμα αριστερά» —
αφαιρέθηκε το `ta-center` από το header ΚΑΙ την read-only γραμμή
(`renderFleetList()`), επιστρέφει σε αριστερή στοίχιση (η στήλη έχει πάντα
σταθερή μορφή «ΧΧΧ 1234», δεν χρειάζεται πλέον φαρδιά στήλη — μειώθηκε
7% από 9%). (2) ΚΤΕΟ/Service είναι ημερομηνίες, «όσο πλάτος χρειάζεται» —
αυξήθηκαν 9%→11% το καθένα (το `fleetDateBadge()` μπορεί να δείχνει κάτι
παραπάνω από ψιλή ημερομηνία, π.χ. «Έληξε · 24/08/2026» ή «24/08/2026
(5ημ.)» για ειδοποιήσεις λήξης — χρειάζεται λίγο παραπάνω χώρο από μια
απλή ημερομηνία). (3) Απαλλαγή είναι πάντα 3ψήφιο ποσό (π.χ. "€500"), άρα
μειώθηκε δραστικά 6%→4%. Το ελευθερωμένο πλάτος από Πινακίδα/Απαλλαγή
(-2/-2) πήγε ακριβώς στα ΚΤΕΟ/Service (+2/+2) — άθροισμα παραμένει 100%.

Για να μην κοπεί ποτέ το κείμενο ΚΤΕΟ/Service badge στην πράξη (ακόμα κι
αν κάποιο συνδυασμό ημερομηνίας/ημερών ξεπερνά το νέο πλάτος και
ενεργοποιηθεί το ήδη υπάρχον `overflow:hidden`/ellipsis fix του
`.status-pill`), το `fleetDateBadge()` πήρε επίσης `title="..."` με το
πλήρες κείμενο — tooltip σε hover, ίδιο μοτίβο με το e-pass badge fix
παραπάνω. Παράλληλα προστέθηκε `escapeHtml()` στο `v.Plate` (read-only
γραμμή ΚΑΙ inline edit input `value=`) — έλειπε πριν, ίδιο pattern με το
code review ασφαλείας του 25/08/2026 (η πινακίδα είναι ελεύθερο κείμενο
από το φύλλο `Στόλος`, ένα `"` θα έσπαγε το attribute στο edit input).
Καμία αλλαγή backend/δεδομένων.

## Στόλος: χειροκίνητη ρύθμιση πλατών στηλών (03/09/2026)

Ο χρήστης έδωσε συγκεκριμένα πλάτη για τον πίνακα Οχημάτων. Πρώτη απόπειρα
αθροίζε σε 101% — το επεσήμανα πριν αλλάξω οτιδήποτε, ο χρήστης ζήτησε να
αφαιρεθεί το 1% από τον Τύπο. Τελικό `<colgroup>` (αθροίζει ακριβώς σε
100%): Πινακίδα 7, Τύπος 6, Ανατεθειμένος 10, e-pass 10, Κατάσταση 8,
Σημείωση 13, ΚΤΕΟ 10, Service 10, Απαλλαγή 7, Χλμ 7, Ενέργειες 12 (6+6).
Καμία αλλαγή backend/δεδομένων.

## Στόλος Οχημάτων: αφαίρεση στήλης «Service» από τον πίνακα — ΕΓΙΝΕ (03/09/2026)

Ρητό αίτημα χρήστη: αφαίρεση της στήλης «Service» από τον πίνακα Οχημάτων
του tab Στόλος (ίδιο μοτίβο με την προηγούμενη αφαίρεση της «Ασφάλιση» —
UI-only, το πεδίο/η λειτουργικότητα ΔΕΝ αγγίζεται). Αφαιρέθηκαν: το
`<th>Service</th>`, το αντίστοιχο `<td>${fleetDateBadge(v.ServiceDate)}</td>`
από την read-only γραμμή, το `<td class="fl-edit">` με το `input#ed-service`
από την inline «Επεξεργασία» γραμμή, και το αντίστοιχο `<col>` από το
`<colgroup>`. Το ελευθερωμένο πλάτος (10%) μοιράστηκε αναλογικά στις
υπόλοιπες 9 στήλες δεδομένων (η στήλη Ενέργειες έμεινε ΣΚΟΠΙΜΑ σταθερή στο
12% — περιέχει 2 σταθερού μεγέθους εικονίδια, δεν ωφελείται από παραπάνω
χώρο): Πινακίδα 7→8, Τύπος 6→7, Ανατεθειμένος 10→11, e-pass 10→11,
Κατάσταση 8→9, Σημείωση 13→15, ΚΤΕΟ 10→11, Απαλλαγή 7→8, Χλμ 7→8 (άθροισμα
παραμένει ακριβώς 100%).

**Συνέπεια (ρητά αποδεκτή, καθαρά αποτέλεσμα της αφαίρεσης στήλης, όχι
ξεχωριστή απόφαση):** το backend `updateVehicle()` (`src/fleet.js`)
κάνει ήδη merge με fallback στην υπάρχουσα τιμή όταν ένα πεδίο λείπει από
το request body (`updates.serviceDate !== undefined ? ... :
existing.ServiceDate`) — άρα οι ήδη καταχωρημένες ημερομηνίες Service
ΔΕΝ σβήνονται όταν αποθηκεύεται μια αλλαγή σε άλλο πεδίο του οχήματος,
απλά δεν είναι πια επεξεργάσιμες μέσω του inline πίνακα.
`saveFleetVehicle()` (`hub.html`) ενημερώθηκε να μην διαβάζει/στέλνει πια
`serviceDate`. Η **φόρμα «Προσθήκη οχήματος»** (πάνω από τον πίνακα) ΔΕΝ
αγγίχτηκε — παραμένει ο μόνος τρόπος να καταχωρηθεί/αλλάξει ημερομηνία
Service, μόνο τη στιγμή δημιουργίας νέου οχήματος (ή, αν χρειαστεί μετά,
ζητήστε ρητά επαναφορά στήλης ή ξεχωριστό μηχανισμό). Οι ήδη υπάρχουσες
ειδοποιήσεις λήξης Service (tile Αρχικής, εβδομαδιαίο email συμμόρφωσης
προς Director, badge tab Στόλου) συνεχίζουν να δουλεύουν κανονικά —
διαβάζουν το `ServiceDate` απευθείας από το API, ανεξάρτητα από το αν
εμφανίζεται ή όχι στον πίνακα. Οι εξαγωγές CSV/PDF (`exportFleetCsv`/
`exportFleetPdf`, `FLEET_EXPORT_HEADERS`) ΔΕΝ αγγίχτηκαν — δεν ζητήθηκε,
συνεχίζουν να περιλαμβάνουν το Service.

## Στόλος Οχημάτων: redesign σε κάρτες — ΕΓΙΝΕ (04/09/2026)

Ρητό αίτημα χρήστη, με preview πριν την υλοποίηση (`mcp__visualize`, mockup
2 καρτών οχημάτων στα δικά μας δεδομένα/CSS vars, εγκρίθηκε "ναι προχώρα"
+ 3 διευκρινιστικές ερωτήσεις): ο χρήστης ανέβασε ένα εξαγόμενο HTML ενός
ξεχωριστού Dyad-generated React/Tailwind/shadcn demo app (fleet management
για συνεργεία, ΔΕΝ είναι το OptikiTec portal — δικό του tab "Στόλος
Οχημάτων"/"Ιστορικό Συμβάντων", δικά του πλασματικά δεδομένα/πεδία όπως
"Fuel: DIESEL"/"Συνεργείο Συγκολλήσεων A1") και ζήτησε να χτιστεί το ΔΙΚΟ
ΜΑΣ tab Στόλος Οχημάτων με ΑΝΤΙΣΤΟΙΧΗ σχεδιαστική γλώσσα — κάρτες αντί για
πίνακα, όχι 1:1 αντιγραφή περιεχομένου/πεδίων (το demo έχει πεδία που δεν
υπάρχουν στο portal, π.χ. καύσιμο/έτος μοντέλου/ιστορικό συμβάντων ως tab).

**Το ίδιο το ανεβασμένο αρχείο ήταν τεχνικά δύσκολο να διαβαστεί** — 3966
γραμμές, με το πραγματικό rendered DOM συμπιεσμένο σε ΜΙΑ γραμμή (~97.500
χαρακτήρες) στο τέλος του αρχείου, μετά από ~3960 γραμμές καθαρού Tailwind
CSS boilerplate (μόνο οι πρώτες 757 γραμμές CSS ήταν ήδη ~111.000 tokens).
Χρειάστηκε στοχευμένη εξαγωγή μέσω `sed`/Python (όχι απευθείας `Read`) για
να εντοπιστεί το πραγματικό markup γύρω από το κείμενο "Στόλος Οχημάτων".

**Αλλαγή (`hub.html`, tab Στόλος — ΜΟΝΟ αυτό το tab, Εξοπλισμός/e-pass
ΔΕΝ αγγίχτηκαν, δεν ζητήθηκε):** ο πίνακας `table.fl-table` (με
`<colgroup>`/`<thead>`/inline-edit γραμμές) αντικαταστάθηκε εξ ολοκλήρου
από `<div class="fleet-grid" id="fl-list">` — CSS grid καρτών
(`repeat(auto-fit, minmax(300px, 1fr))`), μία κάρτα ανά όχημα
(`renderFleetList()`, πλήρως ξαναγραμμένη): πινακίδα σε μονοσπειρωτό chip
(`.fleet-plate`) + status badge (reuse `fleetStatusClass()`/`.status-pill`,
ΑΜΕΤΑΒΛΗΤΟ) δίπλα-δίπλα πάνω, Τύπος από κάτω ως υπότιτλος, εικονίδιο
φορτηγού σε τετράγωνο badge δεξιά· κουτί πληροφοριών (`.fleet-info-box`)
με Ανατεθειμένο (icon+label/τιμή) και, στο κάτω μέρος του, e-pass badge
(reuse ίδιο `EpassLabel`/`status-pill st-ok`) + ΚΤΕΟ badge (reuse
`fleetDateBadge()`, ΑΜΕΤΑΒΛΗΤΟ) σε μία γραμμή· Σημείωση + Απαλλαγή σε μία
γραμμή μεταξύ τους· σκούρο κουτί `.fleet-mileage-box` (σταθερό `var(--navy)`
background — ίδιο σκούρο navy και στα δύο θέματα, ΔΕΝ ακολουθεί
`[data-theme="dark"]`, ίδιο σκεπτικό dark-contrast-box με το reference
design) με τα Τρέχοντα Χιλιόμετρα σε μεγάλο μέγεθος (νέα
`fleetMileageDisplay()` — μορφοποίηση με ελληνικές χιλιάδες
`toLocaleString('el-GR')`, π.χ. "142.350", καθαρά εμφάνιση, το backend/Sheet
κρατάει ακέραιη ωμή τιμή όπως πριν)· footer με τα ήδη υπάρχοντα icon-only
Επεξεργασία/Διαγραφή κουμπιά (`.icon-btn-sq`, ΑΜΕΤΑΒΛΗΤΑ από το πέρασμα
03/09/2026). Το search box πάνω από τη λίστα (`#fl-search`,
`filterFleetList()`) ΔΕΝ άλλαξε καθόλου — φιλτράρει το ίδιο
`FLEET_ALL_VEHICLES` και ξανακαλεί το ίδιο `renderFleetList()`, που τώρα
απλά παράγει κάρτες αντί για γραμμές πίνακα.

**Edit mode — ΑΝΑΘΕΩΡΕΙ το παλιό inline-row edit (ρητή επιλογή χρήστη μέσω
ερώτησης, "Modal φόρμα"):** το inline edit-in-place του πίνακα (κλικ
Επεξεργασία → inputs μέσα στη γραμμή, `editFleetVehicle()`/
`saveFleetVehicle()`) δεν έχει νόημα σε κάρτες — αντικαταστάθηκε από
**ενιαίο modal** που εξυπηρετεί ΚΑΙ Προσθήκη ΚΑΙ Επεξεργασία (το ήδη
υπάρχον `#fl-add-modal`, που είχε ΟΛΑ τα πεδία οχήματος). Νέο module-scope
`FLEET_EDIT_ID` (null = λειτουργία Προσθήκη, αλλιώς το ID του οχήματος υπό
επεξεργασία). Νέα `openFleetAddModal()` (καλείται από το κουμπί «Νέο
όχημα», που έχασε το `data-modal-open` attribute — τώρα event listener,
ώστε να καθαρίζει πρώτα τη φόρμα) μηδενίζει `FLEET_EDIT_ID`, καθαρίζει ΟΛΑ
τα πεδία, βάζει τίτλο/κουμπί σε «Προσθήκη οχήματος», ανοίγει το modal. Η
`editFleetVehicle(vehicleId, vehicles, ...)` πλέον προσυμπληρώνει ΟΛΑ τα
πεδία του ΙΔΙΟΥ modal (πινακίδα/τύπο-ή-«άλλος τύπος»/ανατεθειμένο/
κατάσταση/ΚΤΕΟ/service/απαλλαγή/χιλιόμετρα/ασφάλιση×3/σημείωση), θέτει
`FLEET_EDIT_ID = vehicleId`, αλλάζει τίτλο/κουμπί σε «Επεξεργασία
οχήματος»/«Αποθήκευση αλλαγών», ανοίγει το modal. Νέα ενιαία
`submitFleetVehicleForm()` (αντικαθιστά τις παλιές `addFleetVehicle()`/
`saveFleetVehicle()`) μαζεύει τα ίδια πεδία και διακλαδίζεται σε
`POST /api/fleet/add` (χωρίς `FLEET_EDIT_ID`) ή `POST /api/fleet/update`
(με αυτό, στέλνοντας `vehicleId`) — reuse του ΙΔΙΟΥ σώματος αιτήματος και
για τα δύο endpoints (`updates.xxx !== undefined` merge-fallback στο
`updateVehicle()`, `src/fleet.js`, ήδη υποστηρίζει όλα τα πεδία, καμία
αλλαγή backend χρειάστηκε). Κλείνει το modal (`closeModal('fl-add-modal')`)
πριν το `renderFleet()` re-render.

**Θετική παρενέργεια (όχι ρητά ζητημένη, φυσικό αποτέλεσμα της ενοποίησης):**
τα πεδία **Service** και **Ασφάλιση** (εταιρεία/αριθμός συμβολαίου/
ανανέωση) — που είχαν αφαιρεθεί από το παλιό inline-row edit σε
προηγούμενα περάσματα (βλ. §Στόλος Οχημάτων: αφαίρεση στήλης «Ασφάλιση»/
«Service» παραπάνω, όπου τεκμηριωνόταν ρητά ότι "αν χρειαστεί ξανά
επεξεργασία μετά τη δημιουργία, θα χρειαστεί... ξεχωριστό modal") ξαναγίνονται
επεξεργάσιμα ΜΕΤΑ τη δημιουργία οχήματος, αφού το ενιαίο modal περιέχει
ήδη όλα τα πεδία της παλιάς φόρμας «Προσθήκη οχήματος». Λύνει το
τεκμηριωμένο κενό και για τα δύο πεδία ταυτόχρονα, χωρίς επιπλέον δουλειά.

**CSS** (νέες κλάσεις μετά το `.icon-btn-sq` block): `.fleet-grid`,
`.fleet-card` (+ hover elevation, reuse `var(--shadow-2)`/`var(--accent-line)`),
`.fleet-card-body`, `.fleet-card-top`, `.fleet-plate-row`, `.fleet-plate`
(μονοσπειρωτό `'Manrope'` chip, ίδιο font-family με το sidebar logo),
`.fleet-card-type`, `.fleet-card-icon`, `.fleet-info-box`/`.fleet-info-row`/
`.fleet-info-label`/`.fleet-info-value`/`.fleet-info-foot`, `.fleet-meta-row`,
`.fleet-mileage-box`/`.fleet-mileage-label`/`.fleet-mileage-value`/
`.fleet-mileage-unit`, `.fleet-card-footer` — όλα χτισμένα ΜΟΝΟ πάνω σε
ήδη υπάρχουσες portal CSS vars (`--card`, `--field-bg`, `--line`, `--text`,
`--text-dim`, `--text-faint`, `--accent`, `--navy`, `--shadow-2`), καμία
νέα hardcoded τιμή χρώματος — ήδη dark-mode-safe χωρίς πρόσθετο override.
Οι παλιές κλάσεις `td.fl-edit`/`table.fl-table`/`.card:has(table.fl-table)`
ΔΕΝ αφαιρέθηκαν — παραμένουν σε χρήση από Εξοπλισμό/e-pass (ίδιο "δεν
αφαιρούνται παλιά helpers αν εξακολουθούν να χρησιμοποιούνται αλλού"
μοτίβο του portal).

**Καμία αλλαγή backend/Sheet/δεδομένων** — καθαρά UI: το `/api/fleet`
response, το `updateVehicle()`/`addVehicle()` (`src/fleet.js`), οι
εξαγωγές CSV/PDF (`exportFleetCsv`/`exportFleetPdf`), το κάτω μέρος του
tab (κάρτα «Ατυχήματα οχημάτων», ΑΜΕΤΑΒΛΗΤΗ), και όλα τα σημεία που ήδη
διαβάζουν `KteoDate`/`ServiceDate` (tile Αρχικής, weekly compliance email,
badge nav) συνεχίζουν να δουλεύουν κανονικά — διαβάζουν τα ίδια δεδομένα
από το ίδιο API, ανεξάρτητα από το πώς εμφανίζονται στο tab.

## Στόλος Οχημάτων: πιο απαλά χρώματα στα badges ΚΤΕΟ/Service — ΕΓΙΝΕ (05/09/2026)

Ρητή αναφορά χρήστη ("άλλαξε το χρώμα να μην είναι τόσο έντονο", διευκρίνισε
μέσω ερώτησης ότι εννοεί τα badges ημερομηνιών ΚΤΕΟ/Service στην κάρτα
οχήματος, ΟΧΙ το status badge πάνω δεξιά ή το σκούρο κουτί χιλιομέτρων).
Νέος κανόνας `.fleet-date-item .status-pill{filter:saturate(0.55);
font-weight:600;}` — μειώνει τον κορεσμό χρώματος (πράσινο/πορτοκαλί/κόκκινο
ανάλογα το `fleetDateAlertLevel()`) ΜΟΝΟ μέσα στα δύο πεδία ΚΤΕΟ/Service της
κάρτας, σκόπιμα scoped selector ώστε να ΜΗΝ επηρεάσει το γενικό
`.status-pill.st-ok/st-warn/st-bad` που χρησιμοποιείται αμετάβλητο παντού
αλλού στο portal (Άδειες, Εξοπλισμός, e-pass, το ίδιο το status badge πάνω
στην κάρτα οχήματος). Το σημασιολογικό χρώμα (πράσινο=εντάξει/πορτοκαλί=
λήγει σύντομα/κόκκινο=έληξε) παραμένει αναγνωρίσιμο, απλά λιγότερο vivid —
`filter:saturate()` αντί για αλλαγή σε νέα hardcoded χρώματα, ώστε να
λειτουργεί αυτόματα σωστά και στα δύο θέματα (light/dark) χωρίς ξεχωριστό
dark-mode override.

**Αναθεώρηση ΙΔΙΑ μέρα (05/09/2026, ρητή απαίτηση χρήστη):** το «λιγότερο
έντονο badge» παραπάνω δεν έφτανε — ρητό αίτημα χρήστη «αφαιρεσέ τα τέλειως
τα badges, 5 μέρες πιο νωρίς μόνο να φωτίζεται»: αφαίρεση του badge/pill
φόντου εντελώς από τα πεδία ΚΤΕΟ/Service της κάρτας, με απλό κείμενο ως
προεπιλογή — «φωτίζεται» (χρωματίζεται/τονίζεται) ΜΟΝΟ όταν απομένουν 5
ημέρες ή λιγότερες, ή έχει ήδη λήξει. Νέα `fleetDateInline(dateStr)`
(hub.html, αντικαθιστά το `fleetDateBadge()` ΜΟΝΟ στα δύο call sites μέσα
στην κάρτα οχήματος) — επιστρέφει σκέτο κείμενο ημερομηνίας
(`.fleet-date-plain`, χωρίς φόντο/border-radius) όταν απομένουν >5 ημέρες,
και το ίδιο κείμενο με χρωματισμό (`.fleet-date-warn` πορτοκαλί ή
`.fleet-date-bad` κόκκινο/bold) όταν το διάστημα είναι ≤5 ημέρες ή αρνητικό.
**Σκόπιμα ανεξάρτητο κατώφλι (5 ημέρες) από το γενικό `fleetDateAlertLevel()`
(30 ημέρες)** — το τελευταίο ΔΕΝ αγγίχτηκε, παραμένει όπως ήταν για το tile
"ΚΤΕΟ/Service λήγουν ή έληξαν" της Αρχικής (`renderDashboard()`) και το
αντίστοιχο badge υπολογισμό αλλού (γραμμές `alertCount` στο hub.html) — μια
αλλαγή στο κοινό 30-ημερών κατώφλι θα άλλαζε σιωπηλά και τον αριθμό
συναγερμών της Αρχικής, που δεν ζητήθηκε. Το `.fleet-date-item .status-pill`
κανόνας του προηγούμενου περάσματος αφαιρέθηκε (δεν εφαρμόζεται πια κανένα
`.status-pill` μέσα σε `.fleet-date-item`) και αντικαταστάθηκε από
`.fleet-date-item .fleet-date-plain{...}` + `.fleet-date-warn`/`.fleet-date-bad`
modifiers. Η `fleetDateBadge()` παραμένει στον κώδικα αμετάβλητη — ακόμα
χρησιμοποιείται από την ήδη αχρησιμοποίητη `fleetInsuranceCell()` (η στήλη
Ασφάλιση αφαιρέθηκε από το UI σε προηγούμενο πέρασμα, βλ. §Στόλος Οχημάτων:
αφαίρεση στήλης «Ασφάλιση» — dead code, ίδιο «δεν αφαιρούνται παλιά
helpers» μοτίβο του portal).

## Στόλος Οχημάτων: fix — κάρτες οχημάτων "τεντώνονταν" με λίγα αποτελέσματα — ΕΓΙΝΕ (05/09/2026)

Ρητή αναφορά χρήστη με screenshot: μετά από αναζήτηση με 1 μόνο αποτέλεσμα,
η κάρτα οχήματος εμφανιζόταν πολύ πιο φαρδιά/τεντωμένη απ' ό,τι στη
γενική λίστα με πολλά οχήματα. **Ρίζα:** το `.fleet-grid` (`repeat(auto-fit,
minmax(300px, 1fr))`) χρησιμοποιούσε `1fr` ως άνω όριο ανά track — όταν
υπάρχουν λίγα στοιχεία, το/τα κελιά μοιράζονται ολόκληρο το διαθέσιμο
πλάτος του container (χαρακτηριστική συμπεριφορά `1fr` σε CSS grid), οπότε
μια κάρτα με 1 αποτέλεσμα γέμιζε όλη τη σειρά αντί να κρατήσει το ίδιο
μέγεθος με τις υπόλοιπες. **Fix:** αντικαταστάθηκε το `1fr` με σταθερό άνω
όριο `340px` (`minmax(300px, 340px)`, `auto-fill` αντί για `auto-fit` — δεν
έχει σημασία εδώ αφού δεν υπάρχουν πια κενά tracks να καταρρεύσουν) — το
πλάτος κάθε κάρτας παραμένει πλέον σταθερό (300-340px) ανεξαρτήτως αριθμού
αποτελεσμάτων, το όποιο ελεύθερο πλάτος της σειράς μένει απλά κενό στα
δεξιά αντί να τεντώνει τις κάρτες. Καθαρά CSS, καμία αλλαγή backend/
δεδομένων.

**Αναθεώρηση ίδια μέρα (05/09/2026, ρητή αναφορά χρήστη με screenshot):** με
25 οχήματα (πολλά αποτελέσματα, πλήρεις σειρές των 4) το σταθερό άνω όριο
340px άφηνε αισθητό κενό περιθώριο δεξιά στην κάρτα «Οχήματα» — το πλάτος
της κάρτας-container δεν ήταν ακριβές πολλαπλάσιο του (340px + gap). Ρητό
αίτημα χρήστη: οι στήλες να καλύπτουν όλο το πλάτος της καρτέλας. Fix:
`minmax(300px, 340px)` → `minmax(300px, 1fr)` (επιστροφή σε `1fr`), αλλά
ΔΙΑΤΗΡΩΝΤΑΣ το `auto-fill` (δεν επανήλθε σε `auto-fit`) — το `auto-fill`
από μόνο του λύνει ήδη το πρόβλημα «τεντώνεται με λίγα αποτελέσματα» (δεν
καταρρέουν τα κενά tracks, ένα μοναχικό αποτέλεσμα παίρνει μόνο το δικό
του track, όχι όλη τη σειρά) — το σταθερό `340px` του προηγούμενου
περάσματος ήταν στην πραγματικότητα περιττό για αυτό το πρόβλημα, απλά
δημιουργούσε το νέο κενό-δεξιά σύμπτωμα. Με `1fr` οι κάρτες απλώνονται να
καλύψουν όλο το πλάτος όταν υπάρχουν αρκετά αποτελέσματα ανά σειρά, ΧΩΡΙΣ
να επανεμφανιστεί το αρχικό πρόβλημα (η συμπεριφορά με λίγα αποτελέσματα
εξαρτάται αποκλειστικά από `auto-fill` vs `auto-fit`, όχι από το άνω όριο
του `minmax()`). Καθαρά CSS, καμία αλλαγή backend/δεδομένων.

## Στόλος Οχημάτων: 4 συνοπτικές κάρτες πάνω από τη λίστα — ΕΓΙΝΕ (05/09/2026)

Ρητό αίτημα χρήστη με screenshot (ίδιο Dyad-generated demo app με το redesign
σε κάρτες του 04/09/2026, δική του σελίδα "PendingIncidentsBanner"/stat
cards): προσθήκη 4 συνοπτικών καρτών KPI πάνω από τη γενική κάρτα «Οχήματα
(Ν)» στο tab Στόλος. Καθαρά client-side υπολογισμός (νέα `fleetSummaryCardsHtml
(vehicles)`, hub.html, καλείται μέσα στο `renderFleet()` με τα ήδη φορτωμένα
`data.vehicles`) — καμία νέα κλήση backend/endpoint.

Τρεις από τις 4 κάρτες αντλούνται άμεσα από ήδη υπάρχοντα πεδία, καμία
διευκρίνιση χρειάστηκε: **Σύνολο Στόλου** (συνολικός αριθμός οχημάτων +
τα 2 πιο συχνά `Type` με το πλήθος τους, π.χ. "3 Van / 1 4x4"), **Σε
Λειτουργία** (πλήθος οχημάτων με `Status === "Ενεργό"` + υποδειγμή "Χ σε
ανάθεση" = πόσα από αυτά έχουν `AssignedTo`), **Συνολικά Χιλιόμετρα**
(άθροισμα `CurrentMileage` όλων των οχημάτων + Μ.Ο. ανά όχημα,
μορφοποιημένα με ελληνικές χιλιάδες `toLocaleString('el-GR')`).

Η 4η κάρτα, **«Εκκρεμή Συμβάντα»**, δεν είχε αντίστοιχο πεδίο — το φύλλο
`ΑτυχήματαΟχημάτων` δεν έχει καμία έννοια «εκκρεμές/κλειστό» σήμερα (απλή
λίστα καταγραφών, βλ. `reportVehicleAccident()`/`listVehicleAccidents()`
στο `src/fleet.js`). Ρωτήθηκε ο χρήστης με 4 επιλογές (οχήματα «Σε
service» / σύνολο καταγεγραμμένων ατυχημάτων / νέο πεδίο «Επιλύθηκε» στο
Sheet / παράλειψη της κάρτας) — **ρητή επιλογή χρήστη: οχήματα σε
κατάσταση «Σε service»** (reuse του ήδη υπάρχοντος πεδίου, καμία αλλαγή
Sheet/backend). Η κάρτα δείχνει το πλήθος αυτών των οχημάτων με κόκκινο
χρωματισμό (τίτλος/τιμή/dot, ίδιο σημασιολογικό κόκκινο με το
`.status-pill.st-bad` αλλού στο portal) και υποδειγμή "Υπό παρακολούθηση
(σε service)" όταν υπάρχει έστω ένα, αλλιώς "Κανένα όχημα σε service".

**CSS** (νέο block μετά το `.fleet-service-flag`/dark-mode override):
`.fleet-stats-grid` (CSS grid, `repeat(auto-fit, minmax(200px, 1fr))`, 4
στήλες που σπάνε σε στοίβα σε στενή οθόνη — ίδιο responsive μοτίβο με το
`.fleet-grid` των καρτών οχημάτων), `.fleet-stat-card` (ίδιο styling με
`.card`, δικό του `border` χρώμα ανά variant), `.fleet-stat-top`/`-title`/
`-icon`/`-value`/`-unit`/`-sub`/`-dot` + variant classes `.fleet-stat-blue`/
`-green`/`-red`/`-neutral` (χρώματα από τα ήδη υπάρχοντα `--accent`/
`--green`/`--red` CSS vars, dark-mode-safe χωρίς νέο override εκτός από το
κόκκινο icon background που ήδη ακολουθεί το established `rgba(...)` dark
override μοτίβο του `.fleet-service-flag`). Καμία νέα hardcoded τιμή
χρώματος εκτός των ήδη υπαρχόντων vars.

Τοποθέτηση: αμέσως πριν την κάρτα «Οχήματα (Ν)» (η γενική λίστα/αναζήτηση/
grid οχημάτων), μέσα στο ίδιο `renderFleet()` — ρητή θέση χρήστη ("πριν
την γενική καρτέλα Οχήματα"). Ενημερώνεται αυτόματα σε κάθε φόρτωση/
προσθήκη/επεξεργασία οχήματος (ίδιο `data.vehicles` array, καμία ξεχωριστή
ανανέωση χρειάζεται). Καμία αλλαγή backend/Sheet/δεδομένων.

## Στόλος Οχημάτων: επαναφορά «Επόμενο service» στην κάρτα — ΕΓΙΝΕ (05/09/2026)

Ρητό αίτημα χρήστη: οι κάρτες οχημάτων να δείχνουν και τη λήξη ΚΤΕΟ ΚΑΙ την
ημερομηνία επόμενου service μαζί. Το ΚΤΕΟ ήδη εμφανιζόταν (`fleetDateBadge
(v.KteoDate)`, μέσα στο `.fleet-info-foot` δίπλα στο e-pass) — το Service
είχε αφαιρεθεί εντελώς από το UI σε προηγούμενο πέρασμα (03/09/2026, §Στόλος
Οχημάτων: αφαίρεση στήλης «Service»), αν και το backend/Sheet συνέχιζε να το
αποθηκεύει κανονικά (merge-fallback στο `updateVehicle()`) — απλά δεν
υπήρχε πουθενά ορατό. Με το redesign σε κάρτες (04/09/2026) το πεδίο έγινε
ΚΑΙ πάλι επεξεργάσιμο μέσω του ενιαίου modal, οπότε τώρα προστέθηκε πίσω
και η εμφάνισή του.

Υλοποίηση (`hub.html`, `renderFleetList()`): το `.fleet-info-foot` (μέσα
στο `.fleet-info-box`) κρατάει πλέον ΜΟΝΟ το e-pass badge — το ΚΤΕΟ
μετακόμισε σε νέα ενότητα `.fleet-dates-row` από κάτω, μαζί με το Service,
σε 2-στηλο grid με ρητή ετικέτα πάνω από κάθε badge (`.fleet-date-item`:
"ΚΤΕΟ"/"Επόμενο service" ως `.fleet-info-label`, ίδιο μοτίβο ετικέτας με το
"Ανατεθειμένος" από πάνω) — χωρίς ετικέτες τα δύο badges θα ήταν διφορούμενα
σε μια κάρτα (ο παλιός πίνακας είχε στήλες με headers, η κάρτα όχι). Reuse
100% της ήδη υπάρχουσας `fleetDateBadge()` (ίδιο 30-ημερών κατώφλι/
χρωματισμός/tooltip) και για τα δύο πεδία — καμία αλλαγή στη λογική
alert/χρωματισμού. Νέα CSS `.fleet-dates-row`/`.fleet-date-item` (grid
2 στηλών, divider από πάνω, `min-width:0` ώστε το ήδη υπάρχον
`text-overflow:ellipsis` του `.status-pill` να δουλεύει σωστά σε στενή
μισή-κάρτα στήλη). Καμία αλλαγή backend/δεδομένων — το `ServiceDate`
διαβαζόταν ήδη σωστά από το API, απλά δεν εμφανιζόταν πουθενά στο tab.

## Στόλος Οχημάτων: μετρητής ημερών "Σε συνεργείο" — ΕΓΙΝΕ (04/09/2026)

Ρητό αίτημα χρήστη: όταν ένα όχημα είναι σε κατάσταση «Σε service»
(συνεργείο), η κάρτα να δείχνει την ημερομηνία που μπήκε και έναν
μετρητή ημερών που «τρέχει» όσο παραμένει εκεί, μέχρι να αλλάξει
κατάσταση («να βγει»).

**Backend (`src/fleet.js`):** νέα στήλη `ServiceEnteredAt` στο τέλος του
`FLEET_HEADER_ORDER` — **αυτόματα διαχειριζόμενο πεδίο, ΧΩΡΙΣ κανένα
πεδίο φόρμας** (ο χρήστης δεν το επεξεργάζεται ποτέ χειροκίνητα, ίδιο
"server-derived, αγνοεί ό,τι σταλεί από client" μοτίβο με το
`ResponseMode`/`autoApprove` αλλού στο portal). Λογική μετάβασης
κατάστασης: `addVehicle()` γράφει τη σημερινή ημερομηνία (ISO) αν το
όχημα δημιουργείται ήδη με `Status === "Σε service"`. `updateVehicle()`
συγκρίνει το νέο `Status` με το `existing.Status` πριν την αλλαγή —
**μπαίνει τώρα** (σημερινή ημερομηνία) μόνο όταν το όχημα μόλις ΜΠΗΚΕ σε
"Σε service" (δεν ήταν ήδη εκεί), **καθαρίζει** μόνο όταν μόλις ΒΓΗΚΕ από
"Σε service" προς οποιαδήποτε άλλη κατάσταση, και **παραμένει όπως ήταν**
σε κάθε άλλη περίπτωση (π.χ. επεξεργασία σημείωσης/χιλιομέτρων ενώ το
όχημα είναι ήδη σε service ΔΕΝ μηδενίζει τον μετρητή — κρίσιμο, αφού το
ενιαίο modal Προσθήκης/Επεξεργασίας του redesign σε κάρτες παραπάνω
ξανασώζει ΟΛΑ τα πεδία σε κάθε αποθήκευση). Καμία αλλαγή χρειάστηκε στο
`GET /api/fleet` (`handleFleetList`, `src/index.js`) — το πεδίο περνάει
ήδη μέσα από το spread `{ ...v, EpassLabel: ... }`.

**Frontend (`hub.html`):** νέα `fleetDaysSince(dateStr)` — περνάει την
τιμή από το ήδη υπάρχον `normalizeDateStr()` (ίδια άμυνα με το
`fmtDate()` για ISO/ελληνική/bare-serial μορφές, βλ. §Bugfix garbled
ημερομηνίες) και επιστρέφει ακέραιες ημέρες μέχρι σήμερα (ζωντανός
υπολογισμός σε κάθε render, καμία αποθηκευμένη τιμή). Στην κάρτα οχήματος
(`renderFleetList()`), όταν `v.Status === 'Σε service'` ΚΑΙ υπάρχει
`v.ServiceEnteredAt`, εμφανίζεται νέο badge `.fleet-service-flag`
("Σε συνεργείο από 04/09/2026 · 3 ημέρες", ενικός/πληθυντικός σωστά) —
ανάμεσα στην κορυφή της κάρτας (πινακίδα/status) και το κουτί
Ανατεθειμένου/e-pass/ΚΤΕΟ. Νέα CSS κλάση `.fleet-service-flag` (μετά το
`.fleet-card-footer`) — ίδιο χρωματικό μοτίβο με `.status-pill.st-warn`
(`#FFF4E5`/`var(--amber)`, dark-mode override `rgba(240,169,58,0.16)`
ίδιο με το ήδη υπάρχον global dark-theme override block) αλλά ΧΩΡΙΣ
`white-space:nowrap`/`text-overflow:ellipsis` — είναι πλήρης πρόταση, όχι
σύντομο badge. Καμία αλλαγή στο μοτίβο κατάστασης `fleetStatusClass()`
(«Σε service» συνεχίζει να δείχνει `st-warn` στο κύριο status badge, όπως
πριν) — το νέο flag είναι επιπλέον, όχι αντικατάσταση.

**Χειροκίνητο βήμα εκκρεμεί** (βλ. §Χειροκίνητα βήματα): προσθήκη
επικεφαλίδης `ServiceEnteredAt` στο τέλος του φύλλου `Στόλος`.

## Νέο tab «SIMs» — ΕΓΙΝΕ (05/09/2026)

Ρητό αίτημα χρήστη: νέο tab για αποθήκευση καρτών SIM — αριθμός κάρτας,
PIN, PUK, και σε ποιο tablet είναι τοποθετημένη. Νέο `src/sims.js`, ίδιο
απλό CRUD μοτίβο με το `src/equipment.js` (add/update/delete/list), αλλά
**ΧΩΡΙΣ κανένα μηχανισμό χρέωσης/ανάθεσης σε τεχνικό** (`src/assignments.js`
δεν αγγίζεται) — δεν ζητήθηκε ανάθεση σε πρόσωπο, μόνο "σε ποιο tablet".

**Σχεδιαστικές αποφάσεις (λογικές προεπιλογές, συνεπείς με το υπάρχον
μοτίβο του portal, δεν χρειάστηκε να ρωτηθεί ο χρήστης):**
- **Πρόσβαση: ΜΟΝΟ TL + Director** (`requireTeamLeader`, ΟΧΙ Backoffice) —
  ίδιο gate με Στόλο/Εξοπλισμό/e-pass, αφού πρόκειται για εταιρικό
  εξοπλισμό/απόρρητα στοιχεία (PIN/PUK), όχι κάτι που αφορά το Backoffice.
- **Το πεδίο "Tablet" συνδέεται με το φύλλο Εξοπλισμός — ΕΓΙΝΕ (05/09/2026,
  ρητή απαίτηση χρήστη, ΑΝΑΘΕΩΡΕΙ την αρχική απόφαση «σκόπιμα ΔΕΝ
  συνδέεται»):** το dropdown αντλείται πλέον από τα αντικείμενα του
  φύλλου `Εξοπλισμός` με `Category` που περιέχει "tablet" (case-insensitive
  substring match, ίδιο ελεύθερο πεδίο Category με το tab Εξοπλισμός —
  καμία hardcoded λίστα κατηγοριών), υπολογισμένο server-side μέσα στο
  `handleSimsList()` (`src/index.js`, reuse του ήδη υπάρχοντος
  `listEquipment()`) — ΟΧΙ πια από τις μοναδικές τιμές του ίδιου του
  φύλλου `SIMs` (η παλιά `listSimTablets()` στο `src/sims.js` έμεινε στον
  κώδικα αχρησιμοποίητη, ίδιο «δεν αφαιρούνται παλιά helpers» μοτίβο με
  άλλα σημεία του portal). Η τιμή που αποθηκεύεται στο `Tablet` της SIM
  είναι το κείμενο που εμφανίζεται στο dropdown (όχι κάποιο ID) — καθαρά
  εμφανίσιμο κείμενο, ίδιο μοτίβο με τα dropdowns τεχνικού που επίσης
  αποθηκεύουν όνομα. **Επέκταση ίδια μέρα (ρητή απαίτηση χρήστη):** το
  εμφανιζόμενο κείμενο περιλάμβανε αρχικά ΚΑΙ τον Σειριακό Αριθμό μαζί με
  το Όνομα («Όνομα — ΣΝ: Σειριακός») — **ΑΝΑΘΕΩΡΗΘΗΚΕ αυθημερόν, ρητή
  απαίτηση χρήστη με screenshot:** δείχνει πλέον ΜΟΝΟ τον Σειριακό Αριθμό,
  χωρίς το Όνομα. Συνέπεια: αντικείμενα κατηγορίας "tablet" ΧΩΡΙΣ
  καταχωρημένο σειριακό αριθμό αποκλείονται εντελώς από το dropdown (δεν
  μπορούν να αναγνωριστούν έτσι) — η επιλογή «Άλλο tablet... (δεν υπάρχει
  ακόμα στον Εξοπλισμό)» παραμένει γι' αυτά, ή αν το tablet δεν έχει
  καταχωρηθεί ακόμα ως αντικείμενο εξοπλισμού — γράφει ελεύθερο κείμενο
  όπως πριν, χωρίς να δημιουργεί αυτόματα νέα γραμμή στο Εξοπλισμό (one-way
  lookup, όχι two-way sync). Αν το ίδιο tablet έχει ήδη πάνω του χρέωση/
  ανάθεση σε τεχνικό (μέσω Εξοπλισμού), αυτό ΔΕΝ φαίνεται πουθενά στο tab
  SIMs — καθαρά πληροφοριακή σύνδεση μόνο για να γεμίζει το dropdown, καμία
  άλλη αλληλεπίδραση μεταξύ των δύο φύλλων.
- **Αριθμός κάρτας σε ομάδες των 4 ψηφίων — ΕΓΙΝΕ** (ρητή απαίτηση χρήστη
  με screenshot): ο πίνακας δείχνει πλέον τον `CardNumber` χωρισμένο σε
  ομάδες των 4 χαρακτήρων με κενό (π.χ. «8930 0100 2502 1198 3984»,
  `simCardNumberDisplay()` στο `hub.html`) — καθαρά εμφάνιση, η
  αποθηκευμένη τιμή στο Sheet ΚΑΙ το πεδίο επεξεργασίας (inline edit)
  παραμένουν χωρίς κενά/αμετάβλητα. Το escaping γίνεται ανά ομάδα των 4
  χαρακτήρων πριν την ένωση με κενά (όχι στο τελικό συνενωμένο string) —
  ώστε ένα HTML entity να μην κοπεί ποτέ στη μέση από το χωρισμό.
- **PIN/PUK εμφανίζονται σε απλό κείμενο στον πίνακα** (όχι masked/toggle
  όπως το Huskies password) — η κάρτα SIM/PIN/PUK είναι ήδη ορατή μόνο σε
  TL/Director (ίδιο επίπεδο πρόσβασης με ΑΜΚΑ/ΑΦΜ στο Προφίλ Τεχνικού),
  καμία πρόσθετη πολυπλοκότητα masking δεν κρίθηκε απαραίτητη.
- Κανένα πεδίο "Κατάσταση" (Ενεργή/Ανενεργή) — δεν ζητήθηκε, μόνο τα 3
  ζητηθέντα στοιχεία + Tablet + Σημείωση.

**Backend** (`src/sims.js`): `SHEET_SIMS = "SIMs"`, headers `ID |
CardNumber | PIN | PUK | Tablet | Note | UpdatedBy | UpdatedAt` — νέο
φύλλο, χειροκίνητο βήμα εκκρεμεί (βλ. §Χειροκίνητα βήματα). Endpoints
(`src/index.js`, όλα `requireTeamLeader`): `GET /api/sims` (λίστα καρτών SIM
+ λίστα tablets από τον Εξοπλισμό, βλ. παραπάνω), `POST /api/sims/add`,
`POST /api/sims/update`, `POST /api/sims/delete`. Νέες καταχωρήσεις `sim_add`/`sim_update`/`sim_delete`
στο `ACTIVITY_ACTION_META` (widget «Τελευταία δραστηριότητα» της Αρχικής,
ορατό σε leader/director — ίδιο audience με το tab).

**Frontend** (`hub.html`): νέο tab `sims` στα `TOOLS.leader`/`TOOLS.director`
(section 'team', μετά το e-pass), νέο εικονίδιο `sims: 'device-sim-1'` στο
`NAV_ICON_MAP`. `renderSims()`/`renderSimsList()`/`addSimItem()`/
`editSimItem()`/`saveSimItem()`/`deleteSimItem()` — ίδιο table+modal+inline-
edit μοτίβο με το Εξοπλισμό (`fl-table`, icon-only Ενέργειες κουμπιά,
`ta-center` στήλες, `escapeHtml()` σε όλα τα ελεύθερα πεδία κειμένου).
Καμία εξαγωγή CSV/PDF προς το παρόν (δεν ζητήθηκε — αν χρειαστεί, ίδιο
reuse `exportTableToCsv`/`exportTableToPdf` με τα υπόλοιπα tabs).

**Fix: αρχικά μηδενικά (0) έσβηναν από CardNumber/PIN/PUK — ΕΓΙΝΕ (05/09/2026,
ρητή αναφορά χρήστη με screenshot):** π.χ. ένα PIN "0424" αποθηκευόταν και
εμφανιζόταν ως "424". **Ρίζα:** το `appendRow()`/`updateRow()` (`src/sheets.js`)
γράφουν πάντα με `valueInputOption=USER_ENTERED` (ίδιο σε όλο το backend,
όχι κάτι ειδικό για τα SIMs) — το Sheets API ερμηνεύει μια καθαρά αριθμητική
τιμή σαν αριθμό και πετάει το αρχικό "0" (`"0424"` → `424`), ακριβώς όπως θα
συνέβαινε αν κάποιος πληκτρολογούσε 0424 απευθείας σε ένα κελί μέσα στο
Google Sheets UI. Fix (`src/sims.js`): νέα `forceSheetText(v)` προσθέτει ένα
μπροστινό `'` (το ίδιο σημάδι "εξαναγκασμός σε κείμενο" που χρησιμοποιεί το
ίδιο το Sheets UI) ΜΟΝΟ στις τιμές που γράφονται στο Sheet για τα πεδία
`CardNumber`/`PIN`/`PUK` (`SIMS_TEXT_FIELDS`) — το `'` δεν αποθηκεύεται ποτέ
ως μέρος της τιμής (το Sheets το αφαιρεί αυτόματα στην εγγραφή), απλά
αναγκάζει το κελί να μείνει κείμενο. Τα αντικείμενα που επιστρέφονται στο
frontend (`item`/`merged`) παραμένουν πάντα «καθαρά» (χωρίς το `'`) — το
prefix προστίθεται ΜΟΝΟ στο ξεχωριστό `sheetRow`/`values` array που πάει
στο Sheets API, ίδιο μοτίβο σε ΚΑΙ τα δύο `addSim()`/`updateSim()`. Scope
του fix: ΜΟΝΟ το `src/sims.js` (ρητό αίτημα χρήστη ήταν για το tab SIMs) —
το `Tablet`/`Note`/`UpdatedBy`/`UpdatedAt` ΔΕΝ επηρεάζονται (ελεύθερο
κείμενο, δεν έχει νόημα «αρχικό μηδενικό» εκεί). Το ΙΔΙΟ ρίσκο (αρχικό "0"
σε αριθμητικό-όψεως ελεύθερο πεδίο) υπάρχει θεωρητικά και αλλού στο portal
(π.χ. ΑΜΚΑ/ΑΦΜ/τηλέφωνα στο Υπάλληλοι) — δεν αναφέρθηκε πρόβλημα εκεί ακόμα,
αν εμφανιστεί το ίδιο fix (`forceSheetText`) μπορεί να επεκταθεί εκεί.
**Γνωστό όριο:** το fix αποτρέπει ΜΟΝΟ μελλοντική απώλεια — ήδη υπάρχουσες
γραμμές SIM που έχουν ήδη χάσει το αρχικό "0" (το Sheets το έχει ήδη
αποθηκεύσει σαν αριθμό, δεν είναι ανακτήσιμο από τον κώδικα) χρειάζονται
χειροκίνητη διόρθωση — άνοιγμα «Επεξεργασία» στη συγκεκριμένη κάρτα SIM,
πληκτρολόγηση της σωστής τιμής με το μηδενικό, και «Αποθήκευση» (από εκεί
και πέρα θα αποθηκεύεται σωστά ως κείμενο).

## Προφίλ Τεχνικού: S/N αντικειμένων στο «Στην κατοχή του τώρα» — ΕΓΙΝΕ (05/09/2026)

Ρητό αίτημα χρήστη: ο πίνακας «Στην κατοχή του τώρα» (tab Προφίλ Τεχνικού)
να δείχνει και τον Σειριακό Αριθμό των αντικειμένων εξοπλισμού. Νέα στήλη
«S/N» ανάμεσα στο «Αντικείμενο» και το «Ημ/νία χρέωσης».

**Backend** (`src/assignments.js`, `getTechnicianHoldings()` — shared
συνάρτηση, χρησιμοποιείται ΚΑΙ από το Προφίλ Τεχνικού ΚΑΙ από την
Αναζήτηση τεχνικού στο tab Εξοπλισμός): μετά τον υπολογισμό των ανοιχτών
holdings, αν υπάρχει έστω ένα holding που ΔΕΝ είναι `Type === "Όχημα"`
ούτε `Type === "e-pass"` (δηλ. είναι αντικείμενο εξοπλισμού — το `Type`
μιας χρέωσης εξοπλισμού είναι η Κατηγορία του αντικειμένου, βλ.
`syncEquipmentAssignment()`), διαβάζει το φύλλο `Εξοπλισμός` (lazy, ΜΟΝΟ
όταν χρειάζεται — ίδιο σκεπτικό με τα ήδη υπάρχοντα conditional reads του
ίδιου αρχείου, αποφυγή περιττού API call όταν ο τεχνικός έχει πάνω του
μόνο όχημα/e-pass) και κάνει lookup ανά `ItemID` -> `SerialNumber`,
προσθέτοντάς το ως νέο πεδίο `SerialNumber` στο αντίστοιχο holding object.
Νέα σταθερά `SHEET_EQUIPMENT = "Εξοπλισμός"` — διαβάζεται απευθείας μέσω
`readSheetAsObjects()` (ΟΧΙ import από το `src/equipment.js`, που ήδη
εισάγει από το `assignments.js` — θα δημιουργούσε κυκλική εξάρτηση).
Οχήματα/e-pass δεν έχουν έννοια «Σειριακού Αριθμού» σε αυτό το σημείο,
οπότε αφήνονται ανέγγιχτα.

**Frontend** (`hub.html`, `loadTechProfile()`): νέα στήλη `<th>S/N</th>`
+ αντίστοιχο `<td>` (`h.SerialNumber` αν υπάρχει, αλλιώς «—» με
`.cell-muted`, ίδιο μοτίβο κενής τιμής με τα υπόλοιπα σημεία του portal) —
`colspan` της κενής-λίστας γραμμής ενημερώθηκε 4→5. Παράλληλα προστέθηκε
`escapeHtml()` στο `h.ItemLabel` (έλειπε πριν — ελεύθερο κείμενο από το
`Χρεώσεις`/`Εξοπλισμός`, ίδιο pattern με το code review ασφαλείας του
25/08/2026).

**Θετική παρενέργεια, όχι ρητά ζητημένη:** επειδή το `getTechnicianHoldings()`
είναι shared, ο ίδιος εμπλουτισμός με S/N είναι πλέον διαθέσιμος και στο
`GET /api/equipment/technician-holdings` (Αναζήτηση τεχνικού, tab
Εξοπλισμός) — ΔΕΝ ενημερώθηκε όμως το αντίστοιχο UI (`searchTechnicianEquipment()`
στο `hub.html`) να το εμφανίσει, αφού δεν ζητήθηκε ρητά εκεί· αν χρειαστεί,
είναι πλέον απλή προσθήκη μιας ακόμα στήλης στον ήδη υπάρχοντα πίνακα
αποτελεσμάτων.

## Ανέβασμα δικαιολογητικού αναρρωτικής από τον Team Leader — ΕΓΙΝΕ (03/09/2026)

Ρητό αίτημα χρήστη: όταν ο TL καταχωρεί ο ίδιος μια αναρρωτική άδεια εκ
μέρους τεχνικού (κάρτα «Καταχώρηση άδειας για τεχνικό», tab Άδειες
Ομάδας/Team Leaders), να μπορεί να ανεβάσει και ο ίδιος το δικαιολογητικό
PDF — μέχρι τώρα το ανέβασμα (`uploadLeaveAttachment`, item #1 στο
§Επόμενα ζητούμενα) επιτρεπόταν ΜΟΝΟ στον ίδιο τον αιτούντα τεχνικό, από το
δικό του tab «Οι Άδειές μου».

**Ρίζα του περιορισμού:** το `uploadLeaveAttachment()` (`src/leaves.js`)
έκανε strict έλεγχο ισότητας `row.EmployeeID === employeeId`, και ο
handler `handleLeaveAttachmentUpload()` (`src/index.js`) περνούσε πάντα το
`resolveEmployeeId(env, identity)` του ΙΔΙΟΥ του καλούντος — για staff
identity αυτό επιστρέφει πάντα το EmployeeID της self-row του TL (ποτέ του
τεχνικού για τον οποίο ενεργεί), άρα ο έλεγχος απέτυχε πάντα όταν
προσπαθούσε ένας TL.

**Fix:** `uploadLeaveAttachment()` δέχεται πλέον προαιρετικό `staffEmail` —
επιτρέπεται αν `isOwner` (ίδιο employeeId, όπως πριν) Ή `isTeamLeader`
(`isAuthorizedForTeam(env, staffEmail, row.TeamLeaderEmail)`, ΙΔΙΟΣ έλεγχος
με το `getLeaveAttachmentById()` της λήψης — καλύπτει αυτόματα και τον
backup TL όσο διαρκεί το "Λείπω" του κύριου). Backoffice ΔΕΝ αποκτά
πρόσβαση εδώ (ποτέ δεν είναι το `row.TeamLeaderEmail` μιας αίτησης —
συνεπές με απόφαση #1). `handleLeaveAttachmentUpload()` (`src/index.js`)
υπολογίζει τώρα και `staffEmail` (ίδιο pattern με `handleLeaveAttachmentFile`,
τη λήψη) και το περνάει μέσα — το 403 χαλάρωσε σε `!employeeId &&
!staffEmail`. Το audit log (`leave_attachment_uploaded`) πήρε νέο πεδίο
`uploadedByTeamLeader` για ορατότητα ποιος πραγματικά ανέβασε. Η email
ειδοποίηση προς τον TL (υπήρχε ήδη, για όταν ανεβάζει ο τεχνικός) παραλείπεται
σκόπιμα όταν ο ίδιος ο εγκριτής είναι αυτός που μόλις ανέβασε το αρχείο
(`approver.Email !== staffEmail`) — δεν έχει νόημα να ειδοποιηθεί κάποιος
για τη δική του ενέργεια.

**Frontend (`hub.html`):** το `sickAttachmentBadge()` — χρησιμοποιείται στη
στήλη ημερομηνιών του ενιαίου πίνακα «Αιτήσεις άδειας»
(`renderLeaveRequestsTable()`, tab Άδειες Ομάδας/Team Leaders) — έδειχνε
πριν μόνο στατικό κείμενο «χωρίς δικαιολογητικό» όταν έλειπε το αρχείο· τώρα
δείχνει κουμπί «Ανέβασμα PDF» (`data-tl-upload-id`) σε αυτή την περίπτωση,
ίδιο οπτικά με το αντίστοιχο κουμπί του τεχνικού στο «Οι Άδειές μου»
(`renderAttachmentCell()`). Νέο hidden file input `tl-file-input` (ίδιο
μοτίβο με το `ml-file-input` του τεχνικού, διαφορετικό ID/DOM ώστε να μη
συγκρούεται) μέσα στο template του `renderTeamLeaves()`. Νέες
`triggerTeamLeaveUpload()`/`uploadTeamLeaveFile()` — καθαρό reuse του ήδη
υπάρχοντος `triggerLeaveUpload()`/`uploadLeaveFile()` pattern (ίδιο
`/api/leaves/attachment/upload` endpoint, ίδιοι client-side έλεγχοι τύπου/
μεγέθους PDF), με re-render στο σωστό tab (`renderTeamLeaves()` αντί για
`renderMyLeaves()`) μετά από επιτυχία. Καμία αλλαγή στο ήδη υπάρχον
`renderAttachmentCell()`/`triggerLeaveUpload()`/`uploadLeaveFile()` του
τεχνικού — παραμένουν ξεχωριστές, παράλληλες συναρτήσεις (διαφορετικό
DOM/tab context), όχι reused απευθείας.

**Επέκταση ΙΔΙΑ μέρα (03/09/2026, ρητή απαίτηση χρήστη): δικαιολογητικό ήδη
τη στιγμή της υποβολής, σε ΚΑΘΕ φόρμα αίτησης.** Μέχρι το παραπάνω, το
δικαιολογητικό μπορούσε να ανέβει μόνο ΜΕΤΑ την υποβολή (από το ιστορικό
του τεχνικού ή τον πίνακα «Αιτήσεις άδειας» του TL). Ο χρήστης ζήτησε να
προστεθεί και στις δύο φόρμες υποβολής η δυνατότητα επισύναψης PDF
απευθείας, ώστε να μη χρειάζεται δεύτερο βήμα:
- **«Νέα αίτηση άδειας»** (tab «Οι Άδειές μου», `ml-*` — χρησιμοποιείται
  από ΚΑΘΕ ρόλο που υποβάλλει για τον εαυτό του: τεχνικός, TL, Backoffice,
  Director, ρητά "για κάθε χρήστη").
- **«Καταχώρηση άδειας για τεχνικό»** (tab Άδειες Ομάδας/Team Leaders,
  `tl-*` — ο TL/Director εκ μέρους τεχνικού).

Και στις δύο, νέο προαιρετικό πεδίο «Δικαιολογητικό (PDF, προαιρετικό)»
(`ml-attach-field`/`tl-attach-field`, με εσωτερικό file input) — **ορατό
μόνο όταν ο επιλεγμένος Τύπος == Αναρρωτική** (νέα κοινή
`wireSickAttachmentToggle(typeSelectId, fieldId)`, δεμένη σε `change` του
select + αρχική κλήση, reused και στις δύο φόρμες). Η κάρτα «Μαζική άδεια»
(`bl-*`, Director-only, βλ. παραπάνω) ΔΕΝ πήρε αντίστοιχο πεδίο — δεν
ζητήθηκε, και δεν θα είχε νόημα ένα ΚΟΙΝΟ αρχείο για δεκάδες διαφορετικούς
υπαλλήλους ταυτόχρονα.

**Backend: καμία αλλαγή χρειάστηκε.** Και το `/api/leaves/submit`
(`submitLeaveRequest`) και το `/api/leaves/submit-for-team`
(`submitLeaveRequestAsLeader` → ίδια `submitLeaveRequest` από κάτω, με
`autoApprove:true`) επέστρεφαν ΗΔΗ `requestId` στην απάντηση — αρκούσε να
το χρησιμοποιήσει το frontend. Νέα κοινή `uploadAttachmentSilently(requestId,
file)` (`hub.html`) — ίδιο `POST /api/leaves/attachment/upload` με το
`uploadLeaveFile()`/`uploadTeamLeaveFile()` που ήδη υπάρχουν, αλλά ΧΩΡΙΣ
δικό της `alert()`/re-render (αυτά ενεργοποιούνται από ξεχωριστό κουμπί σε
ΗΔΗ υποβληθείσα αίτηση — διαφορετικό UX) — ο καλών (`submitMyLeave()`/
`submitTeamLeave()`) αποφασίζει πώς να δείξει το αποτέλεσμα μέσα στο ίδιο
μήνυμα επιτυχίας της υποβολής.

**Ανθεκτικότητα σε αποτυχία:** η υποβολή της αίτησης και το ανέβασμα του
αρχείου είναι δύο ξεχωριστά HTTP calls (σκόπιμα — το backend δεν
υποστηρίζει multipart/form-data με JSON+αρχείο μαζί, ίδιο σχεδιαστικό όριο
με το `/api/leaves/attachment/upload`, που δέχεται μόνο raw bytes). Αν η
αίτηση πετύχει αλλά το upload αποτύχει (π.χ. δικτυακό σφάλμα), η αίτηση
ΠΑΡΑΜΕΝΕΙ υποβεβλημένη — το μήνυμα επιτυχίας ενημερώνει ρητά ότι το
δικαιολογητικό απέτυχε και ότι μπορεί να ξαναδοκιμαστεί από το ήδη
υπάρχον σημείο (ιστορικό αιτήσεων για τον εαυτό του, πίνακας «Αιτήσεις
άδειας» για τον TL) — καμία απώλεια δεδομένων, απλά ένα επιπλέον χειροκίνητο
βήμα σε σπάνια περίπτωση σφάλματος δικτύου. Client-side ίδιοι έλεγχοι τύπου
(`application/pdf`)/μεγέθους (20MB) με τα υπόλοιπα σημεία ανεβάσματος, πριν
καν σταλεί η αίτηση.

## Huskies credentials (24/08/2026)

Ρητή απαίτηση χρήστη: «Προσθήκη κωδικών Huskies (Username/Password) στο
Προφίλ Τεχνικού με δυνατότητα αποστολής email αλλα και στο tab Νέος
Τεχνικός ώστε να στέλνονται μαζί με τα credentials του portal». Huskies
είναι τρίτο σύστημα (εκτός portal) — τα credentials αποθηκεύονται ΩΣ ΕΧΟΥΝ
(plaintext) στο φύλλο `Υπάλληλοι`, νέες στήλες `HuskiesUsername`/
`HuskiesPassword` στο τέλος (`EMPLOYEE_HEADER_ORDER`, `src/leaves.js`) —
ίδιο επίπεδο "ασφάλειας" με τα υπόλοιπα ευαίσθητα πεδία του φύλλου
(ΑΜΚΑ/ΑΦΜ), ΔΕΝ hashed σαν το PIN τεχνικού (KV) γιατί χρειάζεται να
εμφανίζεται/αποστέλλεται ξανά, όχι μόνο να επαληθεύεται.

3 ερωτήσεις διευκρίνισης πριν την υλοποίηση, ρητές αποφάσεις χρήστη:
1. Το email του κουμπιού «Αποστολή email» στο Προφίλ Τεχνικού περιέχει
   **ΜΟΝΟ** τα Huskies credentials — καμία υπενθύμιση των portal credentials
   (αυτά έχουν ήδη σταλεί στο welcome email της δημιουργίας).
2. Το password στο Προφίλ Τεχνικού είναι **κρυμμένο by default**
   (`••••••••`) με κουμπί «Εμφάνιση»/«Απόκρυψη» δίπλα (`tp-huskies-toggle-btn`
   στο `hub.html` — client-side μόνο, η πραγματική τιμή είναι ήδη στο DOM
   μέσω `data-value`, καμία επιπλέον κλήση API στο toggle).
3. Τα πεδία Huskies στη φόρμα «Νέος Τεχνικός» είναι **προαιρετικά**.

**Νέος Τεχνικός** (`renderNewTechnician()`/`submitNewTechnician()` στο
`hub.html`): νέα ενότητα «Στοιχεία Huskies (προαιρετικά)» με 2 πεδία
(`nt-huskies-user`/`nt-huskies-pass`, απλά `type="text"` — ρητά ΟΧΙ
`type="password"`, ώστε ο TL/Backoffice να μπορεί να επιβεβαιώσει οπτικά
τι πληκτρολόγησε τη στιγμή της δημιουργίας, καμία ρητή απαίτηση χρήστη για
masking εδώ, μόνο στο Προφίλ Τεχνικού). Αν συμπληρωθεί έστω το ένα,
`handleEmployeesCreate()` (`src/index.js`) προσθέτει 2 επιπλέον γραμμές
("Huskies — Username"/"Huskies — Password") στο ΙΔΙΟ `rows` array του ήδη
υπάρχοντος welcome email (`emailTemplate()`) — ίδιο email, μαζί με
Κωδικό υπαλλήλου/PIN, ρητή απαίτηση χρήστη "να στέλνονται μαζί".

**Προφίλ Τεχνικού** (`loadTechProfile()` στο `hub.html`): νέα κάρτα
«Huskies» ανάμεσα στην κάρτα «Κατάσταση» και την κάρτα «Έγγραφα» — δείχνει
Username απλό κείμενο + Password μασκαρισμένο με toggle + κουμπί «Αποστολή
email» (`tp-huskies-send-btn`, καλεί το νέο `POST /api/employees/huskies/send-email`).
Αν δεν υπάρχουν καταχωρημένα στοιχεία, η κάρτα δείχνει κενή κατάσταση με
οδηγία να συμπληρωθούν από το «Επεξεργασία στοιχείων» (το ίδιο modal
`tp-edit-modal` πήρε επίσης τα 2 νέα πεδία, plaintext, ίδιο μοτίβο merge
`undefined = άφησέ το όπως ήταν` με τα υπόλοιπα πεδία του `updateTechnicianRecord()`).

**Backend**: `createTechnicianRecord()`/`updateTechnicianRecord()`
(`src/leaves.js`) δέχονται τα 2 νέα προαιρετικά πεδία. Νέο
`POST /api/employees/huskies/send-email` (`handleEmployeeHuskiesEmail`,
`src/index.js`) — ίδιο gate `requireTeamLeader` με το υπόλοιπο tab Προφίλ
Τεχνικού (TL+Director, ΟΧΙ Backoffice — δεν έχει καν πρόσβαση στο tab).
Απορρίπτει με σαφές μήνυμα αν δεν υπάρχουν καταχωρημένα Huskies στοιχεία, ή
αν ο τεχνικός δεν έχει email, ή αν η αποστολή αποτύχει (ρητά ΔΕΝ κρύβει
σιωπηλά την αποτυχία — σε αντίθεση με τα passive notification emails του
portal, αυτό είναι ρητή on-demand ενέργεια του χρήστη, πρέπει να ξέρει αν
δεν στάλθηκε). `logEmailFailure(env, "huskies_email_failed", ...)` για
ορατότητα στο Ιστορικό Ενεργειών, ίδιο μοτίβο με τα υπόλοιπα ~18 σημεία
αποστολής email του portal.

**Bug που αποκαλύφθηκε ΤΗΝ ΙΔΙΑ ΜΕΡΑ κατά τον έλεγχο — ΕΓΙΝΕ (24/08/2026):**
ο χρήστης πρόσθεσε τις 2 νέες στήλες στο Sheet και δοκίμασε να καταχωρήσει
Huskies στοιχεία απευθείας στο ίδιο το Sheet για έναν τεχνικό — το Προφίλ
Τεχνικού συνέχιζε να δείχνει «Δεν έχουν καταχωρηθεί στοιχεία Huskies» παρά
την ήδη υπαρκτή τιμή στο κελί. Ρίζα: το `readSheetAsObjects()`
(`src/sheets.js`) διάβαζε με **hardcoded εύρος `A1:Z10000`** (μόνο 26
στήλες, μέχρι τη στήλη Z) — το φύλλο `Υπάλληλοι` είχε ΗΔΗ ακριβώς 26
στήλες μέχρι το `AFM` (τελευταία πριν τις νέες), άρα βρισκόταν ήδη ακριβώς
στο όριο, χωρίς κανείς να το έχει προσέξει· η προσθήκη 2 ακόμα στηλών
(`HuskiesUsername`/`HuskiesPassword`, στήλες AA/AB) τις έσπρωξε ΕΞΩ από το
εύρος ανάγνωσης — σιωπηλά, καμία exception, απλά έλειπαν από κάθε
επιστρεφόμενο object. Το ΙΔΙΟ hardcoded όριο υπήρχε ΚΑΙ στο
`getSheetRawValues()` (καλείται από το καθημερινό backup, `src/backup.js`
— άρα το backup έκοβε σιωπηλά ΚΑΘΕ φύλλο με >26 στήλες, όχι μόνο το
Υπάλληλοι) ΚΑΙ στο `getHeaderRow()` (καλείται από το
`getUnformattedColumns()` της ριζικής λύσης ημερομηνιών, βλ. §Bugfix
garbled ημερομηνίες — αν κάποιο φύλλο εκεί ξεπερνούσε τις 26 στήλες, θα
γύριζε `null`/αδύνατη εύρεση, σιωπηλό fallback).
Fix: και τα 3 σημεία άλλαξαν σε εύρος `A1:ZZ...` (702 στήλες) — αρκετά
γενναιόδωρο ώστε να μην ξαναχτυπηθεί το ίδιο όριο στο ορατό μέλλον, χωρίς
κανένα κόστος (το Sheets API επιστρέφει μόνο ό,τι πραγματικά υπάρχει,
ανεξαρτήτως πλάτους του ζητούμενου εύρους). Καμία αλλαγή χρειάστηκε στο
`getUnformattedColumns()` (χρησιμοποιεί ήδη δυναμικό εύρος βάσει
`columnToLetter()`, όχι hardcoded Z) ούτε στο `columnToLetter()` το ίδιο
(ήδη υποστηρίζει πολυγράμματες στήλες κανονικά — το πρόβλημα ήταν
αποκλειστικά στο read-side, οι εγγραφές/writes δεν είχαν ποτέ πρόβλημα).
**Σημείωση:** αυτό σημαίνει ότι οποιοδήποτε φύλλο με >26 στήλες είχε ΗΔΗ
αυτό το πρόβλημα πριν τη σημερινή προσθήκη — ελέγχθηκε ότι κανένα άλλο
φύλλο δεν είχε ξεπεράσει ακόμα τις 26 στήλες τη στιγμή του fix, άρα δεν
υπήρχε άλλη ήδη-ενεργή απώλεια δεδομένων πέρα από το Huskies.

**Redesign Προφίλ Τεχνικού: Κατάσταση μέσα στην κάρτα ταυτότητας — ΕΓΙΝΕ
(24/08/2026, ρητή απαίτηση χρήστη μετά από screenshot/ερώτηση):** η
ξεχωριστή κάρτα «Κατάσταση» (status badge + κουμπιά Επεξεργασία
στοιχείων/Reset PIN/Απενεργοποίηση) συγχωνεύτηκε ΜΕΣΑ στην κάρτα ταυτότητας
(avatar/όνομα/επικοινωνία/ισοζύγιο αδειών) — το status badge εμφανίζεται
τώρα δεξιά στη γραμμή ονόματος (`margin-left:auto` μέσα στο ήδη υπάρχον
`.tp-identity-head`), και η γραμμή περιγραφής+κουμπιών ακολουθεί από κάτω
με δικό της divider (`border-bottom`) πριν τα στοιχεία επικοινωνίας. Η
μικρή κάρτα «Στην κατοχή του» (σύντομη προεπισκόπηση, δεξιά στήλη του
παλιού 2-column `.tp-identity-grid`) αφαιρέθηκε εντελώς — ρητή απαίτηση
χρήστη, ήταν σκόπιμη μικρή επικάλυψη πληροφορίας με τον ήδη υπάρχον πλήρη
πίνακα «Στην κατοχή του τώρα» παρακάτω στη σελίδα (ίδιο `holdings` array,
απλά δεν εμφανίζεται πια δύο φορές). Η κάρτα ταυτότητας έγινε πλέον ΜΙΑ
πλήρους-πλάτους κάρτα (όχι πια 2-column grid) — αφαιρέθηκε το πλέον νεκρό
CSS `.tp-identity-grid`/`.tp-holdings-mini*`. Καμία αλλαγή backend/
δεδομένων — καθαρά αναδιάταξη UI, όλα τα ίδια IDs κουμπιών/inputs
(`tp-reset-pin-btn`, `tp-status-toggle`, `tp-status-msg`, `data-modal-open`)
άρα το ήδη υπάρχον JS wiring δούλεψε χωρίς καμία αλλαγή.

**Selector τεχνικού στο topbar (ίδια γραμμή με τον τίτλο, 2 στήλες) — ΕΓΙΝΕ**
(24/08/2026, ρητή απαίτηση χρήστη): το dropdown επιλογής τεχνικού + κουμπί
«Προβολή προφίλ» (ήταν ξεχωριστή full-width κάρτα «ΤΕΧΝΙΚΟΣ» ανάμεσα στον
τίτλο και τα αποτελέσματα) μετακόμισε στο `right`-slot του `topbar()` —
ίδιος μηχανισμός που ήδη χρησιμοποιούν τα κουμπιά «Νέο όχημα»/«Νέο
αντικείμενο»/«Νέο e-pass» στα tabs Στόλος/Εξοπλισμός/e-pass (`opts.right`,
βλ. §Στυλ/completed item #38) — τίτλος+υπότιτλος αριστερά, selector+κουμπί
δεξιά, ίδια γραμμή (το `.topbar` είναι ήδη `display:flex;
justify-content:space-between`). Το μήνυμα κατάστασης (`tp-msg`) μπήκε από
κάτω, δεξιοστοιχισμένο. Καμία αλλαγή backend/endpoint — το `GET
/api/employees/profile` (λίστα τεχνικών) καλείται όπως πριν, απλά το
`technicians.map(...)` περνάει τώρα μέσα στο `right` HTML string αντί για
ξεχωριστό `<div class="card">`.

**Αυτόματη φόρτωση προφίλ στην επιλογή (χωρίς κουμπί) — ΕΓΙΝΕ** (ίδια μέρα,
ρητή απαίτηση χρήστη με screenshot mockup): το κουμπί «Προβολή προφίλ»
αφαιρέθηκε — η επιλογή τεχνικού από το dropdown (`change` event στο
`#tp-employee`) καλεί απευθείας `loadTechProfile()`, μία λιγότερη ενέργεια
(καμία αλλαγή χρειάστηκε στο `loadTechProfile()` το ίδιο, ήδη διάβαζε την
τιμή απευθείας από το DOM). Το select πήρε δικό του accent στυλ (μπλε
περίγραμμα 1.5px + ελαφρύ glow `box-shadow`, στρογγυλεμένες γωνίες,
`var(--field-bg)`/`var(--text)` — dark-mode-safe) αντί για bare/unstyled
`<select>` — δεν είναι πια μέσα σε `.field` wrapper άρα δεν κληρονομεί
αυτόματα το `.field select` στυλ.

**Πεδίο «Προϋπηρεσία» — ΕΓΙΝΕ** (24/08/2026, ρητή απαίτηση χρήστη): νέο
προαιρετικό πεδίο `PriorExperience` (νέα στήλη `Υπάλληλοι`, χειροκίνητο
βήμα — βλ. §Χειροκίνητα βήματα) — ίδιο μοτίβο με `TechnicalTraining`/
`Address`. Στη φόρμα «Νέος Τεχνικός» (`nt-prior-experience`) δίπλα στην
Ημερομηνία πρόσληψης, και στο modal «Επεξεργασία στοιχείων»
(`tp-edit-prior-experience`) του Προφίλ Τεχνικού, ίδιο merge-pattern με τα
υπόλοιπα πεδία. Εμφανίζεται στην κάρτα ταυτότητας του Προφίλ Τεχνικού
ακριβώς κάτω από την «Απασχόληση» (ρητή θέση, ζητήθηκε ρητά).
**Αριθμητικό σε έτη — ΕΓΙΝΕ, ΑΝΑΘΕΩΡΕΙ την αρχική υλοποίηση** (ίδια μέρα,
24/08/2026, ρητή απαίτηση χρήστη — "μπορείς να μετράς την προυπηρεσια σε
έτη"): το πεδίο ΔΕΝ είναι πια ελεύθερο κείμενο — και τα δύο inputs
(`nt-prior-experience`/`tp-edit-prior-experience`) έγιναν `type="number"
min="0" step="0.5"` (επιτρέπει μισά έτη, π.χ. 2.5), label σε «Προϋπηρεσία
(έτη)». Backend: `createTechnicianRecord()`/`updateTechnicianRecord()`
(`src/leaves.js`) περνάνε πλέον την τιμή από `Number(...)` πριν γραφτεί
στο Sheet (κενή τιμή παραμένει κενό string, ίδιο μοτίβο με το
`AnnualDays`/`Deductible`/`CurrentMileage`). Εμφάνιση στην κάρτα
ταυτότητας: `${emp.PriorExperience} έτη` αντί για ωμό κείμενο.

**Fix: πίνακας Οχημάτων στο tab Στόλος — στήλες όχι σωστά κεντραρισμένες
— ΕΓΙΝΕ** (25/08/2026, ρητή αναφορά χρήστη με screenshot): ο πίνακας
`<table class="fl-table">` (`hub.html`, tab Στόλος) έχει `table-layout:fixed`
με ρητά ποσοστά πλάτους ανά στήλη μέσω `<colgroup>` — τα ποσοστά αθροίζονταν
σε 86% αντί για 100% (12 στήλες + 2 στήλες Ενεργειών, ξεχασμένο άθροισμα από
προηγούμενες σταδιακές προσθήκες πεδίων όπως Απαλλαγή/Χλμ/Ασφάλιση). Αυτό
προκαλούσε ασύμμετρη κατανομή πλάτους στο πραγματικό rendering — η πλειοψηφία
των στηλών ήταν επίσης εξ ορισμού αριστερά στοιχισμένες (μόνο η Κατάσταση
είχε ήδη `ta-center`), κάτι που έκανε τιμές μίας λέξης/badge (e-pass, ΚΤΕΟ,
Service, Απαλλαγή, Χλμ) να φαίνονται "κολλημένες" αριστερά αντί για
κεντραρισμένες κάτω από τον τίτλο της στήλης τους. Fix: (1) αναδιάταξη
`<colgroup>` ώστε τα 13 πλάτη να αθροίζουν ακριβώς σε 100% (7/7/9/7/6/9/9/9/6/5/10/8/8),
με περισσότερο χώρο στις στήλες με μεταβλητό/μεγαλύτερο κείμενο
(Ανατεθειμένος, Σημείωση, Ασφάλιση, Ενέργειες) και λιγότερο στις στήλες
σταθερού/σύντομου περιεχομένου· (2) προστέθηκε `class="ta-center"` στα
`<th>`/`<td>` των στηλών e-pass, ΚΤΕΟ, Service, Απαλλαγή, Χλμ (ίδιο μοτίβο
με την ήδη κεντραρισμένη Κατάσταση) — Πινακίδα/Τύπος/Ανατεθειμένος/Σημείωση/
Ασφάλιση παραμένουν αριστερά στοιχισμένες (μεταβλητού μήκους ελεύθερο
κείμενο, το κεντράρισμα εκεί θα δυσκόλευε την ανάγνωση). Καμία αλλαγή στα
tables Εξοπλισμού/e-pass (`eq-list`/`ep-list`) — ελέγχθηκαν, τα δικά τους
`<colgroup>` αθροίζουν ήδη σωστά σε 100%, δεν είχαν το ίδιο πρόβλημα.
Καθαρά CSS/markup αλλαγή, καμία επίδραση backend/δεδομένων.

**Ταχύτητα: παραλληλοποίηση ανεξάρτητων Sheets reads — ΕΓΙΝΕ** (25/08/2026,
ρητό αίτημα χρήστη "μπορούμε να κάνουμε πιο γρήγορο το portal;"). Διάγνωση:
το backend δεν κάνει κανένα caching δεδομένων Sheet (μόνο το OAuth access
token + το gid ανά φύλλο cache-άρονται στο KV, βλ. `src/sheets.js`) — κάθε
request διαβάζει ζωντανά το/τα φύλλα που χρειάζεται από το Sheets API. Οι
περισσότεροι GET handlers στο `src/index.js` ήδη έτρεχαν τα ανεξάρτητα reads
τους παράλληλα (`Promise.all` — π.χ. `handleFleetList`, `handleEquipmentList`,
`handleEpassList`, `handleEmployeeProfile`, `listAvailabilityQueries`/
`listMyAvailabilityQueries` στο `src/availability.js`), αλλά εντοπίστηκαν 5
hot-path συναρτήσεις που έκαναν 2-3 ανεξάρτητες αναγνώσεις φύλλων διαδοχικά
(sequential `await`) αντί παράλληλα — κάθε τέτοιο σημείο πρόσθετε αθροιστικά
τον χρόνο ενός επιπλέον Sheets API round-trip χωρίς λόγο, αφού το φιλτράρισμα
με βάση τα αποτελέσματα γίνεται ούτως ή άλλως ΜΕΤΑ, στη μνήμη:
- `getTeamLeaderData()` (`src/leaves.js`) — τροφοδοτεί το tab «Άδειες
  Ομάδας»/«Άδειες Team Leaders» + το nav badge εκκρεμοτήτων: TeamLeaders,
  Υπάλληλοι, Άδειες (μέσω `readLeavesRows`) διαβάζονταν το ένα μετά το άλλο.
- `computeAvailabilityList()` (`src/leaves.js`, κοινός πυρήνας για
  «Διαθεσιμότητα Τεχνικών» του Backoffice ΚΑΙ τη δεξαμενή του tab
  «Συνεργεία», μέσω `getTechniciansPool()`): Υπάλληλοι, TeamLeaders, Άδειες
  το ίδιο.
- `getEmployeeBalance()` (`src/leaves.js`, πολύ hot path — τροφοδοτεί «Οι
  Άδειές μου», Ημερολόγιο τεχνικού, Προφίλ Τεχνικού): Υπάλληλος + Άδειες.
- `getTechnicianLeaveProfile()` (`src/leaves.js`, Προφίλ Τεχνικού): το δικό
  του Υπάλληλος-lookup έτρεχε πριν το (ήδη παράλληλο εσωτερικά) κάλεσμα στο
  `getEmployeeBalance()` — έγιναν κι αυτά τα δύο παράλληλα μεταξύ τους.
- `getTechnicianHoldings()` (`src/assignments.js`, Αναζήτηση τεχνικού στο
  Εξοπλισμός + PDF παράδοσης/παραλαβής): Υπάλληλος + χρεώσεις τεχνικού.

Σε όλα τα παραπάνω ΔΕΝ άλλαξε καμία λογική/αποτέλεσμα — μόνο η σειρά
εκτέλεσης (`Promise.all` αντί διαδοχικών `await`), επαληθεύτηκε ότι το
φιλτράρισμα που εξαρτάται από τα αποτελέσματα συνεχίζει να τρέχει ΜΕΤΑ την
ολοκλήρωση όλων των reads. Ελέγχθηκαν επίσης `assignments.js`
(`listAssignmentsForEmployee`/`getVehicleCalendarEvents` — εκεί οι
διαδοχικές αναγνώσεις είναι ΣΚΟΠΙΜΑ conditional/lazy, π.χ. διαβάζει το
Στόλος ΜΟΝΟ αν ο τεχνικός έχει έστω μία χρέωση οχήματος· η παραλληλοποίηση
θα σήμαινε πάντα ένα επιπλέον, συχνά περιττό, API call — δεν αγγίχτηκαν),
καθώς και `crews.js`/`handover.js`/`epass.js`/`announcements.js`/
`analytics.js`/`reports.js` — καμία άλλη σημαντική διαδοχική αναγνωσιμότητα
βρέθηκε σε GET/list handlers (τα υπόλοιπα σημεία με πολλαπλά `readSheetAsObjects`
είναι είτε ξεχωριστές, άσχετες συναρτήσεις είτε write-paths χαμηλής συχνότητας
όπως `openAssignment`/`openEpassAssignment`, όπου δεν αξίζει το ρίσκο για το
όφελος). Ρητά ΔΕΝ προστέθηκε κανένα caching layer (short-TTL ή άλλο) σε αυτό
το πέρασμα — ο χρήστης επέλεξε ρητά το πιο συντηρητικό από τα δύο προτεινόμενα
επίπεδα (χωρίς ρίσκο stale δεδομένων). Αν χρειαστεί περαιτέρω βελτίωση
ταχύτητας στο μέλλον, το caching layer παραμένει η επόμενη, μεγαλύτερου
ρίσκου επιλογή — να ξαναζητηθεί ρητά.

**Code review ασφαλείας — ΕΓΙΝΕ** (25/08/2026, ρητό αίτημα χρήστη "θες να
κάνεις έναν έλεγχο στον κώδικα μήπως βρεις και τίποτα άλλο"). Δύο ευρήματα,
και τα δύο διορθώθηκαν (ρητή επιλογή χρήστη "διόρθωσε και τα δύο"):

1. **Χωρίς HTML-escaping σε ελεύθερο κείμενο τρίτου χρήστη (θεωρητικό stored
   XSS) — ΕΓΙΝΕ.** Το `hub.html` χτίζει πίνακες/κάρτες με απευθείας
   παρεμβολή `${...}` μέσα σε `innerHTML`/`document.write` template
   literals — υπήρχε ήδη `escapeHtml()` αλλά ΜΟΝΟ τοπικά μέσα στο
   `exportTableToPdf()` (only for PDF export), πουθενά αλλού. Πιο
   ενδιαφέρουσα περίπτωση: η «Σημείωση» μιας αίτησης άδειας/τηλεργασίας
   γράφεται από τον ΙΔΙΟ τον τεχνικό (χαμηλότερο επίπεδο εμπιστοσύνης) και
   εμφανιζόταν ανεπεξέργαστη στο ιστορικό αδειών του Προφίλ Τεχνικού και
   στην Αναζήτηση αδειών τεχνικού — και τα δύο βλέπονται από TL/Director
   (υψηλότερο επίπεδο εμπιστοσύνης/προνομίων) — δηλαδή ένας τεχνικός θα
   μπορούσε θεωρητικά να βάλει HTML/JS στη δική του σημείωση που θα έτρεχε
   μέσα στη συνεδρία του TL. Fix: νέα **global** `escapeHtml(v)` (πριν το
   `topbar()`, ίδια λογική με την τοπική έκδοση + επιπλέον escaping του `"`
   ώστε να είναι ασφαλής και μέσα σε HTML attribute `value="..."`),
   εφαρμόστηκε σε όλα τα σημεία που δείχνουν ελεύθερο κείμενο άλλου χρήστη:
   Σημείωση αίτησης άδειας/τηλεργασίας (Αναζήτηση αδειών τεχνικού + το PDF
   export της, Προφίλ Τεχνικού, «Αιτήσεις άδειας» — εκεί μόνο το
   `EmployeeName`, το πεδίο Σημείωση δεν εμφανίζεται σε αυτόν τον πίνακα),
   κείμενο ερωτήματος + όνομα δημιουργού στο «Ανακοινώσεις» (και οι δύο
   ρόλοι, TL/Director + τεχνικός, και στο Προφίλ Τεχνικού), τίτλος/κείμενο/
   όνομα δημοσίευσης στο «Ενημερώσεις/Οδηγίες», και Όνομα/Κατηγορία/Σειριακός/
   Σημείωση σε Εξοπλισμό/e-pass (κύριος πίνακας ΚΑΙ inline επεξεργασία
   γραμμής) και Σημείωση στον πίνακα Στόλου (κύριος πίνακας ΚΑΙ inline
   επεξεργασία). Ελέγχθηκε ότι το `.an-item-body` (Ενημερώσεις) έχει ήδη
   `white-space:pre-wrap` — το escaping δεν επηρεάζει τις αλλαγές γραμμής.
   Δεν αγγίχτηκαν πεδία που είναι ήδη ελεγχόμενα enum/dropdown (π.χ. `Type`,
   `Status`) ή ΗΔΗ σκόπιμα raw HTML (π.χ. τα `<span class="status-pill">`
   badges) — μόνο ελεύθερο κείμενο.
2. **Καμία προστασία brute-force στο login τεχνικού (PIN 4 ψηφίων) — ΕΓΙΝΕ.**
   Νέο sliding-window lockout στο `handleTechnicianLogin()` (`src/index.js`)
   — reuse του ήδη υπάρχοντος KV `TECHNICIAN_AUTH` (καμία νέα υποδομή), key
   `loginfail:<employeeId>` με `{count}` + `expirationTtl` 15 λεπτά (αυτό-
   καθαρίζεται). Μετά από **5 συνεχόμενες αποτυχημένες προσπάθειες** (λάθος
   PIN Ή άγνωστο employeeId) μέσα σε 15 λεπτά, το login απορρίπτεται με 429
   και σαφές μήνυμα, ανεξάρτητα αν το επόμενο PIN που θα δοκίμαζε ήταν
   σωστό. Καθαρίζεται αμέσως σε επιτυχές login (`clearLoginLockout()`).
   Σκόπιμα rate-limit ΑΝΑ `employeeId` (όχι ανά IP — πίσω από Cloudflare τα
   IP headers δεν είναι πάντα αξιόπιστο διακριτικό, και το ρεαλιστικό ρίσκο
   εδώ είναι κάποιος να δοκιμάζει PIN για ΣΥΓΚΕΚΡΙΜΕΝΟ ήδη γνωστό
   EmployeeID, όχι distributed brute-force σε τυχαία IDs — reuse του
   `getEmployeeById` ούτως ή άλλως ήδη γίνεται ανά employeeId παρακάτω στην
   ίδια συνάρτηση). Καμία αλλαγή στο staff Google SSO login (`/api/auth/
   staff-login`) — άσχετος μηχανισμός, ήδη πίσω από Cloudflare Access.

**Animated loading bar — ΕΓΙΝΕ** (25/08/2026, ρητό αίτημα χρήστη): στο tab
«Προφίλ Τεχνικού», η επιλογή τεχνικού έδειχνε μόνο το στατικό κείμενο
«Φόρτωση...» όσο περίμενε τα 2 παράλληλα fetch (`/api/employees/profile` +
`/api/employees/create`). Νέο γενικό `.loading-bar-*` CSS component (indeterminate
— μπάρα σταθερού πλάτους που γλιστράει με CSS animation, αφού δεν υπάρχει
πραγματικό ποσοστό προόδου ενός fetch) + `loadingBarHtml(label)` helper στο
`hub.html`, χτισμένο ΓΙΑ ΕΠΑΝΑΧΡΗΣΙΜΟΠΟΙΗΣΗ (όχι μόνο για αυτό το σημείο, αν
χρειαστεί αλλού στο μέλλον). Αρχικά εφαρμόστηκε ΜΟΝΟ στο `loadTechProfile()`
(ρητά ζητήθηκε εκεί πρώτα) — **επεκτάθηκε ΠΑΝΤΟΥ στο portal ΙΔΙΑ μέρα**
(25/08/2026, ρητή απαίτηση χρήστη «περασέ το παντού αυτό με την μπάρα και
την φόρτωση»): αντικαταστάθηκαν όλα τα υπόλοιπα σημεία με στατικό
«Φόρτωση...» κείμενο —
(α) οι 19 κοινές εμφανίσεις `content.textContent = 'Φόρτωση...'` στη βασική
συνάρτηση εναλλαγής tab (`openTool`/κάθε `renderXxx()`), που καλύπτουν όλες
σχεδόν τις καρτέλες (Οι Άδειές μου, Άδειες Ομάδας/Team Leaders, Στόλος,
Εξοπλισμός, e-pass, Συνεργεία, Ανακοινώσεις, Ενημερώσεις, Αναλυτικά,
Ιστορικό Ενεργειών, Backups, Αρχική, κ.ά.) — έγιναν
`content.innerHTML = loadingBarHtml('Φόρτωση...')`·
(β) το **στατικό αρχικό placeholder** `<div id="content" class="loading">`
(εμφανίζεται πριν προλάβει να τρέξει καθόλου JS, άρα δεν μπορεί να καλέσει
τη function — αντικαταστάθηκε με το ισοδύναμο hardcoded HTML της
`loadingBarHtml()`)·
(γ) η κάρτα «Έγγραφα παράδοσης (ψηφιακή υπογραφή)» (Εξοπλισμός, Αναζήτηση
τεχνικού) και
(δ) το εσωτερικό list box των modals «Μαζική αποστολή καλωσορίσματος»
(bulk-welcome technicians/staff selection) — και τα δύο περνούσαν raw HTML
string με το κείμενο, έγιναν `loadingBarHtml('Φόρτωση...')`.
Καμία αλλαγή backend/λογικής — καθαρά UI consistency pass. Fix παράλληλα: το `catch` block
της `loadTechProfile()` άδειαζε μόνο το μήνυμα σφάλματος αλλά ΟΧΙ το
`resultsEl.innerHTML` — σε περίπτωση σφάλματος (π.χ. δικτύου) η μπάρα θα
έμενε «κολλημένη» να κινείται επ' άπειρον κάτω από το μήνυμα σφάλματος·
προστέθηκε `resultsEl.innerHTML = ''` στο catch.

## Εκκρεμότητες

- **Email**: αλλάξαμε από Resend σε ένα μικρό **Apps Script mail relay** (`apps-script/EmailRelay.gs`, καλείται από `src/email.js` μέσω HTTP) — ρητή επιλογή χρήστη αντί για SendGrid/τρίτο πάροχο: καμία εγγραφή σε εξωτερική υπηρεσία, καμία αλλαγή DNS, στέλνει σαν πραγματικό Google account. Θεωρείται εξαίρεση στο "δεν υπάρχει Apps Script" γιατί δεν αγγίζει καθόλου Sheets/auth/business logic (βλ. σημείωση στην αρχή του αρχείου). Ο κώδικας στον Worker είναι πλήρης, λείπουν ΜΟΝΟ τα 2 χειροκίνητα βήματα από τον χρήστη: (1) deploy το Apps Script (βλ. SETUP.md §2.1) και (2) τα secrets `EMAIL_RELAY_URL` + `EMAIL_RELAY_SECRET` — χωρίς αυτά, όλες οι ενέργειες καταγράφονται κανονικά αλλά δεν φεύγει καμία ειδοποίηση (σιωπηλό no-op, καμία διακοπή λειτουργίας).
  Οι εβδομαδιαίες/μηνιαίες αναφορές (`src/reports.js`, Cron Triggers στο `wrangler.toml`) είναι έτοιμες κώδικας-wise, ίδιο μπλοκάρισμα (ίδια secrets). Σχεδιαστική επιλογή χωρίς ρητή απαίτηση: κάθε TL παίρνει δική του αναφορά για τη δική του ομάδα (άδειες εντός περιόδου + ΚΤΕΟ/service alerts) — όχι συγκεντρωτική αναφορά σε κάποιο owner email, γιατί δεν υπήρχε τέτοιος ρόλος ορισμένος πουθενά. Αν χρειάζεται κάτι διαφορετικό, πες το.
  **Εβδομαδιαία ειδοποίηση ΚΤΕΟ/service στον Director — ΕΓΙΝΕ** (πρόταση Claude, αποδεκτή από χρήστη): `sendFleetComplianceAlert()` (`src/reports.js`), καλείται ΕΠΙΠΛΕΟΝ (όχι αντί) του per-TL loop μέσα στο `sendWeeklyReports()`. Ξεχωριστό, αποκλειστικό email (ΟΧΙ αντικατάσταση του section που ήδη υπάρχει μέσα σε κάθε report TL) προς όλους τους Director(s), με την ίδια λίστα `fleetAlertLines()` που ήδη υπολογίζεται για το per-TL report — η διαφορά είναι σαφής υπεύθυνος + ξεχωριστό θέμα email (compliance/νομικό), όχι θαμμένο ανάμεσα σε αιτήσεις αδειών. Καμία ειδοποίηση αν δεν υπάρχει κανένα όχημα με ΚΤΕΟ/service που λήγει, ή αν δεν υπάρχει Director καταχωρημένος (σιωπηλό no-op και στις δύο περιπτώσεις).
  **Μηνιαία αναφορά αδειών για τον λογιστή — ΕΓΙΝΕ** (ρητή απαίτηση χρήστη): `sendAccountantMonthlyReport()` (`src/reports.js`), καλείται ΕΠΙΠΛΕΟΝ (όχι αντί) του `sendTeamReport()` ανά TL μέσα στο `sendMonthlyReports()` — το μηνιαίο των TL μένει όπως ήταν. Ένα ενιαίο email με ΟΛΟΥΣ τους τεχνικούς μαζί (όλες οι ομάδες, όχι scoped ανά TL), μόνο η λίστα εγκεκριμένων αδειών (χωρίς πλήθος τεχνικών/εκκρεμείς/ΚΤΕΟ-service — άσχετα με τον σκοπό), για καταχώρηση στη μισθοδοσία. **Παραλήπτης: ο/οι Director(s)** (όλες οι γραμμές του φύλλου `Directors`, ίδιο sheet με τον ρόλο Director — καμία νέα ρύθμιση) — ρητή απόφαση χρήστη: ο Worker ΔΕΝ στέλνει τίποτα απευθείας σε λογιστή, ο Director προωθεί το email ο ίδιος χειροκίνητα. Αν δεν υπάρχει κανένας Director καταχωρημένος, σιωπηλό no-op. Νέα `getApprovedLeavesInRange()` (`src/leaves.js`) — company-wide εκδοχή του φιλτραρίσματος περιόδου, ίδιο μοτίβο με το `leavesInRange()` του TL report. Κάθε άδεια εμφανίζεται με την ΠΛΗΡΗ περίοδό της (StartDate–EndDate), όχι περικομμένη στα όρια του μήνα — ρητή απαίτηση χρήστη: μια άδεια που ξεκινά τέλος μήνα και συνεχίζεται στον επόμενο φαίνεται ολόκληρη, μία φορά (θα ξαναφανεί επίσης, με τα ίδια πλήρη στοιχεία, και στην αναφορά του επόμενου μήνα αφού τέμνει και εκείνη την περίοδο — αποδεκτό, φαίνονται οι ίδιες ημερομηνίες και τις δύο φορές, όχι κομμάτια). Περιλαμβάνει όλους τους τύπους άδειας (και Τηλεργασία) χωρίς φιλτράρισμα κατά τύπο — φαίνεται ο Τύπος ανά γραμμή και αποφασίζει ο λογιστής τι καταχωρεί (δεν υπήρχε ρητή οδηγία να εξαιρεθεί η Τηλεργασία, οπότε προτιμήθηκε να συμπεριληφθεί παρά να κρυφτεί σιωπηλά).
  **Layout λίστας αδειών — ΕΓΙΝΕ** (ρητή απαίτηση χρήστη, δύσκολο να διαβαστεί το αρχικό): αντί για ενιαίο συμπιεσμένο value μέσα σε ένα `rows` row, η λίστα αδειών φαίνεται πλέον ως ξεχωριστή γραμμή ανά άδεια (όνομα + περίοδος/ημέρες αριστερά, χρωματιστό badge τύπου δεξιά) — `leaveListHtml()`/`leaveTypeColor()` στο `src/reports.js`. Χρειάστηκε νέο γενικό slot `listHtml` στο `emailTemplate()` (`src/email.js`, μετά το `rows` κουτί, πριν το CTA) — προαιρετικό raw HTML block, το email.js παραμένει γενικό/άσχετο με άδειες. Επαναχρησιμοποιήσιμο και από άλλα μελλοντικά reports με πολλαπλές εγγραφές (π.χ. αν χρειαστεί ποτέ και το `sendDailyChargesDigest`, που έχει το ίδιο μοτίβο συμπιεσμένης λίστας — δεν αγγίχτηκε, δεν ζητήθηκε). Το `footer` (ήταν: "Αναφορά για καταχώρηση αδειών στη μισθοδοσία.") αφαιρέθηκε από αυτό το email — ρητή απαίτηση χρήστη.
  **Επισύναψη δικαιολογητικών αναρρωτικής — ΕΓΙΝΕ** (ρητή απαίτηση χρήστη): `sickLeaveAttachments()` (`src/reports.js`) μαζεύει τα PDF δικαιολογητικά (μόνο άδειες `Type === "Αναρρωτική"` με ανεβασμένο `FileKey`) και τα περνάει ως `attachments` στο `sendEmail()`. Νέα `getLeaveAttachmentByFileKey()` (`src/leaves.js`) — απευθείας ανάγνωση από το KV `LEAVE_FILES` ΧΩΡΙΣ το per-request owner/TL authorization check του υπάρχοντος `getLeaveAttachmentById()` (δεν εφαρμόζεται εδώ: cron job, όχι HTTP request από συγκεκριμένο χρήστη), όχι εκτεθειμένη σε endpoint. Αν μια αναρρωτική δεν έχει ανεβασμένο δικαιολογητικό, ή λείπει το αρχείο, ή λείπει το KV binding, αγνοείται σιωπηλά — το email φεύγει κανονικά. Απαιτεί υποστήριξη attachments στο `sendEmail()`/`EmailRelay.gs` (βλ. §Email relay — νέο deployment step εκκρεμεί).
  **Reply-To ανά ενέργεια — ΕΠΙΒΕΒΑΙΩΘΗΚΕ και υλοποιήθηκε** (η παλιά εκκρεμότητα "να το δούμε αύριο" έκλεισε): τα emails ενεργειών (νέος τεχνικός, χρέωση, νέα αίτηση/απόφαση άδειας) φαίνονται σαν να τα έστειλε αυτός που έκανε την ενέργεια — η πραγματική τεχνική αποστολή παραμένει πάντα ο relay λογαριασμός (Apps Script `MailApp` δεν μπορεί να "γίνεται" δεκάδες πραγματικοί αποστολείς χωρίς verified Gmail aliases ανά χρήστη), αλλά μπαίνει `Reply-To` = email του actor + εμφανιζόμενο όνομα = το όνομά του (`resolveActorName()` στο `src/email.js`, ψάχνει TeamLeaders -> Backoffice -> Υπάλληλοι). Οι εβδομαδιαίες/μηνιαίες αναφορές (`src/reports.js`) ΔΕΝ περνάνε Reply-To — μένουν στον προεπιλεγμένο (δικό σου) λογαριασμό, όπως ζητήθηκε.
- **Κανάλι ειδοποιήσεων: αποκλειστικά email.** Ρητή απόφαση χρήστη — όλες οι ειδοποιήσεις χτίστηκαν πάνω στο ίδιο `sendEmail()`/Apps Script relay, όχι κάποιο ξεχωριστό in-app/push notification σύστημα.
- **Ειδοποιήσεις υποβολής/έγκρισης άδειας — ΥΛΟΠΟΙΗΘΗΚΕ** (`src/leaves.js`): email στον TL (ΚΑΙ στον backup του αν είναι ενεργό το "Λείπω" — βλ. `additionalRecipientsForAway()` παρακάτω) όταν υποβάλλεται νέα αίτηση άδειας/τηλεργασίας· email στον τεχνικό (αν έχει καταχωρημένο email) όταν η αίτησή του εγκρίνεται/απορρίπτεται. Reply-To = actor (αυτός που υπέβαλε/αποφάσισε).
  **Backup TL ειδοποίηση/πρόσβαση όταν "Λείπω" — ΕΓΙΝΕ** (ρητή απαίτηση χρήστη, έκλεισε το παλιό γνωστό κενό): νέα `additionalRecipientsForAway(env, approverEmail)` στο `src/leaves.js` — ίδιο query pattern με `isAuthorizedForTeam()` (BackupEmail + Away === "TRUE" στο φύλλο TeamLeaders), ΠΡΟΣΘΕΤΕΙ τον backup ως επιπλέον παραλήπτη (CC-style, `to: [approver.Email, ...ccBackup]`) — ΔΕΝ αντικαθιστά τον κύριο TL, που μπορεί ακόμα να ελέγχει το email του περιστασιακά. Εφαρμόστηκε σε ΚΑΙ τα τρία σημεία ειδοποίησης TL: `submitLeaveRequest()`, `submitTeleworkRequest()`, `uploadLeaveAttachment()`. Βλ. και Επόμενα ζητούμενα #1 για το αντίστοιχο fix στο δικαίωμα λήψης του δικαιολογητικού.
- Ο χρήστης είπε ότι θα αποφασίσει αργότερα τι άλλο θα βλέπει το Backoffice (επιλογές που είχαν δοθεί: προβολή όλων των αδειών, διαχείριση στόλου, προβολή όλων των χρεώσεων, πλήρη έγκριση — καμία δεν ενεργοποιήθηκε ακόμα).
- **Ποιος εγκρίνει την άδεια ενός Team Leader; — ΕΓΙΝΕ (μόνιμη λύση):** νέος ρόλος **Director**, βλ. Ρόλοι § Director παρακάτω. Ο μηχανισμός δρομολόγησης ΔΕΝ άλλαξε — παραμένει το `TeamLeaderEmail` στη self-row του TL (φύλλο Υπάλληλοι) — απλά το `submitLeaveRequest()` τώρα κάνει fallback σε `getDirectorByEmail()` όταν το email εκεί δεν αντιστοιχεί σε πραγματικό TL. Το χειροκίνητο βήμα ΠΑΡΑΜΕΝΕΙ (βλ. §Χειροκίνητα βήματα): κάθε self-row TL στο Υπάλληλοι χρειάζεται `TeamLeaderEmail` = το email ενός καταχωρημένου Director.
- **Νέα θέματα προς συζήτηση** (ενημέρωση προόδου):
  1. ~~Tab Τηλεργασίας~~ — ΕΓΙΝΕ: βλ. Ρόλοι § Backoffice.
  2. ~~e-pass καταχώρηση και χρέωση~~ — ΕΓΙΝΕ: βλ. Ρόλοι § Team Leader.
  3. ~~Αξιολόγηση στους TLs~~ — ΕΓΙΝΕ: `evaluation.html`, βλ. Ρόλοι § Team Leader.
  4. ~~Δίπλωμα οδήγησης στη φόρμα Νέος Τεχνικός~~ — ΕΓΙΝΕ.
  5. ~~Νέος ρόλος "Director"~~ — ΕΓΙΝΕ: βλ. Ρόλοι § Director.
  6. ~~Reset PIN τεχνικού ("Ξέχασες τον κωδικό;")~~ — ΕΓΙΝΕ: βλ. Ρόλοι § Νέος Τεχνικός. **Ρητή σχεδιαστική απόφαση χρήστη μετά από συζήτηση**: staff-assisted ΜΟΝΟ (TL/Backoffice/Director κάνει reset από το hub) — ΚΑΝΕΝΑ self-service "forgot PIN" στο login. Λόγος: πολλοί τεχνικοί μοιράζονται το ίδιο placeholder email στην πράξη (ήδη τεκμηριωμένο αλλού σε αυτό το αρχείο ως λόγος που το email θεωρείται αναξιόπιστο διακριτικό) — ένα ανοιχτό "δώσε EmployeeID, πάρε PIN με email" θα επέτρεπε σε τεχνικό να πάρει (χωρίς να το καταλάβει ο άλλος) το νέο PIN συναδέλφου που μοιράζεται το ίδιο inbox. Βλ. σημείωση παρακάτω για πότε γίνεται ασφαλές να προστεθεί self-service.
- **Όταν όλοι οι τεχνικοί αποκτήσουν εταιρικό email** (μελλοντικό, όχι τώρα): σήμερα το login τεχνικού είναι αποκλειστικά employeeID+PIN (KV `TECHNICIAN_AUTH`), εντελώς ανεξάρτητο από Google SSO/`resolveStaffRole()` — το `/hub` παραμένει σκόπιμα εκτός Cloudflare Access ακριβώς επειδή οι τεχνικοί δεν έχουν λογαριασμό Google. Όταν αποκτήσουν, 3 επιλογές συζητήθηκαν: (1) email μόνο για ειδοποιήσεις, login παραμένει PIN — καμία αλλαγή αρχιτεκτονικής· (2) προαιρετικό Google SSO δίπλα στο PIN (χρειάζεται νέος ρόλος "technician" στο `resolveStaffRole()` + προσοχή στο όριο 5 Access paths)· (3) πλήρης μετάβαση σε Google SSO, κατάργηση PIN. Καμία απόφαση ακόμα — ρώτα τον χρήστη ποια θέλει όταν έρθει η ώρα. Σημείωση: το εταιρικό email δεν ξεμπλοκάρει από μόνο του τις αποστολές — αυτό εξαρτάται από το deployment του Apps Script relay παραπάνω, ανεξάρτητο θέμα. Μόλις γίνει αυτή η μετάβαση (μοναδικό email ανά τεχνικό, χωρίς μοιρασμένα placeholders), γίνεται ασφαλές να προστεθεί και self-service "forgot PIN" στο login — ρώτα πρώτα.

## Email relay — τρέχουσα κατάσταση (ζωντανό)

**Μεταφορά στο εταιρικό λογαριασμό — ΕΓΙΝΕ** (11/08/2026, ρητή απαίτηση χρήστη, ήταν item #5 στα «Νέα ζητούμενα 10/08/2026»): το relay τρέχει πλέον κάτω από το εταιρικό `s.xronis@optikitec.gr` (Apps Script Web App, Execute as: Me, Who has access: Anyone), ΟΧΙ πια το παλιό προσωπικό Gmail. Το εμπόδιο (Workspace org policy που μπλόκαρε "Anyone" εξωτερική πρόσβαση σε Apps Script Web Apps) ήταν ήδη χαλαρωμένο όταν το ελέγξαμε — δεν χρειάστηκε αλλαγή στο Admin console. Νέα secrets `EMAIL_RELAY_URL`/`EMAIL_RELAY_SECRET` μπήκαν μέσω `wrangler secret put` (νέο `RELAY_SECRET` script property στο Apps Script project). Reply-To ανά ενέργεια + badge/CTA design παραμένουν ως είχαν (βλ. §Εκκρεμότητες παραπάνω).
**Όριο**: ανέβηκε από 100 σε 1.500–2.000 παραλήπτες/ημέρα (Workspace αντί για consumer Gmail) — σχετικό για τον όγκο emails του daily charges digest (#6 παρακάτω) και τις υπόλοιπες αναφορές.
**Συνημμένα (attachments)**: το νέο deployment έγινε με την τρέχουσα έκδοση του `apps-script/EmailRelay.gs`, που ήδη περιλαμβάνει την υποστήριξη `attachments` (base64 → `Blob` → `MailApp.sendEmail()`) — άρα καλύφθηκε αυτόματα μαζί με τη μεταφορά, χωρίς ξεχωριστό βήμα.

## Επόμενα ζητούμενα (ζητήθηκαν, ΔΕΝ έχει ξεκινήσει υλοποίηση ακόμα)

Λίστα από τον χρήστη, όλα προς σχεδιασμό/υλοποίηση στο επόμενο chat. Σημειώσεις/ανοιχτά ερωτήματα ανά σημείο (δικές μου παρατηρήσεις, να επιβεβαιωθούν με τον χρήστη πριν την υλοποίηση):

1. ~~Ανάρτηση PDF αναρρωτικής άδειας~~ — ΕΓΙΝΕ (κώδικας-wise): ο τεχνικός/staff ανεβάζει το δικαιολογητικό ΜΕΤΑ την υποβολή, από το tab «Οι Άδειές μου» (ρητή απόφαση χρήστη — όχι υποχρεωτικά τη στιγμή της αίτησης). Ισχύει μόνο για `Type === "Αναρρωτική"` (ρητή απόφαση χρήστη, όχι γενικό μηχανισμό για όλους τους τύπους άδειας) — ο τύπος υπήρχε ήδη στα dropdowns, καμία αλλαγή σχήματος εκεί. Μόνο PDF (`src/leaves.js` → `LEAVE_ATTACHMENT_ALLOWED_TYPES`), μέγιστο 20MB.
   - `uploadLeaveAttachment`/`getLeaveAttachmentById` στο `src/leaves.js`· endpoints `POST /api/leaves/attachment/upload` (raw bytes) και `GET /api/leaves/attachment?id=...` στο `src/index.js`. Ίδιο μοτίβο αποθήκευσης με τα συνημμένα Ενημερώσεων (raw bytes σε Cloudflare KV, εκτός Sheet) αλλά σε **δικό του namespace `LEAVE_FILES`** (δημιουργήθηκε μέσω Cloudflare MCP, δηλωμένο στο `wrangler.toml` — ενεργοποιείται αυτόματα στο επόμενο deploy).
   - Λήψη περιορισμένη σε: τον ίδιο τον αιτούντα ή τον TL της συγκεκριμένης αίτησης (`TeamLeaderEmail` στη γραμμή) — Backoffice ΔΕΝ έχει πρόσβαση, συνεπές με απόφαση #1. `getLeaveAttachmentById()` κάνει πλέον reuse `isAuthorizedForTeam()` (αντί για strict ισότητα email) — αν ο κύριος TL είναι "Λείπω", ο backup του βλέπει/κατεβάζει κανονικά το δικαιολογητικό όσο διαρκεί η κάλυψη.
   - Email στον TL μόλις ανέβει (`sendEmail`, όχι Reply-To εδώ — δεν υπάρχει "actor" διαφορετικός από τον ίδιο τον τεχνικό) — ΚΑΙ στον backup του αν είναι ενεργό το "Λείπω" (`additionalRecipientsForAway()`, βλ. Εκκρεμότητες παρακάτω).
   - Ένδειξη στον TL: badge «📄 δικαιολογητικό» (link) ή «χωρίς δικαιολογητικό» δίπλα στον τύπο, στις λίστες «Προς έγκριση» και «Πρόσφατες αποφάσεις» του tab Άδειες Ομάδας (`hub.html` → `sickAttachmentBadge()`).
   - Χειροκίνητο βήμα εκκρεμεί (βλ. §Χειροκίνητα βήματα): προσθήκη στηλών `FileKey`/`FileName`/`FileType` στο φύλλο `Άδειες`.
2. ~~Τηλεργασία στο ημερολόγιο TL~~ — **ΕΠΙΒΕΒΑΙΩΘΗΚΕ** (15/08/2026, έλεγχος κώδικα, καμία αλλαγή χρειάστηκε): το `calendarLeaves` μέσα στο `getTeamLeaderData()` (`src/leaves.js`) περιλαμβάνει ΗΔΗ όλες τις εγκεκριμένες/εκκρεμείς αιτήσεις χωρίς φιλτράρισμα τύπου (η Τηλεργασία εξαιρείται μόνο από τον υπολογισμό `daysPerEmployee`/υπόλοιπο, όχι από το ημερολόγιο) — και το `hub.html` (`dayLeaves.map` στο μηνιαίο ημερολόγιο ομάδας) έχει ήδη ξεχωριστό `cal-chip-telework` για `Type === 'Τηλεργασία'` + αντίστοιχο legend item. Δούλευε ήδη σωστά, απλά δεν είχε επιβεβαιωθεί ρητά.
3. ~~Νέος ρόλος "Director"~~ — ΕΓΙΝΕ: βλ. Ρόλοι § Director. Scope (ρητή απόφαση χρήστη): πλήρης εποπτεία, ό,τι βλέπει Backoffice + Team Leader μαζί.
4. ~~Έγκριση/απόρριψη άδειας απευθείας από το email~~ — ΕΓΙΝΕ: `GET/POST /api/leaves/decide-link` (`src/index.js`), tokens minted στο `src/leaves.js` (`decideLinksHtml`), signed με `src/tokens.js` (νέο module, εξήχθη από το `src/index.js` ώστε να το χρησιμοποιεί και το `leaves.js` χωρίς κυκλική εξάρτηση) — ίδιο secret `LINK_SECRET` με το integration token.
   - **Ασφάλεια (σχεδιαστικές αποφάσεις χωρίς ρητή απαίτηση, λογικές προεπιλογές):** το `GET` ΔΕΝ αποφασίζει ΠΟΤΕ — δείχνει μόνο μια αυτόνομη σελίδα επιβεβαίωσης (εκτός hub.html, χωρίς login/JS) με ένα κουμπί «Ναι, Έγκριση/Απόρριψη» που κάνει `POST` στο ίδιο URL· αυτό προστατεύει από email clients/εταιρικά security scanners που κάνουν prefetch σε GET links (θα ενεργοποιούσαν αλλιώς κατά λάθος απόφαση χωρίς κανένα ανθρώπινο κλικ — γνωστό πρόβλημα με "one-click" email actions). Λήξη token: 14 ημέρες (μετά χρειάζεται κανονικό login). Δύο ξεχωριστά tokens ανά αίτηση (ένα approve, ένα reject), το καθένα με σταθερή απόφαση — όχι επιλογή τη στιγμή του κλικ. Το token payload έχει `type: "leave-decide"` ώστε να μην μπορεί να γίνει replay εδώ ένα session/integration token (ή το αντίστροφο), αφού μοιράζονται το ίδιο `LINK_SECRET`/σχήμα.
   - Replay/idempotency: reuse του υπάρχοντος `decideLeaveRequest()` guard (`Status !== "Εκκρεμεί"`) — δεύτερο κλικ ή δεύτερος σύνδεσμος (π.χ. Απόρριψη μετά από Έγκριση) απλά δείχνει "Η αίτηση έχει ήδη απαντηθεί", καμία διπλή ενέργεια.
   - Καμία υποστήριξη σημείωσης απόφασης μέσω email (ίδιο με το υπάρχον TL UI, που ήδη δεν στέλνει `decisionNote`).
   - Ισχύει για `submitLeaveRequest` πάντα. Για `submitTeleworkRequest` με ΜΙΑ ημέρα (μία αίτηση), ίδιο single-request token/`decideLinksHtml`. **Ομαδική τηλεργασία (πολλαπλές μη-συνεχόμενες ημέρες, κοινό GroupID) — ΕΓΙΝΕ**: νέο token type `"leave-decide-group"` (`buildDecideLinkGroupToken`/`decideLinksHtmlGroup` στο `src/leaves.js`) με κουμπιά «Έγκριση/Απόρριψη όλων», αποφασίζει ΟΛΕΣ τις ημέρες της υποβολής μαζί μέσω `decideLeaveRequestGroup()` — ίδιο μοτίβο επιβεβαίωσης GET→POST, ίδιο idempotency guard (αγνοεί σιωπηλά ό,τι έχει ήδη απαντηθεί). `verifyDecideLinkToken()`/`handleLeaveDecideLinkConfirm`/`handleLeaveDecideLinkSubmit` (`src/index.js`) πλέον διακλαδίζονται στο `payload.type` για να ξέρουν ποιο από τα δύο είναι.
   - Λειτουργεί αυτόματα και για Director (ίδιο `decideLeaveRequest`/`decideLeaveRequestGroup`/`approver.Email`, καμία διάκριση).
   - Αν λείπει το `LINK_SECRET`, τα κουμπιά one-click απλά δεν εμφανίζονται στο email (σιωπηλά ανενεργό, το κανονικό CTA προς `/hub` παραμένει) — καμία διακοπή λειτουργίας.
5. ~~Κατηγορία στο tab "Εξοπλισμός" σε dropdown~~ — ΕΓΙΝΕ: `listEquipmentCategories()` (`src/equipment.js`) μαζεύει τις μοναδικές τιμές `Category` ήδη καταχωρημένες στα αντικείμενα (χωρίς hardcoded λίστα, ίδιο μοτίβο με το Περιοχή/Team στη φόρμα Νέος Τεχνικός) — `GET /api/equipment` τις επιστρέφει μαζί με `items`/`drivers`. Στο `hub.html`, τόσο η φόρμα προσθήκης όσο και η inline επεξεργασία γραμμής έχουν dropdown + επιλογή "Άλλη κατηγορία..." (ελεύθερο κείμενο εμφανίζεται μόνο τότε). Καμία αλλαγή Sheet — το `Category` πεδίο υπήρχε ήδη.
6. ~~Συγκεντρωτικό daily email χρεώσεων στις 19:00~~ — ΕΓΙΝΕ: νέο cron trigger `"0 16 * * *"` (`wrangler.toml`), καλεί `sendDailyChargesDigest()` (`src/reports.js`).
   - **Ερώτημα/απόφαση (ρητή επιβεβαίωση χρήστη, μετά από πρόταση):** αντικαθιστά ΜΟΝΟ την άμεση ειδοποίηση του **TL** ανά χρέωση — ο **τεχνικός** εξακολουθεί να παίρνει το δικό του άμεσο email κανονικά (πραγματικά χρήσιμο real-time, π.χ. "μόλις σου χρεώθηκε το όχημα Χ"). Λόγος: μείωση του όγκου emails λόγω του ορίου 100 παραλήπτες/ημέρα (consumer Gmail relay) — όλα τα TL pings μιας ημέρας γίνονται 1 email, χωρίς να χαθεί η χρήσιμη ειδοποίηση του τεχνικού. `openAssignment`/`openEpassAssignment` (`src/assignments.js`) πλέον στέλνουν `to: [emp.Email]` μόνο (όχι πια `emp.TeamLeaderEmail`) — ο TL εμφανίζεται στο σώμα του email πληροφοριακά ("Team Leader: ...") αλλά δεν ειδοποιείται.
   - **Περιεχόμενο (ρητή απόφαση χρήστη):** μόνο η σημερινή κίνηση (τι χρεώθηκε/αποχρεώθηκε ΣΗΜΕΡΑ), ΟΧΙ όλες οι τρέχουσες ανοιχτές χρεώσεις — `listTodaysChargesForEmployees()` (`src/assignments.js`) φιλτράρει με `ChargedAt === today || ReleasedAt === today`. Αν ένας TL δεν είχε καμία κίνηση σήμερα, δεν στέλνεται τίποτα (κανένα κενό digest).
   - Team roster ανά TL μέσω `getTeamLeaderData()` (ίδιο με weekly/monthly reports) — αυτόματα καλύπτει και backup TL coverage.
   - **Ώρα/DST:** `"0 16 * * *"` = 19:00 Αθήνας ΜΟΝΟ το καλοκαίρι (EEST, UTC+3). Το χειμώνα (EET, UTC+2) θα τρέξει στις 18:00 Αθήνας — τα Cloudflare Cron Triggers δεν έχουν DST-aware timezone, ίδιος αποδεκτός περιορισμός με τα υπάρχοντα weekly/monthly cron.
   - Ίδιο μπλοκάρισμα με όλα τα άλλα emails: no-op μέχρι να μπουν τα `EMAIL_RELAY_URL`/`EMAIL_RELAY_SECRET` secrets.
7. ~~Αναζήτηση τεχνικού στο tab "Εξοπλισμός" + export PDF παράδοσης/παραλαβής~~ — ΕΓΙΝΕ: βλ. Ρόλοι § Team Leader. Το PDF δείχνει ΟΛΑ όσα έχει πάνω του ο τεχνικός (όχι μόνο εξοπλισμό) και δημιουργείται με browser print (`window.print()`), όχι με το `pdf` skill/backend όπως είχε αρχικά σκεφτεί — συνεπές με το ήδη υπάρχον πρότυπο (`evaluation.html`, export ιστορικού αδειών).

## Νέα ζητούμενα (10/08/2026, ζητήθηκαν — ΔΕΝ έχει ξεκινήσει υλοποίηση ακόμα)

Νέα λίστα από τον χρήστη (ξεχωριστή από την προηγούμενη §Επόμενα ζητούμενα, που έχει πλέον ολοκληρωθεί εξ' ολοκλήρου). Ρητή οδηγία χρήστη: μόνο καταγραφή προς το παρόν — **καμία υλοποίηση δεν έχει ξεκινήσει**, ο χρήστης θα πει σε επόμενο chat με ποιο σημείο ξεκινάμε. Σημειώσεις/ανοιχτά ερωτήματα ανά σημείο (δικές μου παρατηρήσεις, να επιβεβαιωθούν με τον χρήστη πριν την υλοποίηση):

1. ~~Ομαδοποίηση σε dropdown lists ανάλογα με τα συνεργεία~~ — **ΕΓΙΝΕ, σε περιορισμένο scope** (13/08/2026): μετά από διευκρίνιση ο χρήστης απάντησε απλά «βάλε τα ονόματα αλφαβητικά» — άρα το πραγματικό ζητούμενο αποδείχτηκε πιο στενό από την αρχική διατύπωση. Έλεγχος έδειξε ότι ΟΛΑ τα dropdown επιλογής τεχνικού (Προφίλ Τεχνικού, Εξοπλισμός/Στόλος driver dropdowns, Νέος Τεχνικός TL-select, Reset PIN, δεξαμενή Συνεργείων) ήταν ΗΔΗ αλφαβητικά ταξινομημένα (`computeAvailabilityList`/`listDrivers`/`listEquipmentDrivers`/`listTeamLeaders` κάνουν όλα `.sort(...localeCompare(..., "el"))`). Το ΜΟΝΟ σημείο που δεν ήταν ταξινομημένο: μέσα σε ΚΑΘΕ κάρτα συνεργείου στο tab Συνεργεία, τα ήδη ανατεθειμένα μέλη εμφανίζονταν με τη σειρά που σύρθηκαν (drag order), όχι αλφαβητικά — νέα `crewsSortedMembers()` στο `hub.html` ταξινομεί ΜΟΝΟ την προβολή (η αποθηκευμένη σειρά στο `memberIds` JSON παραμένει ως έχει, καμία αλλαγή backend/Sheet). Αν χρειάζεται κάτι ευρύτερο (π.χ. πραγματική ομαδοποίηση ανά μόνιμη ανάθεση συνεργείου, όχι μόνο αλφαβητική σειρά), να ζητηθεί ρητά.
2. **Στην «Αρχική»: πάνω από τα tiles, σύνοψη από «Οι Άδειές μου».** Ρητά χαρακτηρισμένο από τον χρήστη ως **«Θέμα προς συζήτηση»** — όχι ακόμα αποφασισμένο απαίτημα. Ιδέα: κάθε TL/Backoffice/Director βλέπει επίσης το δικό του προσωπικό ισοζύγιο αδειών με μια ματιά στην Αρχική, χωρίς να χρειάζεται να πάει στο ξεχωριστό tab «Οι Άδειές μου». Θα χρειαστεί σχεδιαστική απόφαση για το πώς συνυπάρχει οπτικά με τα υπάρχοντα tiles (βλ. `renderDashboard()` στο `hub.html`) — reuse του `getEmployeeBalance()` που ήδη υπάρχει.
3. ~~Προσθήκη προσωπικών στοιχείων τεχνικού~~ — **ΕΓΙΝΕ**: ΑΜΑ, ΑΜΚΑ, τηλέφωνο, διεύθυνση, ημερομηνία πρόσληψης, χρόνια στην εταιρεία (υπολογιζόμενο client-side, δεν αποθηκεύεται), βιογραφικό/ταυτότητα-διαβατήριο/άδεια διαμονής (συνημμένα), τεχνική κατάρτιση (ελεύθερο κείμενο) — και στο «Προφίλ Τεχνικού» και στη φόρμα «Νέος Τεχνικός». Λεπτομέρειες, access control (TL+Director μόνο για ανάγνωση — ρητή απόφαση χρήστη μετά από ερώτηση λόγω GDPR) και το χειροκίνητο βήμα Sheet: βλ. Ρόλοι § Team Leader → Προφίλ Τεχνικού και §Χειροκίνητα βήματα.
4. ~~Στόλος Οχημάτων~~ — **ΕΓΙΝΕ** (11/08/2026): η χρέωση/αποχρέωση οχήματος αποδείχτηκε ότι ΗΔΗ περνούσε από τον μηχανισμό `Χρεώσεις` (καμία αλλαγή χρειάστηκε εκεί, βλ. Ρόλοι § Team Leader → Στόλος Οχημάτων για διόρθωση της παλιάς λανθασμένης σημείωσης). Προστέθηκαν: πεδίο «Απαλλαγή» (σταθερό ποσό/όχημα), κάρτα «Ατυχήματα οχημάτων» (ημερομηνία/οδηγός/σχόλια/συνημμένο, μόνο TL/Director, νέο φύλλο `ΑτυχήματαΟχημάτων`), και «Παραλαβή οχήματος» (χιλιόμετρα + πολλαπλές φωτογραφίες, συμπληρώνεται από τον ίδιο τον τεχνικό στο «Στην Κατοχή μου»). Νέο KV namespace `FLEET_FILES` (R2 παρέμενε ανενεργό όταν ρωτήθηκε ο χρήστης). Λεπτομέρειες πλήρους σχεδιασμού (αποφάσεις μέσω ερωτήσεων πριν την υλοποίηση): βλ. Ρόλοι § Team Leader → Στόλος Οχημάτων και § Τεχνικός.
   **Επέκταση (11/08/2026, ρητή απαίτηση χρήστη, ίδια μέρα)**: αναθεωρήθηκε η αρχική απόφαση "μόνο παραλαβή" — προστέθηκε **«Επιστροφή οχήματος»** (χιλιόμετρα, ίδιο σημείο, ο ίδιος ο τεχνικός καταγράφει όποτε επιστρέψει το όχημα, νέα στήλη `ReturnMileage`) και **«Τρέχοντα χιλιόμετρα»** στη φόρμα οχήματος (`CurrentMileage`, ο TL την καταχωρεί αρχικά, ενημερώνεται αυτόματα σε κάθε επιστροφή — αλυσίδα παραλαβής/επιστροφής ανά τεχνικό). Το «Ιστορικό χρεώσεων» στο «Στην Κατοχή μου» δείχνει πλέον και τα δύο χιλιόμετρα (παραλαβής/επιστροφής) ΚΑΙ την Απαλλαγή του οχήματος ανά χρέωση. Επίσης προστέθηκαν links προβολής των φωτογραφιών παραλαβής για TL/Director (Αναζήτηση τεχνικού στο Εξοπλισμός, Προφίλ Τεχνικού) — το backend ήδη επέτρεπε την πρόσβαση, έλειπε μόνο το UI. Βλ. Ρόλοι § Τεχνικός και § Team Leader → Στόλος Οχημάτων.
5. ~~Μεταφορά email relay στην εταιρεία + χαλάρωση Workspace policy~~ — **ΕΓΙΝΕ** (11/08/2026): το Workspace org policy ήταν ήδη χαλαρωμένο (τσεκαρίστηκε, δεν χρειάστηκε αλλαγή), οπότε προχωρήσαμε κατευθείαν σε νέο deployment του `apps-script/EmailRelay.gs` κάτω από το εταιρικό `s.xronis@optikitec.gr` (Anyone access) + νέα secrets `EMAIL_RELAY_URL`/`EMAIL_RELAY_SECRET`. Λεπτομέρειες: βλ. §Email relay παραπάνω.
6. **Δήλωση ραντεβού τεχνικών στο συνεργείο + email στους TL όταν ορίζονται.** Να διευκρινιστεί: αφορά το tab `Συνεργεία` του portal (σημερινή σύνθεση συνεργείων, `src/crews.js`) ή σχετίζεται με το ξεχωριστό αρχείο/φύλλο «ΡΑΝΤΕΒΟΥ ΤΕΧΝΙΚΩΝ» που ήδη χρησιμοποιείται από άλλα εργαλεία εκτός portal (βλ. skills `rantevou-kpi`/`parousiasi-texnikon` — διαφορετικό Excel, όχι μέρος αυτού του codebase); Αν είναι κάτι νέο μέσα στο portal, χρειάζεται σχεδιασμός από την αρχή (ποιος δηλώνει — τεχνικός ή Backoffice/TL εκ μέρους του, τι στοιχεία έχει ένα «ραντεβού», πού αποθηκεύεται). Να ρωτηθεί πριν την υλοποίηση.

## Νέα ζητούμενα (13/08/2026, ζητήθηκαν — ΔΕΝ έχει ξεκινήσει υλοποίηση ακόμα)

Νέα λίστα από τον χρήστη. Ρητή οδηγία χρήστη: καταγραφή προς το παρόν, να προχωρήσουν με τη σειρά σε επόμενο βήμα/chat. Αρίθμηση όπως δόθηκε από τον χρήστη (δεν υπάρχει #8 — παραλείφθηκε από τον ίδιο). Σημειώσεις/ανοιχτά ερωτήματα ανά σημείο (δικές μου παρατηρήσεις, να επιβεβαιωθούν με τον χρήστη πριν την υλοποίηση):

1. ~~Φωτογραφία χιλιομέτρων (κοντέρ) στην Παραλαβή/Επιστροφή οχήματος~~ — **ΕΓΙΝΕ** (15/08/2026), αποφάσεις μέσω ερωτήσεων πριν την υλοποίηση: (α) **ξεχωριστό ΥΠΟΧΡΕΩΤΙΚΟ πεδίο**, διαφορετικό από τις ήδη υπάρχουσες γενικές `PickupPhotos` (προαιρετικές, πολλαπλές) — μία συγκεκριμένη φωτογραφία του χιλιομετρητή· (β) ισχύει **ΚΑΙ στην Παραλαβή ΚΑΙ στην Επιστροφή** — η Επιστροφή μέχρι τότε δεν είχε καμία δυνατότητα φωτογραφίας, μόνο αριθμό χιλιομέτρων. Backend: νέες στήλες `OdometerPickupPhoto{Key,Name,Type}`/`OdometerReturnPhoto{Key,Name,Type}` στο `Χρεώσεις` (**χειροκίνητο βήμα εκκρεμεί**, βλ. §Χειροκίνητα βήματα) — ΕΝΑ αρχείο ανά ενέργεια (όχι JSON array όπως το `PickupPhotos`, ίδιο 3-στηλο μοτίβο με το συνημμένο ατυχήματος στο `src/fleet.js`), ίδιο KV namespace `FLEET_FILES` με prefix `fleet:odometer-pickup:`/`fleet:odometer-return:`. Νέες `uploadOdometerPickupPhoto()`/`uploadOdometerReturnPhoto()`/`getOdometerPhoto()` στο `src/assignments.js` — ίδιοι guards με `setVehiclePickupMileage`/`setVehicleReturnMileage` (η Παραλαβή απαιτεί ανοιχτή χρέωση, η Επιστροφή απαιτεί ήδη καταχωρημένο `PickupMileage`, ΔΕΝ απαιτεί ανοιχτή χρέωση). Νέα endpoints `POST /api/charges/pickup/odometer-photo/upload`, `POST /api/charges/return/odometer-photo/upload`, `GET /api/charges/odometer-photo?chargeId&which=pickup|return` (`src/index.js`) — ίδιο dual-auth gate (owner τεχνικός Ή TL/Director) με τη λήψη γενικών φωτογραφιών. UI (`hub.html`): νέο υποχρεωτικό file input και στις δύο φόρμες (`showPickupForm`/`showReturnForm`), client-side validation πριν την υποβολή (`submitVehiclePickup`/`submitVehicleReturn`) — η μεταφόρτωση της φωτογραφίας γίνεται ΜΕΤΑ την επιτυχή καταγραφή χιλιομέτρων, δεύτερο HTTP call, ίδιο δίβημα με τα υπόλοιπα συνημμένα σε 2 βήματα. Ο σύνδεσμος «Φωτό κοντέρ» εμφανίζεται αυτόματα σε ΟΛΑ τα σημεία που ήδη δείχνουν χιλιόμετρα/φωτογραφίες παραλαβής-επιστροφής (`chargePickupCell`/`chargeReturnCell`/`chargeHistoryPickupCell`/`chargeHistoryReturnCell`, άρα και Προφίλ Τεχνικού + Αναζήτηση τεχνικού στο Εξοπλισμός) — καμία ξεχωριστή προσθήκη χρειάστηκε εκεί, ίδιο reuse με τα υπόλοιπα.
2. ~~Τεχνική Κατάρτιση σε bullets~~ — **ΕΓΙΝΕ** (13/08/2026): καθαρά αλλαγή εμφάνισης, νέα `tpTrainingBullets()` στο `hub.html` σπάει το ελεύθερο κείμενο του `TechnicalTraining` με κόμμα (ίδιο delimiter με το placeholder του πεδίου εισαγωγής "π.χ. πιστοποιήσεις, σεμινάρια, ειδικότητες") και το δείχνει ως `<ul>` στο tab Προφίλ Τεχνικού. Το πεδίο εισαγωγής (`nt-training` στο Νέος Τεχνικός) ΔΕΝ άλλαξε — παραμένει ένα ελεύθερο πεδίο κειμένου, καμία αλλαγή backend/Sheet.
3. **Documentation για ολόκληρο το portal.** Να διευκρινιστεί: για ποιον προορίζεται — εγχειρίδιο χρήστη (υπάρχει ήδη `OptikiTec_Portal_Egxeiridio_Xristis.pdf`, μήπως χρειάζεται ενημέρωση αντί για νέο) ή τεχνικό documentation αρχιτεκτονικής/API για μελλοντικό developer (κάτι σαν επέκταση αυτού του `CLAUDE.md` σε δημόσια μορφή); Και σε τι μορφή (PDF/Markdown/Word).
4. **Δεν στέλνονται τα emails τηλεργασίας — ΔΙΕΡΕΥΝΗΣΗ ΣΕ ΕΞΕΛΙΞΗ** (15/08/2026): πλήρης έλεγχος κώδικα (`submitTeleworkRequest`, `decideLinksHtmlGroup`, `additionalRecipientsForAway`, `sendEmail`) έδειξε ότι η ροή είναι δομικά ΙΔΙΑ με το `submitLeaveRequest` (που δουλεύει κανονικά) — καμία ασυμμετρία βρέθηκε στον κώδικα. Επιβεβαιώθηκε ότι η στήλη `GroupID` υπάρχει ήδη στο Sheet (άρα δεν είναι θέμα λείπουσας στήλης) και ότι το σύμπτωμα είναι: η υποβολή πετυχαίνει, ο TL δεν παίρνει ΚΑΘΟΛΟΥ email (όχι σφάλμα, όχι κουμπιά χωρίς λειτουργία — πλήρης απουσία).
   - **Ρίζα του προβλήματος (πιθανή, όχι ακόμα επιβεβαιωμένη):** το `sendEmail()` (`src/email.js`) ΠΟΤΕ δεν πετάει exception — μια αποτυχία (κενός/άκυρος παραλήπτης, σφάλμα relay) επιστρέφει σιωπηλά `{error: ...}` ή `{skipped: ...}` που ΚΑΝΕΝΑΣ caller δεν έλεγχε μέχρι τώρα, σε καμία από τις δύο συναρτήσεις. Πιο πιθανό σενάριο: το `TeamLeaderEmail` της self-row του Backoffice χρήστη στο `Υπάλληλοι` (χρησιμοποιείται για να βρεθεί ο εγκριτής μέσω `getTeamLeaderByEmail`) δείχνει σε κάτι που είτε δεν αντιστοιχεί σε πραγματικό inbox είτε παράγει άδειο `approver.Email` — θέμα δεδομένων στο Sheet, όχι κώδικα.
   - **Διαγνωστικό βήμα — ΕΓΙΝΕ** (15/08/2026): το `submitTeleworkRequest()` καταγράφει πλέον `appendAuditLog(env, "telework_email_failed", ...)` ΜΟΝΟ όταν το `sendEmail()` επιστρέψει error/skipped — με τον πραγματικό παραλήπτη που δοκιμάστηκε (`to`), το raw `TeamLeaderEmail` της self-row, και τον λόγο αποτυχίας. Επόμενο βήμα: ο χρήστης δηλώνει τηλεργασία μία φορά ξανά, μετά ελέγχει το tab «Ιστορικό Ενεργειών» (Director) για νέα εγγραφή `telework_email_failed` — αν εμφανιστεί, ο λόγος θα είναι εκεί άμεσα ορατός· αν ΔΕΝ εμφανιστεί καθόλου καμία τέτοια εγγραφή, τότε το `sendEmail()` επέστρεψε επιτυχία (το πρόβλημα θα είναι στο ίδιο το Apps Script relay/inbox παραλήπτη, όχι στον Worker).
   - **Γενικεύτηκε σε ορατότητα αποτυχημένων emails ΣΕ ΟΛΟ ΤΟ PORTAL — ΕΓΙΝΕ** (15/08/2026, ρητή απαίτηση χρήστη): το one-off διαγνωστικό της τηλεργασίας έγινε μόνιμος, κοινός μηχανισμός — νέα `logEmailFailure(env, action, actor, result, context)` στο `src/email.js`, καλείται από ΟΛΑ τα ~18 σημεία του backend που στέλνουν email (`leaves.js`, `assignments.js`, `availability.js`, `handover.js`, `reports.js`, `index.js` — νέος τεχνικός, reset PIN). Ίδια λογική με το αρχικό τηλεργασίας: καταγράφει `appendAuditLog(env, "<action>_email_failed", actor, {...context, reason})` ΜΟΝΟ όταν κάτι πήγε πραγματικά στραβά. **Σκόπιμη εξαίρεση θορύβου:** ΔΕΝ καταγράφει `skipped: "email not configured"` (θα πλημμύριζε το AuditLog — 300 εγγραφές μόνο — με χιλιάδες πανομοιότυπες γραμμές αφού θα συνέβαινε σε ΚΑΘΕ email ΚΑΘΕ ενέργειας μέχρι να μπουν τα relay secrets, χωρίς κανένα νέο σήμα πέρα από ό,τι είναι ήδη τεκμηριωμένο). Καταγράφει όμως πραγματικό `error` (σφάλμα relay/δικτύου) ΚΑΙ `skipped: "no recipients"` (π.χ. συγκεκριμένος παραλήπτης χωρίς καταχωρημένο email) — και τα δύο είναι σήμα ανά-ενέργεια. Τα cron-triggered reports (`reports.js`) καταγράφουν με `actor: "cron"` αφού δεν υπάρχει φυσικός χρήστης-ενεργοποιητής. Το παλιό `telework_email_failed` άλλαξε να καλεί το ίδιο `logEmailFailure()` αντί για δικό του inline block (καμία αλλαγή συμπεριφοράς πέρα από το φιλτράρισμα του "not configured").
5. ~~Ενεργός/Ανενεργός τεχνικός + διάστημα απασχόλησης όταν ανενεργός~~ — **ΕΓΙΝΕ** (13/08/2026), αποφάσεις μέσω ερωτήσεων πριν την υλοποίηση: μόνο TL+Director μπορούν να αλλάξουν κατάσταση (`requireTeamLeader` gate, αποκλείει Backoffice) — `POST /api/employees/status` -> νέα `setEmployeeStatus()` στο `src/leaves.js`. Νέα πεδία `Status`/`InactiveDate` στο `Υπάλληλοι` (**χειροκίνητο βήμα εκκρεμεί**, βλ. §Χειροκίνητα βήματα). Όταν γίνεται Ανενεργός: (α) το login (PIN) μπλοκάρεται αμέσως (`handleTechnicianLogin` στο `src/index.js` ελέγχει `emp.Status`), (β) εξαφανίζεται από τη δεξαμενή Συνεργείων (`handleCrewsPool`) και το dropdown Reset PIN (`handleResetPinInfo`) — και από το tab Backoffice «Διαθεσιμότητα Τεχνικών» (`getAllTechniciansAvailability()` φιλτράρει). Ρητά ΠΑΡΑΜΕΝΕΙ ορατός/αναζητήσιμος στο tab «Προφίλ Τεχνικού» (και για TL και για Director) — ΔΕΝ φιλτραρίστηκε εκεί, σκόπιμα, για ιστορικό/lookup. Το «Διάστημα Απασχόλησης» στο Προφίλ Τεχνικού υπολογίζεται πλέον από `HireDate` έως `InactiveDate` όταν ο τεχνικός είναι Ανενεργός (αντί για "σήμερα") — `tpEmploymentDuration()` στο `hub.html` (επέκταση του παλιού `tpYearsAtCompany`, ίδιο client-side calculated pattern, τίποτα αποθηκευμένο). UI: badge Ενεργός/Ανενεργός + κουμπί εναλλαγής (με `confirm()` dialog πριν την ενέργεια, μη αναστρέψιμο ως προς το login) στην κορυφή του Προφίλ Τεχνικού. Εκτός scope (δεν ζητήθηκε ρητά, ΔΕΝ αγγίχτηκε): Άδειες Ομάδας, Εξοπλισμός/Στόλος driver dropdowns, Νέος Τεχνικός TL-select — αυτά συνεχίζουν να δείχνουν ανενεργούς τεχνικούς όπως πριν, αν χρειαστεί να φιλτραριστούν κι εκείνα να ζητηθεί ρητά.
6. ~~Κουμπί «Λήψη Όλων» στις φωτογραφίες~~ — **ΕΓΙΝΕ** (13/08/2026), σκοπίμως ΧΩΡΙΣ zip (ρητή απόφαση χρήστη μετά από συζήτηση — καμία νέα εξωτερική βιβλιοθήκη, μικρός αριθμός φωτογραφιών ανά χρέωση, η χρήση είναι κυρίως TL/Director από desktop): `pickupPhotosLinksHtml()` στο `hub.html` προσθέτει σύνδεσμο «Λήψη όλων» όταν υπάρχουν 2+ φωτογραφίες παραλαβής οχήματος, ένας delegated `document` click listener ανοίγει όλα τα URLs σε νέες καρτέλες (`window.open` loop) — καλύπτει αυτόματα ΟΛΑ τα σημεία που ήδη καλούν αυτή τη συνάρτηση (ενεργές χρεώσεις στο «Στην Κατοχή μου», ιστορικό χρεώσεων, Προφίλ Τεχνικού, Αναζήτηση τεχνικού στο tab Εξοπλισμός) χωρίς ξεχωριστό wiring σε καθένα. Καμία αλλαγή backend/Sheet. Αφορά ΜΟΝΟ τις φωτογραφίες παραλαβής οχήματος — δεν ζητήθηκε ρητά και για τα συνημμένα ατυχημάτων/έγγραφα τεχνικού (εκεί υπάρχει ΕΝΑ αρχείο, όχι πολλαπλά, οπότε δεν έχει νόημα «Λήψη Όλων»).
7. ~~Νέο tab/μηχανισμός «Διαθεσιμότητα Επαρχίας» (ή «Ανακοινώσεις»;)~~ — **ΕΓΙΝΕ** (14/08/2026), σχεδιάστηκε μέσω σειράς ερωτήσεων πριν την υλοποίηση (όλα τα ανοιχτά ερωτήματα παρακάτω απαντήθηκαν ρητά): ξεχωριστό νέο tab «Ανακοινώσεις» (ΟΧΙ επέκταση του «Ενημερώσεις/Οδηγίες»). Πολλαπλές ταυτόχρονες ενεργές ερωτήσεις επιτρέπονται (από διαφορετικούς TL). ΟΛΟΙ οι TL/Director βλέπουν τη συγκεντρωτική λίστα απαντήσεων ΟΛΩΝ των ερωτημάτων, όχι μόνο ο δημιουργός. Email ειδοποίησης στέλνεται στους στοχευμένους τεχνικούς τη στιγμή δημιουργίας. Στόχευση: πολλαπλή επιλογή **ειδικοτήτων** (πεδίο `Team`, ρητή διευκρίνιση χρήστη — ΟΧΙ ομάδα αναφοράς ανά TL), όχι μόνο "όλη η εταιρεία" ή "μόνο η ομάδα του δημιουργού" όπως προτάθηκε αρχικά. Προστέθηκε επίσης δυνατότητα διαγραφής ερωτήματος από οποιονδήποτε TL/Director (ρητή απαίτηση χρήστη, δεν ήταν στην αρχική ιδέα). Λεπτομέρειες πλήρους σχεδιασμού: βλ. Ρόλοι § Team Leader → Ανακοινώσεις (ερωτήματα διαθεσιμότητας επαρχίας) και `src/availability.js`. **Εμφάνιση απαντήσεων στο Προφίλ Τεχνικού — ΕΓΙΝΕ** (15/08/2026, ρητή απαίτηση χρήστη — ΑΝΑΘΕΩΡΕΙ το αρχικό "εκτός scope" εδώ): νέα κάρτα «Απαντήσεις Ανακοινώσεων» στο tab «Προφίλ Τεχνικού» (`loadTechProfile()` στο `hub.html`), κάτω από «Στην κατοχή του τώρα» — πίνακας με Ερώτημα/Προθεσμία/Κατάσταση (Ενεργό/Έληξε)/Απάντηση (Ναι/Όχι/«δεν απάντησε ακόμα» αν ενεργό/«δεν απάντησε» αν έληξε χωρίς απάντηση). Backend: `handleEmployeeProfile()` (`src/index.js`) κάνει ΑΥΤΟΥΣΙΟ reuse του ήδη υπάρχοντος `listMyAvailabilityQueries()` (ίδια συνάρτηση με το tab «Ανακοινώσεις» του τεχνικού) απλά με το `employeeId` του επιλεγμένου τεχνικού — καμία νέα συνάρτηση/αποθήκευση. Δείχνει ΚΑΙ ενεργά ΚΑΙ ληγμένα ερωτήματα (όχι μόνο κλειδωμένα) ώστε ο TL/Director να βλέπει πλήρη εικόνα. Χειροκίνητο βήμα εκκρεμεί: δύο νέα φύλλα Sheet, βλ. §Χειροκίνητα βήματα.
9. ~~Ημερολόγιο τεχνικού με όλα τα γεγονότα~~ — **ΕΓΙΝΕ** (15/08/2026), σχεδιάστηκε μέσω διευκρινιστικών ερωτήσεων πριν την υλοποίηση: ΜΟΝΟ στο δικό του tab (όχι στο Προφίλ Τεχνικού), δείχνει άδειες/τηλεργασία + παραλαβή/επιστροφή οχήματος + ΚΤΕΟ/Service οχήματος όσο ήταν ανατεθειμένο. Λεπτομέρειες πλήρους σχεδιασμού, περιορισμοί ΚΤΕΟ/Service, και νέες στήλες Sheet: βλ. Ρόλοι § Τεχνικός → Ημερολόγιο και §Χειροκίνητα βήματα.
10. ~~Πεδίο εταιρικό νούμερο~~ — **ΕΓΙΝΕ** (13/08/2026, χειροκίνητο βήμα Sheet ολοκληρώθηκε 15/08/2026): νέο πεδίο `CompanyPhone` στο `Υπάλληλοι`, ίδιο μοτίβο με το ήδη υπάρχον προσωπικό `Phone` — προστέθηκε στο `EMPLOYEE_HEADER_ORDER` του `src/leaves.js` **ΣΤΟ ΤΕΛΟΣ** (μετά τα 3 συνημμένα, όχι δίπλα στο Phone — για να μη μετατοπιστούν οι ήδη υπάρχουσες στήλες, ίδια λογική με τις υπόλοιπες προσθήκες), `createTechnicianRecord()`/`handleEmployeesCreate` το περνάνε στη δημιουργία τεχνικού. Νέο πεδίο "Εταιρικό τηλέφωνο" στη φόρμα «Νέος Τεχνικός» (`nt-company-phone`) και νέα γραμμή στο tab «Προφίλ Τεχνικού». Label: "Εταιρικό τηλέφωνο" (επιλέχτηκε αντί για "Εσωτερικό νούμερο" — άλλαξε αν προτιμάς κάτι άλλο). Η επικεφαλίδη `CompanyPhone` προστέθηκε στο τέλος του φύλλου `Υπάλληλοι` — καμία εκκρεμότητα πια.

## Προς Συζήτηση (13/08/2026)

Θέματα που ζήτησε ρητά ο χρήστης να συζητηθούν πριν προχωρήσουν — καμία υλοποίηση, μόνο καταγραφή:

1. ~~Header με το όνομα του συνδεδεμένου χρήστη~~ — **ΑΠΟΦΑΣΙΣΤΗΚΕ: ΟΧΙ ΑΛΛΑΓΗ** (15/08/2026): παρουσιάστηκαν 3 mockups (όνομα δίπλα στο role badge / χαιρετισμός όπως στην Αρχική / μεγαλύτερη ανάδειξη στο sidebar footer) — ο χρήστης αποφάσισε να μείνει όπως είναι, το όνομα ήδη φαίνεται στο sidebar footer, δεν αξίζει αλλαγή σε κάθε tab για το όφελος. Καμία δράση.
2. ~~Συζήτηση περί database~~ — **ΑΠΟΦΑΣΙΣΤΗΚΕ: ΟΧΙ ΤΩΡΑ** (15/08/2026): δεν υπάρχει επείγων λόγος στην τρέχουσα κλίμακα (~40 τεχνικοί) — τα πραγματικά όρια του Google Sheets API (10 εκατ. κελιά/spreadsheet, ~300 read req/λεπτό) απέχουν πολύ. Το πραγματικό ρίσκο είναι σταδιακή επιβράδυνση καθώς μεγαλώνουν φύλλα όπως `Άδειες`/`Χρεώσεις`/`AuditLog` (το `readSheetAsObjects()` διαβάζει ολόκληρο το φύλλο σε κάθε request, καμία indexed αναζήτηση/cache) — UX πρόβλημα, όχι διακοπή λειτουργίας. Ο χρήστης επιβεβαίωσε ότι είναι "future-proofing" σκέψη, όχι υπαρκτό πρόβλημα σήμερα. Αν χρειαστεί ποτέ πραγματική μετάβαση, φυσικό επόμενο βήμα είναι Cloudflare D1 (SQLite, ίδιο account) — αλλά σημαίνει ουσιαστικά ξαναγράψιμο κάθε backend module + migration script + απώλεια της σημερινής άμεσης πρόσβασης/επεξεργασίας του Sheet με το χέρι. Θα το επανεξετάσουμε μαζί με το #4 (storage) αν εμφανιστεί πραγματικό σύμπτωμα (αργές σελίδες, σφάλματα quota) — καμία δράση προς το παρόν.
3. **Εξοδολόγιο σε Team Leaders ΚΑΙ Τεχνικούς** — παραμένει προς σχεδιασμό, όχι έτοιμο για υλοποίηση. Ερωτήματα διευκρίνισης απαντήθηκαν εν μέρει (15/08/2026): είδη εξόδων = ημερήσιο εκτός έδρας (επαρχία), καύσιμα/διόδια, γενικά έξοδα/αποδείξεις, ΚΑΙ ρητά "δεν ξέρουμε ακόμα, να το σκεφτούμε" (άρα το scope δεν έχει κλειδώσει) — ρόλοι: **TL και τεχνικοί εξίσου υποβάλλουν** (όχι μόνο TL). Λόγω μεγέθους/ασάφειας, συμφωνήθηκε να ΜΗΝ σχεδιαστεί πρόχειρα εδώ — χρειάζεται δική του αφιερωμένη συνεδρία με πλήρη σειρά διευκρινιστικών ερωτήσεων πριν την υλοποίηση (ίδια μέθοδος με Ημερολόγιο τεχνικού/Ανακοινώσεις) — πιθανό σχήμα (μη αποφασισμένο ακόμα): νέο φύλλο Sheet, νέο tab σε τεχνικό+TL, ροή έγκρισης πιθανόν ίδιο μοτίβο με τις άδειες, ίσως μηνιαία αναφορά προς λογιστή όπως και οι άδειες.
4. ~~Storage~~ — **ΑΠΟΦΑΣΙΣΤΗΚΕ: ΟΧΙ ΤΩΡΑ** (15/08/2026, ίδιο συμπέρασμα με το #2): το Google Sheet έχει το μόνο πραγματικό όριο (10 εκατ. κελιά/spreadsheet), πολύ μακριά στην τρέχουσα κλίμακα. Το Cloudflare KV (namespaces `LEAVE_FILES`, `EMPLOYEE_FILES`, `FLEET_FILES`, `ANNOUNCEMENTS_FILES`) δεν έχει ουσιαστικό όριο συνολικής χωρητικότητας (χρέωση ανά GB), μόνο όριο 25MB ανά μεμονωμένο αρχείο — ήδη μέσα σε αυτό αφού τα uploads περιορίζονται στα 20MB (`LEAVE_ATTACHMENT_ALLOWED_TYPES`, `EMPLOYEE_DOCUMENT_ALLOWED_TYPES`). Καμία δράση προς το παρόν — το R2 (ανενεργό ακόμα στο Cloudflare account) παραμένει ασυσχέτιστο θέμα, βλ. §Ενημερώσεις/Οδηγίες.
5. ~~Push Notifications~~ — **ΑΠΟΦΑΣΙΣΤΗΚΕ: ΑΝΑΒΑΛΛΕΤΑΙ** (15/08/2026): εφικτό (web push + service worker + VAPID keys, νέο KV/Sheet για subscriptions ανά συσκευή), αλλά ουσιαστικό εμπόδιο — στο iOS Safari το web push δουλεύει ΜΟΝΟ αν ο χρήστης έχει κάνει "Προσθήκη στην Αρχική οθόνη" πρώτα (καμία λειτουργία σε απλή καρτέλα Safari), κάτι που σε ~40 τεχνικούς σε κινητά σημαίνει είτε καθολική εκπαίδευση είτε ανομοιόμορφη κάλυψη. Ο χρήστης αποφάσισε να μην προχωρήσει τώρα — παραμένει αποκλειστικά email το κανάλι ειδοποιήσεων. Αν χρειαστεί ξανά στο μέλλον, να σχεδιαστεί ως ξεχωριστό μεγάλο feature.
6. ~~Διαχωρισμός εξοπλισμού σε κατηγορίες~~ — **ΕΓΙΝΕ** (15/08/2026, ρητή απαίτηση χρήστη μετά από ερώτηση): το `Category` παρέμενε ελεύθερο πεδίο/dropdown (καμία αλλαγή εκεί, ήδη αρκούσε). Προστέθηκε νέο πεδίο **«Σειριακός/Κωδικός»** (`SerialNumber`, νέα στήλη στο τέλος του `Εξοπλισμός` — χειροκίνητο βήμα, βλ. §Χειροκίνητα βήματα) στη φόρμα προσθήκης/επεξεργασίας αντικειμένου ΚΑΙ ως νέα στήλη στον πίνακα εξοπλισμού (`hub.html` → tab Εξοπλισμός). **Σχεδιαστική απόφαση (ρητή επιλογή χρήστη μετά από 2 προτάσεις):** ΕΝΑ γενικό πεδίο για όλες τις κατηγορίες (όχι ξεχωριστή στήλη ανά κατηγορία, π.χ. IMEI/SerialNumber χωριστά) — μόνο το ΕΜΦΑΝΙΖΟΜΕΝΟ label στο UI αλλάζει δυναμικά ανάλογα με την επιλεγμένη Κατηγορία (`eqSerialLabel()`: «IMEI» για Κινητό, «Σειριακός αριθμός» για Tablet, γενικό «Σειριακός/Κωδικός» για οτιδήποτε άλλο) — καμία αλλαγή σχήματος χρειάζεται όταν προστεθεί μελλοντικά νέα κατηγορία. Προαιρετικό πεδίο, καμία επικύρωση μορφής (π.χ. δεν ελέγχεται αν το IMEI έχει 15 ψηφία).

## Στυλ

Login: σκούρο navy με fiber-optic ακτίνες. Hub: ανοιχτό περιεχόμενο + σκούρο sticky sidebar, accent `#2E9BFF`.
Όλο το UI και τα commit messages στα ελληνικά.
